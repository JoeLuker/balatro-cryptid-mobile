# Build artifacts (no trailing slash: also matches the self-symlink in linked worktrees)
/build

# Downloaded sources (can be re-fetched)
/src/Balatro.love
/src/base.apk
/src/dump

# Downloaded mods (can be re-fetched)
/mods

# Signing keys (machine-specific, preserves app identity across rebuilds)
/keys/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Ghost session data (local only)
.ai-sessions/

# Claude Code working dir: linked worktrees (with self-symlinks), settings, scratch shots
/.claude/

# Pre-DebugPlus backup of the (re-fetchable) decompiled source dump — mirrors /src/dump
/src/dump.bak-*

# Lovely injector: downloaded/built binaries (.so, tarball) + the upstream source clone
# (314M, its own git repo). All re-fetchable — see tools/uiref for the committed toolchain.
/tools/lovely/
/tools/lovely-src/
