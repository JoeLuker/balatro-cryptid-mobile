============ hunt:draw-path ============
[HIGH] "time" uniform is ID-constant but recomputed + sent on every draw_shader call
  where: build/game/engine/sprite.lua:110
  claim: G.SHADERS[_shader]:send("time", 123.33412*(_draw_major.ID/1.14212 or 12.5123152)%3000) — the value depends only on _draw_major.ID which is assigned once at Node:init and never changes. It is currently recomputed and sent to the GPU driver on every draw_shader invocation: ~2 draws/card x ~33 visible cards x 60 FPS = ~4000 :send() calls/frame for a constant value.
  cost_when: Every frame during any game state with visible cards. Rough magnitude: 4000 redundant :send() calls at ~50ns Mali driver overhead each = ~200 microseconds/frame wasted in driver validation alone.
  measure: Add timer_checkpoint('pre_draw_shader', 'draw') and timer_checkpoint('post_draw_shader', 'draw') around the 10 :send() block in sprite.lua:101-118. With G.SETTINGS.perf_mode=true, compare the 'pre->post' bucket before and after caching. Alternatively: adb logcat | grep timer_checkpoint and count 'canvas-uiboxes' interval.
  fix: At Card:init (or after ID is assigned), store self.ARGS.shader_time = 123.33412*(self.ID/1.14212)%3000. In draw_shader, replace the inline expression with G.SHADERS[_shader]:send('time', _draw_major.ARGS and _draw_major.ARGS.shader_time or 0). Risk: trivial; ID is permanently fixed. For shared sprites (seals, stickers) _draw_major is set to the card right before draw_shader, so _draw_major.ARGS.shader_time is always valid.
  risk: None — the shader formula is purely ID-based, no side effects from caching.
[HIGH] "screen_scale" uniform sent per-sprite per draw_shader despite being frame-constant for 95% of cards
  where: build/game/engine/sprite.lua:107
  claim: G.SHADERS[_shader]:send('screen_scale', G.TILESCALE*G.TILESIZE*(_draw_major.mouse_damping or 1)*G.CANV_SCALE) — mouse_damping is 1.5 only for Booster cards (card.lua:468), all other cards get *1. G.TILESCALE, G.TILESIZE, G.CANV_SCALE do not change within a frame. So this multiplication runs ~4000 times/frame producing two distinct constant values (base and boost).
  cost_when: Every frame, every draw_shader call (~4000x/frame for 33 visible cards + UI sprites). Minor mul cost; the real cost is the :send() driver call itself on Mali.
  measure: At frame start (game.lua:2940 area), compute G._DRAW_SCREEN_SCALE = G.TILESCALE*G.TILESIZE*G.CANV_SCALE and G._DRAW_SCREEN_SCALE_BOOST = G._DRAW_SCREEN_SCALE*1.5. Instrument the 'uiboxes'->'rest' perf_mode window before/after. Also measure via `adb shell simpleperf stat -e cache-misses,instructions -p <pid>` to detect reduced instruction count.
  fix: Precompute G._DRAW_SCREEN_SCALE = G.TILESCALE*G.TILESIZE*G.CANV_SCALE once at frame start. In sprite.lua:107 replace with: G.SHADERS[_shader]:send('screen_scale', (_draw_major.mouse_damping or 1) == 1 and G._DRAW_SCREEN_SCALE or G._DRAW_SCREEN_SCALE*1.5). Risk: if CANV_SCALE or TILESCALE change mid-frame (they don't in normal play) the cached value would be stale.
  risk: Low — CANV_SCALE/TILESCALE only change on orientation/resize events, not mid-frame.
[HIGH] setShader() + setShader(nil) called per sprite even when adjacent sprites use the same shader
  where: build/game/engine/sprite.lua:133, 141
  claim: Every draw_shader call ends with love.graphics.setShader() (nil/clear) at line 141, then immediately the next draw_shader binds the same shader again at line 133. For a plain joker card: center->dissolve then clear, front->dissolve then clear = 4 setShader calls to draw 2 sprites with the same shader. For 33 cards * 2 draws = 132 unnecessary bind/unbind pairs per frame.
  cost_when: Every frame. On Mali-G710, each setShader involves GL program bind + driver state validation. Rough estimate: ~100-200ns per call, 264 calls = 26-53 microseconds/frame. Worst case with many-edition cards (holo+foil+seal = 6+ passes): scales linearly.
  measure: Use `adb shell dumpsys gfxinfo <package>` to get frame rendering stages. Add a logcat counter inside sprite.lua:133 (increment G._SHADER_BINDS) and log it every 60 frames: `if G.FRAMES.DRAW % 60 == 0 then print('shader_binds/frame:', G._SHADER_BINDS/60) G._SHADER_BINDS=0 end`.
  fix: Track G._CURRENT_SHADER as a global. In draw_shader line 133: if G._CURRENT_SHADER ~= _shader then love.graphics.setShader(G.SHADERS[_shader], G.SHADERS[_shader]); G._CURRENT_SHADER = _shader end. At frame end or before any non-sprite draw: love.graphics.setShader(); G._CURRENT_SHADER = nil. Remove the per-call setShader() at line 141. Requires auditing all call sites that assume the shader is cleared after draw (the G.under_overlay and debug paths in game.lua need the shader cleared before they run).
  risk: Medium — any draw code that implicitly expects a cleared shader after a sprite draw would break. The fix needs careful auditing of game.lua draw order (the canvas sections at 3135+ must ensure shader is nil). Test thoroughly with edition+seal cards which interleave multiple shaders.
[MEDIUM] SMODS DrawStep dispatch: 22 check_conditions() calls per Card:draw() regardless of card type
  where: build/game/SMODS/_/src/card_draw.lua:512-514
  claim: Card:draw() iterates all 22 entries in SMODS.DrawStep.obj_buffer and calls check_conditions(card, layer) on each. check_conditions walks pairs(self.conditions) (2-3 entries) for every step even when the step is irrelevant (e.g. all 'facing=front' steps run check_conditions for a back-facing deck card). For 33 cards * 22 steps * 2 layers = 1452 check_conditions invocations per frame.
  cost_when: Every frame, all visible cards. Each check_conditions = 2-3 pairs() iterations + string comparisons. At 33 cards: ~1450 Lua function calls and ~3000 table lookups purely for dispatch overhead.
  measure: Add G._DS_CALLS counter in the Card:draw loop and log per 60 frames. Compare 'uiboxes' timer_checkpoint before/after a patch that caches per-card which steps are applicable (or skips via early returns). The baseline checkpoint 'uiboxes'->'rest' should tighten measurably.
  fix: The minimal fix: in check_conditions, short-circuit by layer before iterating conditions — most steps have layers={card=true, both=true} or {shadow=true, both=true}, so a layer mismatch can bail out immediately without a pairs loop. The deeper fix: build a per-card 'applicable steps' bitmask at card creation or after state changes (edition, seal, etc.) — Card:draw would then only call into steps that are actually enabled for this card's current state. Risk of bitmask approach: must invalidate/rebuild when card state changes (edition added/removed, vortex toggle, etc.).
  risk: Medium for bitmask approach (invalidation logic). Low for simple layer short-circuit.
[MEDIUM] "edition" DrawStep iterates all G.P_CENTER_POOLS.Edition entries for every front-facing card, even cards with no edition
  where: build/game/SMODS/_/src/card_draw.lua:248
  claim: for k, v in pairs(G.P_CENTER_POOLS.Edition) do ... end runs for every front-facing non-vortex card. The outer guard is `if edition then` but that's inside the loop. If self.edition is nil, the `if edition then` block is skipped, but the pairs() traversal still happens. With Cryptid adding ~10+ custom editions, G.P_CENTER_POOLS.Edition has ~14+ entries. For 15 front-facing cards: 210 pairs() iterations per frame that immediately skip.
  cost_when: Every frame during a run with visible front-facing cards (jokers, hand cards, shop). With many Cryptid editions loaded: proportionally worse.
  measure: Count via G._EDITION_ITERS counter inside the pairs loop, log per 60 frames. Compare baseline vs patched (move the nil guard outside the loop).
  fix: Move the edition nil check outside the pairs loop: in card_draw.lua:244-265, change `local edition = self.delay_edition or self.edition; if edition then for k, v in pairs(...) do` — this is already structured that way but verify the opening `if edition then` is at line 245, BEFORE the pairs loop at 248. If so, this is already guarded correctly. Check the actual loaded file (SMODS/_/src) to confirm the guard covers the loop entry.
  risk: None if the guard is already correct — just a code-reading verification issue. If the guard is missing or misplaced, a one-line fix.
[MEDIUM] "stickers" DrawStep iterates all 8 SMODS.Stickers for every front-facing card regardless of whether the card has any sticker
  where: build/game/SMODS/_/src/card_draw.lua:320
  claim: for k, v in pairs(SMODS.Stickers) do if self.ability[v.key] then ... end end runs for every front-facing card per frame. SMODS.Stickers has 8 entries (eternal, perishable, rental, pinned, plus mod stickers). For a plain joker with none of these: 8 pairs() iterations + 8 table lookups that all return nil. For 15 front-facing cards: 120 wasted iterations/frame.
  cost_when: Every frame during a run. Increases proportionally with more mod stickers registered.
  measure: Add counter inside the pairs loop before the ability check, log per 60 frames. The 'uiboxes'->'rest' checkpoint should narrow by a small but measurable amount.
  fix: Pre-check: add a flag self._has_any_smods_sticker at card init / when sticker is set. In the stickers DrawStep, gate the entire pairs loop: `if self._has_any_smods_sticker then for k, v in pairs(SMODS.Stickers) do ... end end`. Set the flag in Card:set_ability or whenever self.ability.eternal/perishable/rental etc. changes. Risk: must keep flag in sync with ability table changes.
  risk: Low — flag invalidation is straightforward since sticker changes happen at well-defined events.
[LOW] Shadow DrawStep computes send_to_shader arithmetic and iterates all card children unconditionally even when shadows=Off
  where: build/game/SMODS/_/src/card_draw.lua:48-62
  claim: The shadow DrawStep func always runs (1) send_to_shader[1] arithmetic (math.min + math.sin + conditionals), (2) send_to_shader[2] = G.TIMERS.REAL, (3) a pairs(self.children) loop setting VT.scale on 3-5 children, and (4) G.shared_shadow assignment — before checking `if shadows == 'On'`. When shadows are Off (the mobile default), none of these sends are used by the shadow GPU draw, but the children VT.scale loop IS still needed for child sprite scaling. The purely wasted part is the send_to_shader arithmetic when the shadow draw is skipped AND when no edition/debuff/etc. shader will use send_to_shader this frame.
  cost_when: Every frame for every visible card when shadows=Off. ~33 cards * (2 arithmetic ops + 1 G.TIMERS lookup + 3-5 table writes) = ~200 Lua ops/frame. Minor absolute cost but zero benefit when shadows=Off and no edition shaders fire.
  measure: Verify: add a G._SHADOW_BLOCKED counter that increments when the draw_shader at line 61 is skipped due to shadows=Off. Log it. If it equals total card count every frame, the arithmetic at 48-52 is always wasted.
  fix: Lazily compute send_to_shader: move lines 48-52 out of the shadow step and into the first step that actually needs them (center, edition, etc.). The children VT.scale loop (52-54) can stay as a standalone DrawStep with no shadow dependency. send_to_shader would then be computed on-demand the first time a shader pass uses it within the frame (e.g. gate it: `if not self.ARGS.send_to_shader_ready then ... self.ARGS.send_to_shader_ready=true end` with send_to_shader_ready reset in the drawhash_boundingrect step).
  risk: Low — the change is internal to DrawStep execution order. Must verify no step relies on send_to_shader being pre-populated before it reads it.
[LOW] ambient_tilt cards compute cos/sin per frame unconditionally in the tilt DrawStep
  where: build/game/SMODS/_/src/card_draw.lua:89-93
  claim: For cards with self.ambient_tilt set (truthy non-zero), each frame computes: G.TIMERS.REAL*(1.56 + (ID/1.14212)%1) + ID/1.35122 (tilt_angle), then math.cos(tilt_angle), math.sin(tilt_angle), and a third math.cos for amt. These trig calls are cheap individually but tilt_angle changes every frame so cannot be cached. ambient_tilt is currently set only for G.SANDBOX.joker (game.lua:1479) and cleared to 0 for replace_card transitions (1658, 1848). 0.0 is falsy in Lua so those clear correctly.
  cost_when: Only while ambient_tilt cards are on screen (sandbox mode primarily, possibly during specific animations). Low risk in normal gameplay.
  measure: Check during sandbox mode: add logcat counter when the ambient_tilt branch fires. The 'canvas' timer_checkpoint delta before/after is the baseline.
  fix: No change warranted for normal gameplay — ambient_tilt is rare. If sandbox performance matters, the tilt_angle's per-ID offset (ID/1.35122) IS constant and could be cached as self._ambient_tilt_phase at card init.
  risk: None for the phase caching; nominal fix.
============ hunt:scoring-path ============
[HIGH] Dead copy_table(ability) on every calculate_joker call
  where: build/game/Mods/Cryptid/lib/calculate.lua:137
  claim: local orig_ability = copy_table(active_side.ability) performs a full deep recursive copy (including setmetatable re-application) of the joker ability table. orig_ability is assigned and never read again — confirmed by tracing all references in the function. copy_table is O(depth*width) of the ability subtree.
  cost_when: Every calculate_joker invocation, which fires for every joker against every scored card and retrigger. With 5 jokers and a modest hand: 20-50 deep copies per hand scored. ability tables typically have 10-30 fields plus nested subtables, so each copy is non-trivial. Cost scales linearly with joker count.
  measure: In perf_mode: add timer_checkpoint('pre_calc_joker') and timer_checkpoint('post_calc_joker') bracketing the Cryptid wrapper body. Compare flamegraph with line 137 present vs deleted. Alternatively: add logcat print of os.clock() diff inside the wrapper for 100 invocations.
  fix: Delete line 137 (and the unused local in_context_scaling on line 138). Zero correctness risk — the variable is provably dead. If future code ever needs ability rollback, reconstruct at that point.
  risk: Zero — variable is never consumed. Search for 'orig_ability' in calculate.lua confirms single assignment, no reads.
[HIGH] find_joker linear scan on every calculate_joker call regardless of ownership
  where: build/game/Mods/Cryptid/lib/calculate.lua:112
  claim: next(find_joker('cry-Flip Side')) is called unconditionally on every calculate_joker invocation. find_joker (misc_functions.lua:1044-1058) does a full linear scan of G.jokers.cards + G.consumeables.cards by name match. The result is only used to branch Flip Side behavior; when Flip Side is not owned the scan is entirely wasted.
  cost_when: Every calculate_joker call. With J total jokers * N cards scored * R retrigers: O(J*N*R) scans, each O(J+consumables). With 5 jokers, 5 scored cards, no retrigers: 25 scans * 5+N = 125-175 string comparisons per hand. Scales quadratically with joker count.
  measure: perf_mode checkpoint bracketing the Cryptid calculate_joker wrapper. Before: log invocation count and os.clock() delta. After caching: compare. Or: monkey-patch find_joker to increment a global counter and log it at end of scoring coroutine.
  fix: At the top of evaluate_play (state_events.lua) or in the Cryptid eval_card wrapper entry point, set G._cry_flip_side_card = find_joker('cry-Flip Side') once per scoring pass. Clear it on exit. Replace line 112 with if G._cry_flip_side_card then. Risk: if a joker can be gained/lost mid-scoring (Cryptid edge case), the cache could be stale — acceptable because Flip Side appearing mid-hand is not a supported game mechanic.
  risk: Low. Mid-hand joker creation is possible in edge cases but Flip Side is not created mid-hand. Add a safety check in any joker-gain hook if needed.
[HIGH] OmegaNum table allocated for every to_big() call including sub-1e15 values like 2000 chips
  where: build/game/Mods/Talisman/talisman.lua:524-548 (to_big), build/game/Mods/Talisman/big-num/omeganum.lua:85-87,221-308,608-618, build/game/card.lua (22 active to_big call sites)
  claim: With break_infinity='omeganum' active (talisman.lua:170-172), every to_big(x) call routes through Big:create(x) -> Big:new({x}) -> setmetatable({array={x}, sign=1}, OmegaMeta) -> :normalize(). normalize() runs multiple while loops and arraySize() which uses a pairs() iteration. A value like 2000 goes through the full OmegaNum machinery despite being representable as a plain Lua double. card.lua has 22 to_big() call sites in hot joker eval, many as paired comparisons: to_big(self.ability.extra) < to_big(1) allocates two OmegaNum tables to compare two numbers that are certainly < 1e15.
  cost_when: Every joker evaluation that uses to_big comparisons or arithmetic, which is the majority of card.lua:calculate_joker. With 22 sites * 5 jokers * 5 cards = 550+ OmegaNum object creations per hand at normal chip counts. Each object is GC pressure; paired with nuGC's stop-after-step policy this means accumulating dead OmegaNum tables between GC windows.
  measure: Add collectgarbage('count') logging at scoring coroutine start and end (before first yield, after last). Compare heap delta with to_big guarded vs unguarded. Or: in perf_mode, bracket the calculate_joker inner body and count to_big allocations with a monkey-patch counter.
  fix: In card.lua hot path comparisons where both operands are known Lua numbers (ability.extra, ability.x_mult, etc.), replace to_big(a) OP to_big(b) with plain a OP b. For arithmetic sites, only call to_big when the result needs to be added to a running big-num chip total. A helper: local function num_gt(a,b) return type(a)=='number' and type(b)=='number' and a>b or to_big(a)>to_big(b) end. Alternatively, add a fast-path in to_big itself: if type(x)=='number' and math.abs(x) < 1e15 then return x end and make big-num operators tolerate plain numbers as operands.
  risk: Medium. Changing to_big semantics requires verifying all downstream operators handle plain-number operands. The card.lua call-site substitution approach is safer but more lines changed. Start with the to_big fast-path approach and test with a chip-score that crosses 1e15.
[MEDIUM] Cryptid SMODS.calculate_context double-iterates SMODS.Events (11 entries) on every call even when no events are active
  where: build/game/Mods/Cryptid/lib/calculate.lua:52-107
  claim: Two separate for k, v in pairs(SMODS.Events) loops run on every SMODS.calculate_context call. Each loop body checks G.GAME.events[k] which is nil in the vast majority of game states (events are transient). SMODS.calculate_context is called from state_events.lua 16+ times per scoring pass, plus recursively from the other_joker cross-product loop.
  cost_when: Every SMODS.calculate_context invocation during scoring. 16 direct calls in state_events.lua * O(J^2) inner invocations = potentially hundreds of calls per hand. Each call runs 22 loop iterations (11 per loop) checking nil table lookups. Overhead is small per call but accumulates across the full scoring pass.
  measure: Monkey-patch the top of the Cryptid SMODS.calculate_context wrapper to increment a counter; log total at coroutine end. Then add os.clock() timing inside both loops. Compare total loop time with and without the next(G.GAME.events) guard.
  fix: Wrap both pairs(SMODS.Events) loops with: if G.GAME.events and next(G.GAME.events) then ... end. When no events are active this short-circuits all 22 iterations. Risk: if an event fires exactly at the calculate_context call and G.GAME.events is populated mid-scoring, the guard correctly allows the loop to run.
  risk: Low. The guard condition is the same check the loop body effectively does — it just hoists it.
[MEDIUM] get_card_areas allocates a fresh 3-element table on every call inside O(J^2) inner loop
  where: build/game/SMODS/_/src/utils.lua:2161-2163, called from build/game/functions/state_events.lua:847
  claim: get_card_areas('jokers') executes local t = {G.jokers, G.consumeables, G.vouchers} creating a new table on every invocation. It is called inside the other_joker cross-product loop in state_events.lua — for each outer joker card, for each joker area, calling eval_card which calls SMODS.calculate_context which calls get_card_areas. The areas themselves (G.jokers, etc.) do not change during scoring.
  cost_when: O(J^2 * areas) calls per scoring pass. With 5 jokers * 3 areas * 5 scored cards = 75 fresh table allocations for the other_joker section alone. Low per-table cost but adds to GC pressure alongside OmegaNum churn.
  measure: Monkey-patch get_card_areas to increment a counter and log at scoring end. Compare heap-delta (collectgarbage('count')) with cached vs fresh table approach.
  fix: Cache the result of get_card_areas per type as a module-level upvalue: SMODS._card_areas_cache = {}. Populate lazily on first call per type and return the cached table. Invalidate the cache when card areas change (hook G.jokers/G.consumeables assignment, or simply rebuild once per love.update frame rather than per-call).
  risk: Low if cache is invalidated correctly. During scoring, areas do not change, so a per-scoring-pass cache (built at coroutine start, cleared at end) is safe.
[MEDIUM] collectgarbage('count') called every frame while scoring coroutine is live despite threshold being unreachable
  where: build/game/Mods/Talisman/talisman.lua:696
  claim: if collectgarbage('count') > 1024*1024 — threshold is ~1 GB Lua heap. LuaJIT's GC heap on a mobile game will never reach this. The collect branch never fires. But collectgarbage('count') itself is not free: it returns current heap usage which requires a GC traversal of the allocation pool. This runs every frame (60 Hz) while the coroutine is alive.
  cost_when: Every love.update frame during the entire scoring coroutine lifetime. Scoring a hand with 5 jokers takes ~10-30 frames. This is 10-30 redundant collectgarbage('count') calls per hand.
  measure: Comment out lines 696-698 and compare E_MANAGER timing via perf_mode over 100 hands. Also compare collectgarbage('count') heap readings at coroutine start/end to verify nuGC is sufficient without this guard.
  fix: Delete lines 696-698. nuGC (game.lua:2617 -> misc_functions.lua:718) already runs a budgeted GC step every frame with disable_otherwise=true, making this guard redundant and contradictory (nuGC stops the GC after its budget; this code would immediately restart a full collect if the threshold were ever reached).
  risk: Near-zero. The threshold is 1 GB — effectively dead code. nuGC covers the actual GC policy.
[MEDIUM] CARD_CALC_COUNTS re-summed via pairs() every frame while overlay is shown, plus OmegaNum alloc for elapsed-time float
  where: build/game/Mods/Talisman/talisman.lua:744-752
  claim: Lines 744-748: for i, v in pairs(G.CARD_CALC_COUNTS) do totalCalcs = totalCalcs + v[1] end re-computes the sum from scratch every frame. Line 752: number_format(G.CURRENT_CALC_TIME) passes a Lua float (elapsed seconds, e.g. 0.47) through to_big/number_format which in OmegaNum mode creates a full OmegaNum table for a sub-1 float every frame.
  cost_when: Every love.update frame while G.OVERLAY_MENU is shown (i.e., from 0.3s into a long scoring pass until the player exits). At 60 Hz with 10 cards in CARD_CALC_COUNTS: 600 pairs() iterations per second plus 60 OmegaNum allocs/sec for the time display.
  measure: perf_mode checkpoint bracketing lines 744-754. Record frame time with overlay shown vs hidden over 60 frames. Compare incremental-counter approach (no pairs loop) vs current.
  fix: Maintain G.CURRENT_TOTAL_CALCS as an incremental integer: increment it by v[1] at the point where new entries are inserted into CARD_CALC_COUNTS (in the Card:calculate_joker wrapper at talisman.lua:785-814). Remove the re-sum loop. For the time display, use tostring(math.floor(G.CURRENT_CALC_TIME * 10) / 10) .. 's' directly, bypassing number_format/to_big entirely for a value that is always a small float.
  risk: Low. CURRENT_TOTAL_CALCS incremental approach requires finding all insertion sites for CARD_CALC_COUNTS (one primary site in the Talisman calculate_joker wrapper). The time display bypass is independent and zero-risk.
[MEDIUM] Fresh context table allocated for every eval_card call in O(J^2) other_joker cross-product loop
  where: build/game/functions/state_events.lua:854,862
  claim: Each eval_card(_joker, {full_hand=..., scoring_hand=..., scoring_name=..., poker_hands=..., [other_joker]=_card, ...}) constructs a new table literal for every invocation in the inner other_joker loop. The base fields (full_hand, scoring_hand, scoring_name, poker_hands) are identical across all iterations; only the [other_joker/_card] field changes. This pattern repeats for the other_card, joker_main, and individual loops in the same function.
  cost_when: O(J * areas * N_scored) table allocations for the other_joker section. With 5 jokers * 3 areas * 5 scored cards: ~75 tables per outer joker iteration = 375 total for the section. Each table has ~6 fields. This is the densest allocation site in the hot path outside of OmegaNum.
  measure: collectgarbage('count') at scoring coroutine start vs end (already available). Alternatively: instrument state_events.lua with a table-creation counter upvalue and log at end. Compare with hoisted base-context approach.
  fix: Hoist base context table outside the inner loop: local ctx = {full_hand=full_hand, scoring_hand=scoring_hand, scoring_name=scoring_name, poker_hands=poker_hands}. Inside the loop, set ctx[other_joker]=_card before eval_card, then clear it after: ctx[other_joker]=nil. This reuses one table instead of allocating N. Requires verifying eval_card and all callee wrappers do not retain a reference to the context table after returning (they should not — context is consumed synchronously).
  risk: Medium. Must audit that no wrapper caches the context table reference. SMODS context stack push/pop copies the reference — confirm it only reads, does not mutate after the call returns. If any wrapper writes back to the context table post-call, mutation reuse is unsafe.
============ hunt:update-path ============
[HIGH] remove_nils(self.ANIMATIONS) return value discarded — pure wasted allocation every frame
  where: build/game/game.lua:2816
  claim: remove_nils() allocates a new table, iterates all animated sprites into it, then returns it; the call site discards the return value so G.ANIMATIONS is never compacted. Because AnimatedSprite:remove() uses table.remove() (no nil holes ever exist), the compaction would also be a no-op if captured. The whole call is dead work.
  cost_when: Every frame, always. At ~30-60 animated sprites in-run (jokers, blinds, backgrounds), this is one ~60-element table + header allocated and immediately GC-queued each frame — ~600–1200 bytes/frame added to GC pressure, roughly 1 extra nuGC step/frame at 300ms budget.
  measure: Enable G.SETTINGS.perf_mode. timer_checkpoint already surrounds the animate block (lines 2821). Baseline: check 'animate' checkpoint TTC and collectgarbage('count') delta across 300 frames. Then delete line 2816 and compare. Alternatively: logcat with G.PREV_GARB delta printed; the animate checkpoint already tracks GC KB consumed.
  fix: Delete game.lua:2816. The line is both semantically inert (return discarded) and structurally redundant (G.ANIMATIONS has no holes). Zero risk.
  risk: None. AnimatedSprite:remove() already keeps G.ANIMATIONS hole-free via table.remove().
[HIGH] SMODS.enh_cache:clear() allocates a fresh metatable literal {__mode='k'} every frame
  where: build/game/Mods/Steamodded/src/utils.lua:1012, called from game.lua:2627
  claim: enh_cache:clear() executes `self.data = setmetatable({}, { __mode = 'k' })` — both the data table AND the metatable `{__mode='k'}` are new allocations every call. Since clear() is called unconditionally from Game:update every frame, this is 2 table allocations per frame just for the clear, before any cache reads.
  cost_when: Every frame, unconditionally (game.lua:2627 is outside all state checks). At 60fps that is 120 tables/second from the clear alone. Additionally, every cache read (enh_cache:read) does copy_table() on the cached result — during HAND_PLAYED scoring with 5 cards × 10+ jokers this can be 50+ copy_table calls per scoring event.
  measure: Add timer_checkpoint('pre_enh_clear','update') at game.lua:2626 and timer_checkpoint('post_enh_clear','update') at 2628. GC delta (collectgarbage('count') in the checkpoint label) will show KB consumed by clear alone. For scoring cost: add checkpoints around E_MANAGER:update during HAND_PLAYED state; enh_cache reads dominate the scoring GC delta.
  fix: Two independent fixes: (1) Cache the metatable as a module-level upvalue in utils.lua: `local _weak_mt = {__mode='k'}` and change clear() to `self.data = setmetatable({}, _weak_mt)` — saves 1 table/frame. (2) Make enh_cache:read() return a direct reference (not copy_table) when the caller does not mutate the result — most callers only iterate it. Audit call sites; the only mutation-unsafe path is calculate_quantum_enhancements which already removes a key.
  risk: (1) Zero risk — metatable is never mutated after creation. (2) Medium: requires auditing all get_enhancements call sites for mutation; can be done incrementally.
[MEDIUM] G.MOVEABLES iterated twice per frame with pairs() instead of ipairs() on a sequence table
  where: build/game/game.lua:2832 and 2837
  claim: G.MOVEABLES is a pure sequence (all insertions via table.insert, line moveable.lua:80). Both move and update loops use `for k, v in pairs(self.MOVEABLES)` which forces LuaJIT to use the generic hash-table iterator, preventing the compiler from emitting the tighter counted-loop form. ipairs on a sequence is a JIT-compiled counted loop in LuaJIT 2.1.
  cost_when: Every frame, both loops. Typical in-run MOVEABLES count is ~200-400 objects (every Card, UIElement, Sprite, CardArea, UIBox is a Moveable). The overhead is proportional to N: two full pairs() traversals vs two ipairs() traversals of the same sequence.
  measure: Add timer_checkpoint('pre_move','update') before line 2832 and use the existing 'move' and 'update' checkpoints. Switch both loops to ipairs() and compare average TTC. On Tensor G2 with LuaJIT, pairs-vs-ipairs on a 300-element sequence is measurable at 0.01–0.05ms total.
  fix: Change both `for k, v in pairs(self.MOVEABLES)` (lines 2832, 2837) to `for k, v in ipairs(self.MOVEABLES)`. Precondition: no new Moveables are created during the move/update iteration (events that spawn Moveables run earlier in the frame at line 2690). Verify with a one-frame stress test.
  risk: Low. ipairs stops at first nil; if any code ever sets G.MOVEABLES[i]=nil directly (instead of table.remove), it would truncate. Current code only uses table.insert/table.remove, so no holes exist. If unsure, add an assertion before switching.
[HIGH] Redundant math.sin calls with identical arguments for edition color oscillation
  where: build/game/game.lua:2668-2674
  claim: Lines 2668-2669 call math.sin(self.TIMERS.REAL*1.3) twice with the exact same argument to compute DARK_EDITION[1] and DARK_EDITION[3]. Lines 2672-2674 call math.sin with the same base argument (self.TIMERS.REAL*1.5 + offset) three times. On ARM (Mali G710, LuaJIT), math.sin is not an inlined intrinsic and takes ~5-15ns per call; two redundant calls here are trivially eliminated.
  cost_when: Every frame, unconditionally. Total redundancy: 2 extra math.sin calls/frame (1 duplicate for DARK_EDITION, and the three EDITION calls share the same base — sin(x), sin(x+3), sin(x+6) — which cannot be fully collapsed without trig identities, but the DARK_EDITION duplicate is a pure win).
  measure: Micro-benchmark: add timer_checkpoint around lines 2668-2674 with perf_mode on. Compare 1000-frame average before/after caching `local _s13 = math.sin(self.TIMERS.REAL*1.3)` and reusing it on both lines 2668 and 2669.
  fix: game.lua:2668-2669: `local _s13 = math.sin(self.TIMERS.REAL*1.3); self.C.DARK_EDITION[1] = 0.6+0.2*_s13; self.C.DARK_EDITION[3] = 0.6+0.2*(1-_s13)`. For EDITION (2672-2674), cache `local _base = self.TIMERS.REAL*1.5` before the three calls — does not eliminate calls but avoids the multiply on each.
  risk: None. Pure value-identity refactor.
[MEDIUM] math.exp called 3 times per frame for G.exp_times — values nearly constant at stable framerate
  where: build/game/game.lua:2824-2826
  claim: Three math.exp calls per frame: exp(-50*dt), exp(-60*dt), exp(-190*dt). At 60fps, dt ~= 0.01667s and changes by at most ±0.002s frame to frame on a stable device. The resulting exp values change by less than 0.1% between frames. These could be cached and only recomputed when dt changes beyond a threshold.
  cost_when: Every frame in-run. math.exp on ARM/LuaJIT is ~10-20ns each; 3 calls = ~30-60ns/frame. Small but genuinely redundant when framerate is stable.
  measure: Add timer_checkpoint('pre_exp','update') before 2824 and check GC-neutral TTC. Log G.exp_times.xy over 100 frames to confirm value stability. Compare against a version that caches: `if math.abs(self.real_dt - self._cached_dt) > 0.001 then recompute end`.
  fix: Cache last dt: `if not self._exp_dt or math.abs(self.real_dt - self._exp_dt) > 5e-4 then self._exp_dt = self.real_dt; G.exp_times.xy = math.exp(-50*self.real_dt); ... end`. Reset _exp_dt on SPEEDFACTOR change or game state transitions (since move_dt = math.min(1/20, real_dt) the effective dt also clamps).
  risk: Low. At worst (thermal throttle causes dt spike > 0.001s), exp_times are stale for 1 frame — produces a tiny visual glitch in easing. Add a generous threshold (0.002s) to eliminate the risk.
============ verify:draw-path ============
overall=mostly-reliable checked=8
  [imprecise] SMODS DrawStep dispatch: 22 check_conditions() calls per Card:draw() regardless of card type (card_draw.lua:512-514).
    FIX: The dispatch loop at lines 512-514 is confirmed. However the DrawStep count is 21, not 22 (grep -c '^SMODS.DrawStep {' returns 21). The structural cost claim is correct: check_conditions is called for every step unconditionally, with only the layers table lookup as an early-exit 
  [wrong] "edition" DrawStep iterates all G.P_CENTER_POOLS.Edition entries for every front-facing card even when self.edition is nil (card_draw.lua:248).
    FIX: The code at lines 246-260 shows: 'local edition = self.delay_edition or self.edition' at line 246, then 'if edition then' at line 247, then 'for k, v in pairs(G.P_CENTER_POOLS.Edition) do' at line 248. The nil guard IS correctly placed outside the pairs loop. For a card with no e
  [imprecise] ambient_tilt cards compute cos/sin per frame; '0.0 is falsy in Lua so those clear correctly' (card_draw.lua:89-93).
    FIX: The trig computation at lines 91-95 is confirmed. However the claim '0.0 is falsy in Lua so those clear correctly' is factually wrong about Lua semantics: in Lua, only nil and false are falsy; 0 and 0.0 are truthy. Setting replace_card.ambient_tilt = 0.0 (game.lua:1658, 1848) doe
============ verify:update-path ============
overall=unreliable checked=5
  [wrong] remove_nils(self.ANIMATIONS) at game.lua:2816 allocates and discards a table every frame
    FIX: This call does not exist. grep -n 'remove_nils' game.lua returns zero results. Line 2816 is timer_checkpoint('states', 'update'). The ANIMATIONS loop at line 2818 is a plain pairs() with no remove_nils call before or after it. AnimatedSprite:remove() and Sprite:remove() both use 
  [wrong] math.sin(self.TIMERS.REAL*1.3) called twice with identical arguments for DARK_EDITION (game.lua:2668-2669); EDITION lines 2672-2674 do not cache the base multip
    FIX: The proposed fix is already implemented in the code. Lines 2668-2670 already read: `local _sin_dark = math.sin(self.TIMERS.REAL*1.3); self.C.DARK_EDITION[1] = 0.6+0.2*_sin_dark; self.C.DARK_EDITION[3] = 0.6+0.2*(1-_sin_dark)`. Lines 2673-2676 already read: `local _ed_base = self.
============ verify:scoring-path ============
overall=unreliable checked=8
  [wrong] local orig_ability = copy_table(active_side.ability) on line 137 of calculate.lua is a dead deep-copy, variable never read
    FIX: This code no longer exists. Line 141 has comment -- CRY_DEAD_COPY_FIXED; line 142 is local callback = context.callback (a different, used variable). The fix was already applied before this audit. The finding describes a ghost.
  [wrong] next(find_joker('cry-Flip Side')) called unconditionally on every calculate_joker invocation at line 112
    FIX: Line 116 uses G._cry_flip_side_active (a pre-cached boolean), not a live find_joker call. The caching fix described is already implemented: overrides.lua:154 sets G._cry_flip_side_active = next(find_joker('cry-Flip Side')) once at the top of G.FUNCS.evaluate_play, clears it at li
  [wrong] Two separate pairs(SMODS.Events) loops run on every SMODS.calculate_context call even when no events are active
    FIX: Both loops (lines 53-61 and 101-109) are already wrapped with if G.GAME.events and next(G.GAME.events) then -- CRY_EVENTS_GUARDED. The fix was applied before this audit.
  [imprecise] get_card_areas('jokers') allocates a fresh 3-element table on every call inside the O(J^2) other_joker cross-product loop
    FIX: The function body (utils.lua:2152-2156) does allocate a fresh table per call and has no caching. However, state_events.lua:821 shows local _joker_areas = SMODS.get_card_areas('jokers') -- SCORING_AREAS_CACHED already hoists the call outside the outer loop, and line 855 reuses _jo
  [wrong] collectgarbage('count') > 1024*1024 guard called every frame while scoring coroutine is live; threshold is ~1 GB and unreachable
    FIX: The code is gone. talisman.lua:697 has the comment -- TAL_GC_DEAD_REMOVED where the guard was; no collectgarbage('count') call exists in the love.update wrapper. The finding describes already-removed code.
  [wrong] Lines 744-748 re-sum G.CARD_CALC_COUNTS via pairs() every frame; line 752 passes elapsed-time float through number_format/to_big creating an OmegaNum table ever
    FIX: Both issues are already fixed. Line 741 reads G.CURRENT_TOTAL_CALCS or 0 directly (no pairs loop; incremental counter maintained in the calculate_joker wrapper at lines 785/788). Line 745 uses string.format('%.1f', G.CURRENT_CALC_TIME or 0)..'s' directly, bypassing number_format/
  [wrong] Each eval_card call in the other_joker cross-product loop constructs a new table literal with identical base fields; only the other_joker field differs
    FIX: Already fixed. Lines 847-858 show the OTHER_KEY_HOISTED CTX_TABLE_HOISTED comment and a single _other_ctx table allocated before the inner loop: local _other_ctx = {full_hand=..., scoring_hand=..., ...}; then _other_ctx[other_key] = _card inside the loop, reusing the table. The f
============ hunt:memory-alloc ============
[HIGH] align_cards: 6 sort closures allocated per CardArea per frame
  where: cardarea.lua:504,520,534,549,563,584,600
  claim: Every `CardArea:align_cards()` call creates a fresh anonymous function for `table.sort`. There are 6–7 distinct sort closures in the function body, each one a new heap object. `align_cards` is called from `CardArea:move(dt)` (line 289) every frame for every CardArea. In a typical run there are ~7 CardAreas (hand, play, jokers, consumeables, deck, discard, vouchers). Even if only 4 are live/moving, that is 24–28 closure allocations per frame from sort alone.
  cost_when: Every frame while SELECTING_HAND or any in-run state (~60 fps × 7 areas = ~420 closure allocs/sec). Magnitude: each closure is ~80–120 bytes in LuaJIT; 420/sec = ~40 KB/sec of short-lived garbage.
  measure: Wrap `CardArea:align_cards` entry/exit with `timer_checkpoint('align_start')` / `timer_checkpoint('align_end')` and log `collectgarbage('count')` before and after. Expect a measurable delta every frame; after fix, delta should drop by ~30 KB/sec.
  fix: Hoist all sort comparators to module-level named functions (e.g., `local function sort_by_x(a,b) ... end`). The pinned-sort comparator appears 5 times identically — one named function replaces all five. Risk: none, pure refactor; the comparator bodies are already identical.
  risk: Zero functional risk; comparator bodies are byte-identical copies.
[HIGH] get_X_same: inner-loop `curr = {}` allocates O(hand × hand) tables per hand eval
  where: functions/misc_functions.lua:661-684 (line 668 inner loop)
  claim: `get_X_same` iterates `#hand` times; each iteration creates a fresh `curr = {}` table (line 668). With a 5-card hand, that is 5 table allocations per `get_X_same` call. `evaluate_poker_hand` calls `get_X_same` 4 times (_5, _4, _3, _2), so one hand evaluation = 20 table allocs just from `curr`. Additionally `vals = {}` with `SMODS.Rank.max_id.value` (≥14) pre-filled inner-empty tables (lines 663–666) on every call.
  cost_when: Once per hand played (scoring path) and once per `parse_highlighted` call (every card added/removed from selection, and on every `remove_from_highlighted`/`add_to_highlighted` -> `parse_highlighted`). With rapid selection changes: several calls per second. Magnitude: ~20–30 table allocs per call × (N calls/sec).
  measure: Add `timer_checkpoint('eval_hand_pre')` just before `evaluate_poker_hand(_cards)` in `get_poker_hand_info` and `timer_checkpoint('eval_hand_post')` after; log GC count delta. Compare before/after fix.
  fix: Reuse a module-level `curr` table: clear it via `for i=#curr,1,-1 do curr[i]=nil end` at the top of the inner loop instead of `local curr = {}`. Similarly, allocate `vals` once module-level and EMPTY it on entry. Risk: `curr` must not escape the inner loop (it does not; it is assigned to `vals[id]`, so the reference is transferred — need to copy instead: `vals[id] = {table.unpack(curr)}` after building, then clear `curr`).
  risk: Low. Care needed to copy `curr` into `vals` before reuse; straightforward pattern.
[HIGH] evaluate_poker_hand: large `results` table with ~14+ pre-allocated sub-tables rebuilt every call
  where: functions/misc_functions.lua:412-428,430-431
  claim: `evaluate_poker_hand` allocates a `results` table containing 12 named string-keyed sub-tables (lines 415–427) plus one per `SMODS.PokerHand.obj_buffer` entry (line 431, Cryptid adds many custom hands). All of these are thrown away after the call. Called from `get_poker_hand_info` which is called from `parse_highlighted` (on every selection change) and once at hand-play time.
  cost_when: Every time the player changes card selection (could be 5–10 times per hand = 50–100 sub-table allocs per hand-play). With Cryptid's extra poker hands, `obj_buffer` may have 20+ entries → 32+ sub-tables per call.
  measure: Log `collectgarbage('count')` before/after `evaluate_poker_hand` call in `get_poker_hand_info` (state_events.lua:685). A drop of >5 KB per call confirms the magnitude.
  fix: Allocate a module-level `_EVAL_RESULTS` table and a corresponding `_EVAL_RESULTS_KEYS` list at startup. On entry to `evaluate_poker_hand`, EMPTY each sub-table in place (`_EVAL_RESULTS['Flush Five'] = EMPTY(_EVAL_RESULTS['Flush Five'] or {})`). For SMODS custom hands, maintain the key list lazily. Risk: result table must not be held across GC boundaries by callers — check that `get_poker_hand_info` only reads from it before returning.
  risk: Medium. Callers must not store the returned `poker_hands` table beyond the call frame; audit `parse_highlighted` and `evaluate_play_intro` usage. Both appear to only read `.top` and check `next()` on sub-tables immediately.
[MEDIUM] pseudoseed: `string.format('%.13f', ...)` allocates a new string on every call
  where: functions/misc_functions.lua:347 (also 339)
  claim: `string.format` always returns a new string. Line 347 runs on every `pseudoseed(key)` call in the normal (non-predict) path. With 18 `pseudorandom_element` calls in card.lua and 13 in common_events.lua, plus direct `pseudorandom(pseudoseed(...))` patterns, this fires many times per hand-play. Also, line 344 concatenates `key..(G.GAME.pseudorandom.seed or '')` on first call per key — but that is a one-time cost per key, amortized.
  cost_when: Scoring path: ~10–30 pseudoseed calls per hand scored × (number of jokers triggering random elements). Magnitude: each `string.format` allocation is ~30–50 bytes; 30 calls/hand = ~1.5 KB per hand.
  measure: Add `timer_checkpoint('score_pre')` / `timer_checkpoint('score_post')` around the full scoring loop in main.lua. Log GC count delta. Then patch pseudoseed to cache the formatted value in a local and confirm the delta drops.
  fix: Replace `math.abs(tonumber(string.format('%.13f', expr)))` with `math.abs(expr % 1)` — the `string.format('%.13f', x)` is only needed to clamp floating-point precision, but `x % 1` achieves the fractional-part extraction directly as a number. Verify numerically that the sequence produced is stable (diff the PRNG outputs for 1000 seeds). Risk: PRNG output sequence will change, invalidating existing seeds in ongoing runs saved to disk. Acceptable for new runs; could gate behind a save-version check.
  risk: Medium — changes PRNG sequence, breaks save determinism for existing runs. Could be applied only to new saves.
[HIGH] parse_highlighted: two fresh table literals per update_hand_text call, fired multiple times per selection
  where: cardarea.lua:201,203,218,220,225,227
  claim: Each `update_hand_text({immediate=true, nopulse=true, delay=0}, {mult=0,...})` allocates TWO table literals: the options table and the values table. `parse_highlighted` is called on every `add_to_highlighted`, `remove_from_highlighted`, and `unhighlight_all`. In the normal (non-NULL, non-backwards) branch (lines 223–227), it fires `N_scoring_params + 1` pairs per call (N = count of `SMODS.Scoring_Parameters`, likely 2+ in vanilla, more in Cryptid). So per selection change: 2×(N+1) table literals.
  cost_when: Every card tap during SELECTING_HAND. With 5-card selection, up to 5 calls × 2×(N+1) tables. Low per-call cost but high frequency during active play. Also called from Cryptid's `Cryptid.table_merge(self.highlighted, tbl)` at line 196 — which itself allocates a fresh merged table on every call.
  measure: Instrument `parse_highlighted` with GC count delta via perf_mode checkpoints. After hoisting the option-table literals to module-level constants, re-measure.
  fix: Hoist the first-arg table literals to module-level constants: `local _UHT_OPT_IMMEDIATE = {immediate=true, nopulse=true, delay=0}` and `local _UHT_OPT_PULSE = {immediate=true, nopulse=nil, delay=0}`. Pass these constants every call. Second-arg value tables are dynamic so cannot be fully pre-allocated, but the `{[name]=value}` tables in the loop (line 225) could use a module-level scratch table that is reused. Risk: nil if options table is mutated by callee — verify `update_hand_text` only reads.
  risk: Low for the option tables; verify update_hand_text does not mutate its first arg.
[HIGH] juice_up: inline `{0.6*amount, -0.6*amount}` table literal allocated every call when rot_amt is nil
  where: engine/moveable.lua:260
  claim: `pseudorandom_element({0.6*amount, -0.6*amount})` creates a new 2-element table literal every time `juice_up` is called without a `rot_amt` argument. `juice_up` is called extensively during scoring (every joker trigger, every card score popup) — card.lua alone has 35+ call sites.
  cost_when: Every joker/card trigger during scoring (~10–40 calls per hand). Magnitude: small per-call (~40 bytes), but multiplied by frequency. Also `pseudorandom_element` on this 2-element table still builds a `keys` table internally (see finding 8 below) so the real cost is that too.
  measure: Log GC count in the scoring loop before/after fix.
  fix: Pre-compute the two candidate values and use `math.random() > 0.5 and (0.6*amount) or (-0.6*amount)` directly — this eliminates both the inline table literal and the `pseudorandom_element` call entirely. Risk: micro-change to the rotation direction distribution (previously seeded, now uses love's RNG directly); visually indistinguishable.
  risk: Very low — purely cosmetic animation direction.
[MEDIUM] pseudorandom_element: allocates `keys` table + sort closure on every call regardless of table size
  where: functions/misc_functions.lua:268-302
  claim: Every `pseudorandom_element(_t, seed)` call: (1) allocates a fresh `keys = {}` table, (2) pushes `{k=k, v=v}` sub-tables for every element in `_t`, (3) allocates a sort closure. For small tables (sigil: 4 suits, ouija: 13 ranks, familiar: 3 face ranks), this is 4–13 sub-table allocs + 1 keys table + 1 closure per call. Called 18 times in card.lua, 13 in common_events.lua.
  cost_when: Scoring path (event-driven): ancient, idol, castle, familiar, grim, incantation, invisible joker, etc. — O(1) per hand but with many jokers can be O(N_jokers) per hand. Magnitude: ~20–50 byte/sub-table × 13 cards = ~650 bytes per ouija call.
  measure: Add GC delta around `pseudorandom_element` calls in the joker-effect hot path (card.lua ~line 1587, 1601, 1672) via perf_mode checkpoints.
  fix: For the common case of small fixed arrays (suit list, rank list), pre-sort them once at module init and replace with `math.randomseed(seed); return _t[math.random(#_t)]` when the table is a plain sequential array with no `in_pool` filtering needed. Add a fast-path: `if type(_t[1]) ~= 'table' and not args then local k = math.random(#_t); return _t[k], k end`. Risk: skips the `in_pool` filter and sort-by-sort_id; only safe for hard-coded array literals where all elements are always valid.
  risk: Medium — must not apply the fast path when in_pool filtering is needed. Needs a caller-side opt-in flag.
[HIGH] Cryptid.table_merge: allocates fresh merged table on every parse_highlighted and evaluate_play_intro call
  where: Mods/Cryptid/lib/misc.lua:1039-1049, called from cardarea.lua:196 and state_events.lua:723
  claim: `Cryptid.table_merge(self.highlighted, tbl)` creates a new table by copying all elements from both inputs. Called from `parse_highlighted` (line 196) every time a card is added/removed from hand selection. Also the `local tbl = {}` at cardarea.lua:190 is rebuilt by scanning all joker cards on every `parse_highlighted` call.
  cost_when: Every card selection change during SELECTING_HAND. With 5 highlighted cards + N joker-cards, produces a table of size 5+N per call. Magnitude: ~5–15 table entries per call, 5–10 calls per hand selection phase.
  measure: GC count delta around `parse_highlighted` before vs after hoisting the joker-card scan.
  fix: Cache the joker-card scan result: rebuild `tbl` only when the joker area changes (i.e., jokers are added/removed), not on every `parse_highlighted` call. Store as `self._cryptid_joker_card_cache` on G.hand; invalidate in `CardArea:emplace` / `CardArea:remove_card` when `self == G.jokers`. Risk: low; the joker card list changes infrequently compared to hand selection.
  risk: Low. Cache invalidation is straightforward since joker add/remove already calls align_cards.
[MEDIUM] SMODS.get_card_areas: allocates a fresh table on every call inside the scoring loop
  where: SMODS/_/src/utils.lua:2142-2157, called from common_events.lua:770-772
  claim: `SMODS.get_card_areas('jokers')` creates `local t = {G.jokers, G.consumeables, G.vouchers}` every invocation. In `eval_card` (common_events.lua:770), this is called once per joker card evaluated. Also called inside `SMODS.calculate_context` (utils.lua:1909, 1912, 1914). With 10 jokers, `eval_card` is called ~10 times per scored card × 5 scored cards = 50 `get_card_areas` calls per hand, each allocating a 3-element table.
  cost_when: Every hand scored. Magnitude: 50 × ~3-element tables = 150 table-slot allocs. Small individually but in the scoring hot-loop these persist until next GC sweep.
  measure: Log GC delta over a full scoring sequence in perf_mode. Patch to return a cached table and verify delta drops by the corresponding amount.
  fix: Hoist the result tables to module-level constants: `SMODS._CARD_AREAS_JOKERS = {G.jokers, G.consumeables, G.vouchers}` (populated at game start after areas are created). Return the cached table from `get_card_areas`. Update the cache only when areas are added/removed (rare). The `eval_card` copy at line 770–772 that builds `area_set` from `areas` can also be hoisted: cache `area_set` as a module-level table reset only when joker areas change. Risk: if mods dynamically add card areas mid-run, the cache is stale — add a dirty flag.
  risk: Medium — mod compatibility; the SMODS target comment implies extensibility. Dirty-flag pattern mitigates.
[MEDIUM] card_eval_status_text: `localize{type='variable',...,vars={math.abs(amt)}}` allocates 2 tables per scoring popup
  where: functions/common_events.lua:966,971,979,987,994
  claim: Each `localize{type='variable', key='...', vars={math.abs(amt)}}` call passes a table literal as the first arg (1 alloc) and `vars={...}` is a nested table literal (1 more alloc). `card_eval_status_text` is called once per joker/card trigger during scoring. With 10 jokers × 2 allocs = 20 table allocs in the status-text path per hand, plus `local config = {}` at line 951 on every call.
  cost_when: Every scoring trigger display — fired via E_MANAGER events during hand scoring. Not per-frame but concentrated in the scoring animation burst (0.5–2 seconds). Magnitude: 20–60 small table allocs per hand.
  measure: Wrap `card_eval_status_text` with GC count before/after. The `config` table alloc at line 951 is the cleanest fix to verify.
  fix: Hoist `local config = {}` to a module-level scratch `_STATUS_CONFIG = {}` and EMPTY it on entry. For `vars={math.abs(amt)}`, use a module-level `_VARS_SCRATCH = {false}` and set `_VARS_SCRATCH[1] = math.abs(amt)` before passing. Risk: `localize` must not retain a reference to `vars` beyond the call; verify it only reads.
  risk: Low — localize is a pure string-lookup function; it does not store the vars table.
============ verify:memory-alloc ============
overall=mostly-reliable checked=10
  [imprecise] align_cards: 6 sort closures allocated per CardArea per frame — 5 pinned-sort comparators are byte-identical copies, lines 504,520,534,549,563,584,600
    FIX: The line list is correct and there are 7 sort closures total (not 6), matching the 7 lines cited. Five have the identical pinned-sort body (504, 520, 534, 563, 600). Line 584 has the same pinned-sort body but is guarded by a Cryptid conditional (`if not G.GAME.modifiers.cry_conve
  [imprecise] evaluate_poker_hand: large `results` table with ~14+ pre-allocated sub-tables rebuilt every call; 12 named string-keyed sub-tables plus SMODS.PokerHand.obj_buff
    FIX: Confirmed at lines 415-427: the results literal has 12 named keys (Flush Five through High Card) plus `top = nil`. `top` is NOT a sub-table (it is `nil`), so there are 12 sub-tables in the literal, not '12 named string-keyed sub-tables plus top'. The obj_buffer loop at line 431 t
  [imprecise] pseudoseed: `string.format('%.13f', ...)` allocates a new string on every call; fix is replace with `math.abs(expr % 1)`
    FIX: Line 347 is confirmed. However the proposed fix is semantically incorrect: `string.format('%.13f', x)` rounds x to 13 decimal places before extracting the fractional part, while `x % 1` extracts the exact floating-point fractional part with no rounding. For the PRNG intermediate 
  [imprecise] parse_highlighted: two fresh table literals per update_hand_text call; option-table form `{immediate=true, nopulse=true, delay=0}` vs `{immediate=true, nopulse=
    FIX: Confirmed that table literals are created per call at lines 201, 203, 218, 220, 225, 227. However there are THREE distinct first-arg forms in this function: `{immediate=true, nopulse=true, delay=0}` (NULL branch, lines 201/203), `{immediate=true, nopulse=nil, delay=0}` (backwards
  [imprecise] pseudorandom_element: allocates `keys` table + sort closure on every call; for small fixed arrays this is 4-13 sub-table allocs + 1 keys table + 1 closure per c
    FIX: Confirmed that `local keys = {}` is allocated and `{k=k, v=v}` sub-tables are pushed per element. However the code at lines 294-298 contains an if/else: ONE sort closure is allocated per call (either the sort_id branch or the key branch), not both. The claim says 'allocates a sor
  [imprecise] SMODS.get_card_areas: allocates a fresh `{G.jokers, G.consumeables, G.vouchers}` table on every 'jokers' call; called from common_events.lua:770 once per joker 
    FIX: Confirmed at utils.lua lines 2152-2157: `local t = {G.jokers, G.consumeables, G.vouchers}` is created fresh per call. However the finding omits a critical detail: there is an immediate conditional override `if G.GAME.modifiers.cry_beta then t = {G.jokers, G.vouchers} end` at line
============ hunt:platform ============
[HIGH] FPS_CAP=500 sleep-pacing with vsync=1 — double-bounded, battery waste on menus
  where: main.lua:954-955, button_callbacks.lua:1186-1197
  claim: G.FPS_CAP defaults to 500 (main.lua:954). The sleep loop at main.lua:955 fires only when a frame completes faster than 1/500 s (~2 ms), so it provides essentially no cap in practice. The true frame rate bound is the vsync setting passed to love.window.updateMode: G.SETTINGS.WINDOW.vsync defaults to 1 (globals.lua:182), so present() blocks at the display refresh rate (~120 Hz on Pixel Fold). In a run at 63-76 FPS the vsync present is the binding constraint, not the sleep — correct. BUT on low-load screens (main menu, collection, pause) where GPU finishes well under 8 ms, the vsync present still caps at 120 fps unless the driver uses adaptive sync, meaning menu screens can render at 120 Hz drawing nothing meaningful — twice the GPU/display work needed. There is no lower FPS cap applied for non-run screens.
  cost_when: Continuous on main menu / collection / pause overlays. At 120 Hz vs 60 Hz: roughly 2x GPU cycles, 2x present interrupts, non-trivial battery on a game that spends significant time in menus.
  measure: adb shell cat /sys/class/thermal/thermal_zone*/temp while sitting in main menu with vsync=1 vs after patching vsync=2 (adaptive/half). Or: enable perf_mode (G.SETTINGS.perf_mode=true), note timer_checkpoint('canvas','draw') average on menu screen at 120Hz vs 60Hz. Also: adb shell dumpsys batterystats --reset; idle 5 min on menu; adb shell dumpsys batterystats | grep -i discharge.
  fix: In set_globals or set_render_settings, for Android, set G.SETTINGS.WINDOW.vsync = 2 (adaptive vsync, which maps to EGL_SWAP_INTERVAL=1 but allows tearing instead of blocking when late — not ideal) OR more correctly cap the sleep-based pacer for non-run stages. The cleanest fix: in love.run, change the sleep guard to `if run_time < 1./G.FPS_CAP then` where G.FPS_CAP is set to 60 (not 500) for Android, and keep vsync=1 — then vsync=1 at 60 Hz LSKM prevents tearing and the sleep provides the 60 Hz floor on menu. Add a G.SETTINGS.WINDOW.vsync Android override of 1 and set G.FPS_CAP = 60 (or 120 matching display) via config-overrides. This removes the 500-cap no-op.
  risk: Changing vsync=0 would produce tearing on touch interactions. Using vsync=1 + FPS_CAP=60 is safe but means any frame that takes >16 ms will not be sleep-rescued — already the case. FPS_CAP=120 matching display Hz with vsync=1 is the neutral change.
[HIGH] 10 CRT shader uniform sends every draw frame even with crt=0, bloom=0
  where: game.lua:3151-3168
  claim: The block starting at game.lua:3151 executes every frame when `(not G.recording_mode or G.video_control) and true` — which is always true in normal play. It calls SHADERS['CRT']:send() 10 times (distortion_fac, scale_fac, feather_fac, bloom_fac, time, noise_fac, crt_intensity, glitch_intensity, scanlines, mouse_screen_pos, screen_scale, hovering — actually 12 sends). With G.SETTINGS.GRAPHICS.crt=0 and bloom=0 these uniforms are all zero-valued, but the `:send()` calls are not skipped. Each `:send()` is a Lua→C FFI call that crosses into the LÖVE/OpenGL uniform-upload path; on Mali-G710 each uniform upload is a driver call.
  cost_when: Every draw frame, ~60-76 times/second in-run. 12 driver uniform calls per frame that produce no visual effect. Rough magnitude: each uniform send at ~1-3 µs driver overhead = 12-36 µs/frame of pure overhead, plus the Lua math ops (multiplications by 0).
  measure: In G.SETTINGS.perf_mode, the timer_checkpoint('canvas','draw') segment covers this block (line 3184 comes after). Add a checkpoint immediately before line 3154 (`timer_checkpoint('pre_crt','draw')`) and one after line 3169. Compare average with crt=0 vs with the whole block guarded by `if G.SETTINGS.GRAPHICS.crt > 0 or G.SETTINGS.GRAPHICS.bloom > 1 then`. Before/after delta in the 'canvas' segment.
  fix: Wrap lines 3154-3168 in `if G.SETTINGS.GRAPHICS.crt > 0 or G.SETTINGS.GRAPHICS.bloom > 1 then ... end`. The `love.graphics.setShader(G.SHADERS['CRT'])` call at line 3167 is also inside the block, so when the block is skipped, no shader is bound, and the subsequent `love.graphics.draw(self.CANVAS, 0, 0)` at line 3171 uses the default shader — which is the correct no-effect path. Then `love.graphics.setShader()` at line 3175 is a no-op clear already present.
  risk: Low. The guard condition mirrors the intent of crt=0/bloom=0. The time uniform (line 3159) is used for shader animations but irrelevant when the shader is not bound. The screenwipe_amt calculation at line 3153 would need to remain outside the guard since it drives other visual state — move it before the if-block.
[HIGH] HTTP_MANAGER in_channel:pop() called every frame despite F_HTTP_SCORES=false on Android
  where: game.lua:2646
  claim: Every frame in Game:update, `G.HTTP_MANAGER.in_channel:pop()` is called (game.lua:2646). G.F_HTTP_SCORES is false on Android (the HTTP thread is never started, game.lua:124-126). The channel:pop() call is not guarded by `if G.F_HTTP_SCORES`. This means every frame on Android: a Lua→C channel lock-acquire-pop-release cycle runs against a channel that will never have data.
  cost_when: Every frame, ~60-76 Hz in-run. The channel pop is a mutex-guarded operation in LÖVE's thread channel implementation. Cost is small (~0.5-2 µs/frame) but it is 100% waste on Android.
  measure: Add timer_checkpoint before and after line 2646. With perf_mode enabled, the 'alerts'→'e_manager' segment contains this call; a targeted micro-benchmark wrapping just the pop call (even 10k iterations in a test loop via a patch) would confirm magnitude.
  fix: Wrap line 2646 with `if G.F_HTTP_SCORES then`. One-line patch in game.lua.
  risk: Zero. F_HTTP_SCORES=false means the response channel is never written to; the guard is a strict no-op behavioral change.
[MEDIUM] LONG DT print() fires unconditionally on every frame where dt > 50 ms — logcat spam that blocks main thread
  where: game.lua:2639
  claim: `if self.real_dt > 0.05 then print('LONG DT @ '..math.floor(G.TIMERS.REAL)..': '..self.real_dt) end` — when a frame stutter occurs (GC, thermal throttle, app resume), this fires print() which routes to Android logcat via SDL's LOG_INFO. On LÖVE/Android, print() is synchronous: it calls SDL_LogInfo which formats and writes to logcat. String concatenation also allocates. During recovery from a stutter (e.g., a 100ms GC pause), this fires once and is fine. But if the device is thermally throttling and multiple consecutive frames exceed 50ms, this fires every slow frame, adding logcat write pressure at exactly the moment the CPU is already burdened.
  cost_when: Only when frames are already slow (>50ms), but that is precisely the highest-pressure moment. One print = ~10-50 µs of string alloc + syscall. Not catastrophic but adds insult to injury during stutter recovery.
  measure: adb logcat -s SDL/APP | grep 'LONG DT' while playing — count frequency during thermal ramp. Compare with/without by temporarily raising threshold to 0.1.
  fix: Rate-limit: track `G._last_long_dt_warn = 0` and only print if `G.TIMERS.UPTIME - G._last_long_dt_warn > 5` (max once per 5 seconds). Or gate entirely on `if not _RELEASE_MODE` since this is a debug diagnostic.
  risk: Low. Diagnostic only; no behavior change. If it fires repeatedly it is already generating noise.
[MEDIUM] Telemetry Game:update hook allocates locals and checks UPTIME comparison every frame
  where: android-telemetry.lua:44-79
  claim: The android-telemetry.lua wrapper around Game:update (line 44) reads `self._exp_dt` before and after the real update to detect recomputes (line 45-49), then checks `now - TEL.exp_recompute_window_start >= EXP_REPORT_INTERVAL` (line 55) and checks for state change (line 69) — every single frame. While individually cheap, the wrapper adds 2 local reads, 1 subtraction, 2 comparisons, and 1 function-call overhead (the wrapper closure itself) per frame. The state-change check (line 69) also does `G.STATE ~= TEL.last_state or G.STAGE ~= TEL.last_stage` — two table lookups — every frame.
  cost_when: Every frame on Android, all states. ~3-6 µs/frame aggregate. Over 60 fps = 0.18-0.36 ms/frame added to the update budget. Not large on its own but the wrapper calls `_original_game_update` which adds an extra Lua call frame vs the direct method dispatch.
  measure: Add timer_checkpoint at start of love.update (main.lua:1037 already has `timer_checkpoint(nil,'update',true)`) then a second one after the G:update(dt) call; this spans the whole telemetry wrapper. Compare with telemetry disabled (set `if false then return end` at top of android-telemetry.lua) — look at the 'update' segment average difference in perf_mode.
  fix: Merge the EXP_REPORT_INTERVAL check into a coarser timer — only check `os.time()` (1-second granularity) rather than TIMERS.UPTIME (every frame). For the state-change check, set a flag in the existing STATE transition code rather than polling every frame. These two changes eliminate the per-frame arithmetic while preserving the telemetry semantics.
  risk: Low. The telemetry is diagnostic instrumentation; slightly coarser state-change detection is acceptable.
[MEDIUM] texture_scaling=1 uses dpiscale=1 in newImage — textures load at half the native atlas resolution, and mipmaps are generated for 1x assets that don't need them
  where: game.lua:1128, 1138
  claim: When texture_scaling=1 (current mobile default), all atlas newImage calls use `{mipmaps=true, dpiscale=1}` (game.lua:1128, 1138). dpiscale=1 tells LÖVE the image is for a 1x screen, so when rendered on the 2272x1840 Pixel Fold inner display, LÖVE scales it up. The 1x textures are 71x95 px per card sprite vs 142x190 px in the 2x set. Using dpiscale=1 with nearest-neighbour filter (line 1040: texture_scaling==1 → 'nearest') means cards render at 1x resolution upsampled to full display — correct for pixel-art style. However, `mipmaps=true` is passed for 1x textures where no mipmap chain makes visual sense (they are already at minimum meaningful resolution), wasting GPU memory for mip level storage and adding texture upload time. Additionally, at texture_scaling=1, the setMipmapFilter call at line 1141 (inside a SMODS config check) may still set mipmap filtering on 1x assets creating GPU sampler overhead.
  cost_when: Boot time (texture upload): one-time, but contributes to the measured boot budget. VRAM: 1x mipmaps add 33% overhead to texture memory on top of the base 1x atlas — though 1x textures are small (~2-4 MB total), so the absolute number is low. Not a hot-path cost.
  measure: Boot timer: compare boot_timer segments for 'shared sprites' to 'prep stage' with and without mipmaps=false for 1x path. For VRAM: adb shell dumpsys meminfo <package> | grep -i 'graphics\|gl\|texture' before and after.
  fix: In set_render_settings (game.lua:1125-1154), when texture_scaling==1, pass `{mipmaps=false, dpiscale=1}` to newImage. The 'nearest' filter already set at line 1040 makes mipmaps irrelevant — they will never be sampled.
  risk: Low. Mipmaps only matter when minification occurs (texture drawn smaller than native). 1x textures at nearest filter are always magnified on a 1440p display, never minified.
[MEDIUM] 309-file mod push copies all Cryptid assets to save-dir even though the loader reads from embedded archive
  where: scripts/build.sh:1591-1625
  claim: prepare_transfer() (build.sh:1583) copies the full Steamodded, Cryptid, Talisman, sticky-fingers, and reserve-shim mod directories to build/phone-transfer/Mods/, then deploy() pushes this via `adb push transfer_dir temp_dir` and copies to files/save/. The Cryptid mod alone is 24 MB and contains 54 Lua files plus all assets. The LÖVE Android filesystem mounts the APK archive for love.filesystem reads, and the Steamodded mod loader (confirmed by the comment at build.sh:1624: 'Dump files are embedded in APK, only Mods folder needs to be pushed') reads Lua source from the save directory path set by lovely.mod_dir='Mods'. So the Lua source must be in save-dir. However, Cryptid's assets/shaders and assets/textures sub-directories (part of the 24 MB) are referenced as love.filesystem paths that would also resolve from the APK if embedded — only the Lua sources strictly require the save-dir copy. The current push sends everything including large texture PNGs and shader files that are duplicated in the APK, adding ~20 MB to each deploy push.
  cost_when: Deploy time only (not runtime). Each full deploy pushes ~34 MB to the device over USB, taking 15-30 seconds. Texture/shader files in Cryptid assets that are already in the APK are redundant. No runtime cost since files/save/ shadows the archive.
  measure: Time `adb push` for a full transfer vs a transfer with only Lua files (find Mods/ -name '*.lua' -o -name '*.jkr'). Compare: `time adb push build/phone-transfer /data/local/tmp/test`.
  fix: In prepare_transfer, after copying each mod directory, strip non-Lua assets that are already baked into the APK: `find transfer_dir/Mods/ -not -name '*.lua' -not -name '*.json' -not -type d -delete`. This requires verifying which asset paths Cryptid loads via love.filesystem (from APK) vs nativefs (from save-dir). The blur.fs shader patch (build.sh:1602) must be kept since it is a patched file. An inventory pass on Cryptid's asset load calls would confirm which files can be dropped from the push.
  risk: Medium. If any Cryptid code loads assets via nativefs (which reads from save-dir, not APK), stripping them from the push would break those assets. Requires an audit of Cryptid's asset loading paths before applying.
============ verify:platform ============
overall=unreliable checked=7
  [wrong] FPS_CAP=500 sleep-pacing provides essentially no cap in practice; there is no lower FPS cap applied for non-run screens (globals.lua:182 for vsync=1)
    FIX: Two errors. First, the fix has already been applied: globals.lua:67 sets `self.FPS_CAP = 60` inside the Android OS block, with a comment explaining the exact rationale described in this finding. The claim 'there is no lower FPS cap applied for non-run screens' is false in the cur
  [wrong] 10+ CRT shader uniform sends every draw frame even with crt=0, bloom=0 (game.lua:3151-3168, block executes always)
    FIX: The fix has already been applied. game.lua:3163 now reads `if G.SETTINGS.GRAPHICS.crt > 0 or G.SETTINGS.GRAPHICS.bloom > 1 then` guarding all 12 uniform sends and the setShader call. The screenwipe_amt calculation at line 3158 is correctly left unconditional as the finding's own 
  [wrong] HTTP_MANAGER in_channel:pop() called every frame despite F_HTTP_SCORES=false on Android (game.lua:2646, unguarded)
    FIX: The fix has already been applied. game.lua:2649 now reads `if G.F_HTTP_SCORES then` with a comment explaining the Android rationale, guarding the pop. The claim that the call is 'not guarded by if G.F_HTTP_SCORES' is false in the current file. The underlying characterization of F
  [imprecise] texture_scaling=1 uses dpiscale=1 in newImage (game.lua:1128, 1138) — textures load at half native atlas resolution; mipmaps=true is passed for 1x assets that d
    FIX: The dpiscale claim is correct in effect but the code does not hardcode 1: it passes `dpiscale = self.SETTINGS.GRAPHICS.texture_scaling`. The value evaluates to 1 at runtime because globals.lua:209 sets `texture_scaling = 1` as the Android default and set_render_settings (game.lua
  [imprecise] 309-file mod push copies all Cryptid assets to save-dir including large texture PNGs; prepare_transfer() at build.sh:1591-1625 sends ~20 MB of redundant assets
    FIX: The behavior described is accurate — prepare_transfer (build.sh:1586-1628, not :1591-1625 as cited) does `cp -r` each mod directory with no asset stripping, and the blur.fs shader patch is correctly called out as a required exception. However, the claim that textures are 'duplica
============ hunt:ui-path ============
[HIGH] Double update_text() in hand_chip/mult UI set functions
  where: build/game/functions/button_callbacks.lua:1940,1956
  claim: hand_mult_UI_set and hand_chip_UI_set each call e.config.object:update_text() explicitly, then immediately call G.FUNCS.text_super_juice(e, num) which calls update_text() a second time. The first call is wasted — the DynaText layout produced before text_super_juice runs is immediately discarded.
  cost_when: Every frame the scoring animation updates chips or mult (~60-76 Hz while scoring). At 2 calls per function × 2 functions = 4 redundant update_text() calls per scoring frame reduced to 2. Each update_text() call on a changed string triggers love.graphics.newText per character for DynaText.
  measure: Add perf_mode timer_checkpoint before/after hand_chips_container update in talisman.lua's Game:update; compare frame time during active scoring before vs after patch. Alternatively: logcat grep for GC pauses during scoring animation frames.
  fix: Removed the explicit update_text() calls at lines 1940 and 1956. text_super_juice at line 1988 already calls update_text() — it is the authoritative call site. Patch: apply_hand_update_text_dedup in build.sh. APPLIED to build/game/functions/button_callbacks.lua.
  risk: Low. text_super_juice calls update_text() unconditionally (line 1988) so the DynaText is always updated correctly. The removed calls were strictly redundant.
[HIGH] Missing no_recalc on hand_level T-element triggers full HUD recalculate on level-up display
  where: build/game/Mods/Steamodded/src/ui.lua:2132
  claim: The hand_level T-element polls ref_value='hand_level' every frame via UIElement:update_text(). When the string length changes (e.g. ' Lv 1' → ' Lv 10'), UIBox:recalculate() fires — walking the entire HUD tree: calculate_xywh on every child, set_wh, set_alignments, initialize_VT (moves every UIElement). No no_recalc guard is present.
  cost_when: On every hand level-up display transition where string length changes. The HUD recalculate walks O(N) nodes where N is the full HUD child count. With Steamodded+Talisman+Cryptid, N is large. Cost is one full layout pass per level-change event.
  measure: Enable G.SETTINGS.perf_mode; add timer_checkpoint around G.HUD:recalculate() calls. Compare frame spike magnitude on level-up before vs after. Or: grep logcat for dropped frames around hand-level UI updates.
  fix: Add no_recalc=true to the T-element config. UIElement:update_text() checks this flag before calling UIBox:recalculate(). Safe because the hand_level column has a fixed-width parent container — the layout does not need to remeasure. Patch: apply_hand_level_no_recalc in build.sh. APPLIED to build/game/Mods/Steamodded/src/ui.lua:2132.
  risk: Low. The no_recalc flag only suppresses the layout walk; the text drawable is still updated in-place via set(). If a future change makes the hand_level column variable-width, this flag would need removal.
[HIGH] localize('k_lvl') called twice per hand-level update in update_hand_text
  where: build/game/functions/common_events.lua:559,563
  claim: The level update path calls localize('k_lvl') twice — once in the guard comparison (' '..localize('k_lvl')..tostring(vals.level)) and once in the assignment. Each call produces a new intermediate string via concatenation.
  cost_when: On every update_hand_text() invocation where vals.level is set (every scored hand). Low absolute cost per call but produces two short-lived strings the GC must collect. On a high-joker run with Talisman's disable_anims=true, this fires every scoring step.
  measure: Minor — confirm with logcat GC frequency baseline vs after patch. Primary value is GC pressure reduction, not CPU time.
  fix: Cache `local _lvl_pfx = ' '..localize('k_lvl')` before the guard/assignment block. Uses the local in both places. Patch: apply_lvl_prefix_cache in build.sh. APPLIED to build/game/functions/common_events.lua:559-564.
  risk: Negligible. The local is scoped to the enclosing event function block, computed fresh each invocation. Behavior is identical.
[MEDIUM] Card hover popup rebuilt from scratch on every hover start
  where: build/game/card.lua:4872-4874
  claim: Card:hover() unconditionally calls generate_UIBox_ability_table() (many localize() calls for joker descriptions) and G.UIDEF.card_h_popup() (full UIBox tree construction with SMODS.create_mod_badges) on every hover start. No caching between successive hovers of the same card.
  cost_when: Once per touch-hold gesture start (NOT per-frame). On mobile, hover() fires only on prev_target change (controller.lua:451), deferred by G.MIN_HOVER_TIME. Cost is one-time frame jank when a card description appears. High cost per event but low frequency — not a sustained FPS drain in normal gameplay.
  measure: adb shell perfetto trace or perf_mode checkpoint around the hover event dispatch. Compare frame time at the moment description appears for jokers with 20+ active effects vs base jokers.
  fix: Cache ability_UIBox_table on the card instance using a popup_dirty flag. Set dirty in Card:set_ability(), Card:set_edition(), Card:set_seal(). Only regenerate when dirty=true. NOT APPLIED — risk/reward ratio unfavorable until measurement confirms sustained impact on mobile.
  risk: Medium. Dirty flag requires tracking all card mutation sites. A missed site would show stale tooltip content.
[MEDIUM] love.graphics.newText allocated per character on every DynaText string change
  where: build/game/engine/text.lua:118-122
  claim: DynaText:update_text() rebuilds the letter array on every string content change, calling love.graphics.newText(font, char) for each character. Each call allocates a GPU-backed Text object. Old letter objects become garbage. During scoring animations, chip/mult DynaText updates every frame, triggering GC pressure proportional to the number of display digits.
  cost_when: Every frame chips or mult display string changes during scoring animation. A chip value like '12345' = 5 newText allocations per update. At 60-76 Hz scoring, this is 300-380 GPU Text objects allocated+collected per second per DynaText that updates.
  measure: logcat | grep -E 'GC|Explicit GC|art.*GC' during scoring vs idle. Also: perf_mode timer_checkpoint wrapping DynaText:update() in game.lua's MOVEABLES loop to see its fraction of frame time.
  fix: Per-font character cache: table keyed by (font_object, char) → love.graphics.newText object. Populate on first use, reuse thereafter. Requires ~100 entries per font (printable ASCII). NOT APPLIED — needs measurement to justify; cache structure adds memory; risk of stale objects if font changes.
  risk: Medium. Cache invalidation if font scale changes. Memory overhead ~100 Text objects per font.
