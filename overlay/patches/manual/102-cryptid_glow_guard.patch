diff --git a/Mods/Cryptid/lib/overrides.lua b/Mods/Cryptid/lib/overrides.lua
index 0e1e56e..934b4bc 100644
--- a/Mods/Cryptid/lib/overrides.lua
+++ b/Mods/Cryptid/lib/overrides.lua
@@ -413,11 +413,13 @@ function Game:update(dt)
 			glowingobj.pos.y = glowingobj.pos.y + 1
 		end
 	end
+	if G.GAME and G.GAME.selected_back and G.GAME.selected_back.effect and G.GAME.selected_back.effect.center and G.GAME.selected_back.effect.center.key == "b_cry_glowing" then -- PERF: only the glowing deck has cry_glowing backs
 	for k, v in pairs(G.I.CARD) do
 		if v.children.back and v.children.back.atlas.name == "cry_glowing" then
 			v.children.back:set_sprite_pos(G.P_CENTERS.b_cry_glowing.pos or G.P_CENTERS["b_red"].pos)
 		end
 	end
+	end
 	if not G.OVERLAY_MENU and G.GAME.CODE_DESTROY_CARD and not G.OVERLAY_MENU_POINTER then
 		G.FUNCS.exit_overlay_menu_code()
 	end
