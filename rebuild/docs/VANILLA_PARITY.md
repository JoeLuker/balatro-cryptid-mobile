# VANILLA PARITY LEDGER — vanilla Lua vs Kotlin rebuild

Generated 2026-07-02 by a 12-domain read-only audit fleet (58 agents; every 'wrong'/'blocker'
claim adversarially re-verified against both sources — 42/45 held, 3 corrected in place).
Vanilla reference: `src/Balatro.love` (extracted). This supersedes UI_AUDIT.md as the port burn-down list.

**154 findings** — status: ✅ faithful 28 · ❌ missing 68 · ◑ partial 13 · ⚠️ wrong 45 — severity: **blocker 9** · **major 65** · **minor 36** · **cosmetic 44**

Legend: ✅ faithful · ◑ partial · ❌ missing · ⚠️ present-but-diverges. 'verified' = survived adversarial re-read.

## Ranked worklist (blockers → major)

1. **[BLOCKER]** ❌ `Save/load & profiles` — Mid-round autosave (SELECTING_HAND state) — run is durable during actual card play, not just at shop/blind-select
   - vanilla: `functions/misc_functions.lua:1452-1485 (save_run culls cardAreas{hand,deck,play,discard,jokers,consumeables}+GAME+STATE+ACTION+BLIND+BACK); game.lua:3` · kotlin: `ui/RunScreen.kt:2514-2528 (LaunchedEffect(s.phase, s.blindIndex, s.money, s.consumables.size) writes ONLY when s.phase i`
   - fix: Add a periodic (~30s) or every-hand-boundary autosave that also fires in Phase.ROUND/ROUND_EVAL/PACK_OPEN, and extend RunSnapshot with the in-round transient fields (current hand cards, hands/discards left, round chip to
2. **[BLOCKER]** ❌ `Save/load & profiles` — Unlock/discovery/alert persistence (meta.jkr) — cross-run progression: which jokers/backs/vouchers are unlocked, which items have been discovered (seen in a run
   - vanilla: `engine/save_manager.lua:18-49 (save_progress request merges UDA 'u'/'d'/'a' flags into meta.jkr's unlocked/discovered/alerted tables, appended not ove` · kotlin: `none (grep -rln "unlocked|discovered" over rebuild/app/src/main/kotlin/systems/balatro → zero hits outside this audit)`
   - fix: Add a Meta persistence object (SharedPreferences or a JSON file) storing unlocked/discovered/alerted sets per item key, populate it from run-end/mid-run discovery events the way vanills's UDA flags do, and gate joker/bac
3. **[BLOCKER]** ❌ `The 150 vanilla Balatro jokers — config fidelity and scoring-trigger correctness in the Kotlin rebuild's Score.kt/JokerManifest.kt engine (cross-checked against RunScreen.kt where a joker's mechanic is legitimately non-scoring, e.g. economy/shop hooks)` — Roster completeness — every vanilla joker key must exist somewhere in the engine (scoring, shop offer, or economy hook)
   - vanilla: `game.lua P_CENTERS (150 j_* entries, lines 368-526)` · kotlin: `none (grepped the entire rebuild/app/src/main/kotlin tree)`
   - fix: Triage the 36 keys: port each calculate_joker branch (card.lua, searched by self.ability.name) into JokerManifest.kt or the appropriate RunScreen hook, and add the missing shop Offer() entries.
4. **[BLOCKER]** ⚠️ `The 150 vanilla Balatro jokers — config fidelity and scoring-trigger correctness in the Kotlin rebuild's Score.kt/JokerManifest.kt engine (cross-checked against RunScreen.kt where a joker's mechanic is legitimately non-scoring, e.g. economy/shop hooks)` — context.before accumulator timing — Green Joker (+1 Mult/hand), Spare Trousers (+2 Mult/Two-Pair-or-Full-House), Runner (+15 Chips/Straight), Square Joker (+4 C *(verified)*
   - vanilla: `functions/state_events.lua:574 (hands[text].played++ happens before the context.before loop at line 630) and card.lua:3411-3569 (Spare Trousers 3412, ` · kotlin: `JokerManifest.kt:194-234,860-870 (green_joker/j_spare_trousers/j_runner/j_square/j_obelisk reducers pattern-match only G`
   - fix: Rewrite these 5 reducers to react to GameEvent.BeforeHand (reading e.ctx.pokerHands / e.ctx.fullHand.size / e.ctx.scoringName as vanilla's context.before does), matching the j_cry_bonk/j_cry_whip pattern, so Score.kt's l
5. **[BLOCKER]** ⚠️ `The 150 vanilla Balatro jokers — config fidelity and scoring-trigger correctness in the Kotlin rebuild's Score.kt/JokerManifest.kt engine (cross-checked against RunScreen.kt where a joker's mechanic is legitimately non-scoring, e.g. economy/shop hooks)` — Constellation — X(1 + 0.1×planets_used) Mult, grown each time a Planet consumable is used *(verified)*
   - vanilla: `card.lua:2727-2732 (context.consumeable, ability.set=='Planet' → self.ability.x_mult += self.ability.extra) read by joker_main at card.lua:806 loc_var` · kotlin: `RunScreen.kt:931-934 (the x accumulator IS grown: `for (o in owned) if (o.fj.key == "j_constellation") o.fj.x += 0.1`) —`
   - fix: Add `"j_constellation" to JokerSpec(jokerMain = { s, _ -> if (s.x > 1.0) Effect.XMult(s.x) else Effect.None })` to JOKER_MANIFEST, mirroring j_hologram/j_throwback's already-correct RunScreen-event-then-jokerMain-read pa
6. **[BLOCKER]** ⚠️ `UI panels (create_UIBox_* — HUD, run flow, overlays, meta/collection)` — HUD sidebar button_area — 'run_info' and 'options' buttons (pause/settings + poker-hand reference) *(verified)*
   - vanilla: `functions/UI_definitions.lua:1385 (run_info_button, button='run_info') and :1573/6218-area (button='options') inside create_UIBox_HUD's button_area` · kotlin: `rebuild/.../ui/HudSpec.kt:139-145 (buttonOnClick only handles toggle_shop/reroll_shop/buy_from_shop/redeem_from_shop/ope`
   - fix: In HudBind.buttonOnClick, map "run_info" -> { s.openRunInfo() } and "options" -> the in-run options callback (thread an onOptions lambda into HudBind like GameOverBind already threads onRestart/onMainMenu); delete the no
7. **[BLOCKER]** ❌ `board physics & card containers (CardArea/Moveable/Card — hand, jokers, consumeables, play, deck, discard)` — Discard pile has no visual container at all — vanilla flies discarded cards into G.discard, which scatters them via a dedicated align branch (card.discard_pos.x
   - vanilla: `cardarea.lua:425-435 (align_cards discard branch)` · kotlin: `engine/EngineHost.kt:29 (`val discard = areaMoveable(Room.discard)` — a plain Moveable, not a CardArea); no reference to`
   - fix: Promote host.discard to a real CardArea (kind="discard"), feed it from `s.discardedThisRound` (or similar), port the discard align branch (per-card discard_pos scatter), and render it like the hand/play areas.
8. **[BLOCKER]** ⚠️ `scoring pipeline (evaluate_play sequence: card order, retriggers, held-in-hand, joker context order, edition/seal timing)` — context.before joker pass mutates ability state (Spare Trousers/Square Joker/Runner/Ride the Bus/Obelisk/Green Joker's per-hand growth, Space Joker's random lev *(verified)*
   - vanilla: `state_events.lua:628-638 (the context.before pass, run before mult/hand_chips are re-fetched at line 640-641 and before the joker_main pass at line 87` · kotlin: `game/Score.kt:48 (Sctx.before declared but never set true anywhere in score()); game/JokerManifest.kt:194-201 (j_green_j`
   - fix: Change these jokers' reduce from GameEvent.HandScored to GameEvent.BeforeHand (dispatched inside Score.score(), Score.kt:521) so the growth applies before that hand's joker_main reads it, matching the pattern already use
9. **[BLOCKER]** ⚠️ `scoring pipeline (evaluate_play sequence: card order, retriggers, held-in-hand, joker context order, edition/seal timing)` — The Flint boss blind: halve base chips/mult using round-half-up with a minimum Mult floor of 1 (chips floor of 0). *(verified)*
   - vanilla: `blind.lua:511-513: `return math.max(math.floor(mult*0.5 + 0.5), 1), math.max(math.floor(hand_chips*0.5 + 0.5), 0), true` — round-half-up, and Mult can` · kotlin: `game/Score.kt:485: `if (debuff is Debuff.Flint) { chips = floor(chips / 2); mult = floor(mult / 2) }` — plain integer-st`
   - fix: Replace `floor(chips/2)`/`floor(mult/2)` with `floor(chips*0.5+0.5).coerceAtLeast(0)` and `floor(mult*0.5+0.5).coerceAtLeast(1)`.
10. **[MAJOR]** ⚠️ `Blinds & boss effects (blind.lua hooks + P_BLINDS roster) vs Kotlin RunScreen.kt/Blinds.kt` — THE_MANACLE boss debuff *(verified)*
   - vanilla: `blind.lua:187-189 (set_blind: `if self.name == 'The Manacle' and not reset then G.hand:change_size(-1) end`), blind.lua:341-343 & 386-390 (restore +1 ` · kotlin: `game/Blinds.kt:54,62,100-104 (`THE_MANACLE -> default + 1` hands, comment "+1 Hand, -1 Joker slot"); ui/RunScreen.kt:131`
11. **[MAJOR]** ⚠️ `Blinds & boss effects (blind.lua hooks + P_BLINDS roster) vs Kotlin RunScreen.kt/Blinds.kt` — THE_HOOK boss trigger and cost *(verified)*
   - vanilla: `blind.lua:464-487 (press_play: fires on every PLAY, randomly picks 2 cards from the REMAINING hand — not the played cards — and discards them); functi` · kotlin: `ui/RunScreen.kt:1818-1826 (`fun discard() { ... if (boss==THE_HOOK) { val hookIndices = hand.indices.shuffled().take(2).`
12. **[MAJOR]** ⚠️ `Blinds & boss effects (blind.lua hooks + P_BLINDS roster) vs Kotlin RunScreen.kt/Blinds.kt` — THE_SERPENT boss effect *(verified)*
   - vanilla: `functions/state_events.lua:355-374 (`G.FUNCS.draw_from_deck_to_hand`: once `hands_played>0 OR discards_used>0`, every subsequent refill draws AT MOST ` · kotlin: `ui/RunScreen.kt:1642 (`if (boss == Boss.THE_SERPENT) { hand = deck.draw(hand.size); faceDown = emptySet(); ...; selected`
13. **[MAJOR]** ⚠️ `Blinds & boss effects (blind.lua hooks + P_BLINDS roster) vs Kotlin RunScreen.kt/Blinds.kt` — THE_FLINT modify_hand halving formula *(verified)*
   - vanilla: `blind.lua:510-517 (`return math.max(math.floor(mult*0.5 + 0.5), 1), math.max(math.floor(hand_chips*0.5 + 0.5), 0), true`) — round-half-up, floored at ` · kotlin: `game/Score.kt:485 (`if (debuff is Debuff.Flint) { chips = floor(chips / 2); mult = floor(mult / 2) }`)`
14. **[MAJOR]** ⚠️ `Blinds & boss effects (blind.lua hooks + P_BLINDS roster) vs Kotlin RunScreen.kt/Blinds.kt` — THE_EYE / THE_MOUTH / THE_PSYCHIC debuff_hand consequence *(verified)*
   - vanilla: `blind.lua:519-548 (debuff_hand returns triggered=true for a repeat hand-type/locked-type/wrong-size hand); functions/state_events.lua:614-645 (`if not` · kotlin: `ui/RunScreen.kt:1372-1374 (`if (boss==THE_PSYCHIC && sel.size!=5) return`; `if (boss==THE_EYE && handType0 in eyeUsedHan`
15. **[MAJOR]** ⚠️ `Blinds & boss effects (blind.lua hooks + P_BLINDS roster) vs Kotlin RunScreen.kt/Blinds.kt` — THE_OX "most played hand" snapshot timing *(verified)*
   - vanilla: `functions/state_events.lua:127-136 (`current_round.most_played_poker_hand` is recomputed ONCE, only at the END of a defeated Boss round, from lifetime` · kotlin: `ui/RunScreen.kt:846 (`val mostPlayedHand: Pair<HandType,Int>? get() = _handPlayed.entries.maxByOrNull { it.value }`); :1`
16. **[MAJOR]** ⚠️ `Blinds & boss effects (blind.lua hooks + P_BLINDS roster) vs Kotlin RunScreen.kt/Blinds.kt` — THE_TOOTH money loss floor *(verified)*
   - vanilla: `blind.lua:497-507 (press_play: `for i=1,#G.play.cards do ease_dollars(-1) ... end`); functions/common_events.lua:68-83 (`ease_dollars` has no floor — ` · kotlin: `ui/RunScreen.kt:1549 (`if (boss == Boss.THE_TOOTH) money = maxOf(0, money - pendingSel.size)`)`
17. **[MAJOR]** ⚠️ `Blinds & boss effects (blind.lua hooks + P_BLINDS roster) vs Kotlin RunScreen.kt/Blinds.kt` — THE_FISH face-down cards on the opening hand *(verified)*
   - vanilla: `blind.lua:176-178 (set_blind: `if self.name=='The Fish' and not reset then self.prepped=nil end` — prepped starts OFF); blind.lua:494-496 (press_play ` · kotlin: `ui/RunScreen.kt:1267 (`Boss.THE_FISH -> newIndices`, unconditional) combined with :1322 (`applyFaceDown(hand.indices.toS`
18. **[MAJOR]** ⚠️ `Blinds & boss effects (blind.lua hooks + P_BLINDS roster) vs Kotlin RunScreen.kt/Blinds.kt` — CRIMSON_HEART joker-disable timing (first hand) *(verified)*
   - vanilla: `blind.lua:94 (set_blind: `self.prepped = true` — default true for all blinds except Fish); blind.lua:588-600 (drawn_to_hand: disables a random joker w` · kotlin: `ui/RunScreen.kt:1308 (`crimsonHeartDisabled = null` at round start); :1645-1649 (only assigned inside `scoreBank()`, i.e`
19. **[MAJOR]** ❌ `Rendering & shaders — card/joker edition shimmer, dissolve/materialize, particles, felt/CRT post-processing, hover-tilt (vanilla shaders in src/vanilla/resources/shaders/*.fs, engine/sprite.lua, engine/particles.lua, card.lua draw)` — Full-screen CRT post-process filter (scanlines, vignette distortion, bloom, noise) applied over the entire play canvas every frame
   - vanilla: `src/vanilla/resources/shaders/CRT.fs (153 lines); game.lua:2940-2952 (G.SHADERS['CRT']:send(...) + love.graphics.setShader(G.SHADERS['CRT']) wrapping ` · kotlin: `none (only mention: ui/MainActivity.kt:216 comment 'CRT/shake ... Android-inapplicable' for the options screen, no shade`
   - fix: Port CRT.fs to an AGSL RuntimeShader applied as a graphicsLayer renderEffect on the top-level RunScreen Box, driven by the same uniform formulas (distortion/scale/bloom/noise/scanlines), gated by a settings toggle.
20. **[MAJOR]** ❌ `Rendering & shaders — card/joker edition shimmer, dissolve/materialize, particles, felt/CRT post-processing, hover-tilt (vanilla shaders in src/vanilla/resources/shaders/*.fs, engine/sprite.lua, engine/particles.lua, card.lua draw)` — debuff.fs (red diagonal-slash desaturation overlay on boss-suit/face-debuffed playing cards) and played.fs (darkened/desaturated overlay on 'greyed' cards/disab
   - vanilla: `src/vanilla/resources/shaders/debuff.fs, played.fs; card.lua:4526-4537 (self.debuff -> 'debuff' shader on children.center/front; self.greyed -> 'playe` · kotlin: `none — grep for isCardDebuffed/DebuffOverlay/isDisabled/greyOut/desaturate in ui/RunScreen.kt returns nothing; boss debu`
   - fix: Port debuff.fs and played.fs to AGSL RenderEffects (same HSL/RGB pattern as Editions.kt); apply as a graphicsLayer overlay on CardFace when the card is in the active Debuff set, and on the joker chip when it equals debuf
21. **[MAJOR]** ❌ `Rendering & shaders — card/joker edition shimmer, dissolve/materialize, particles, felt/CRT post-processing, hover-tilt (vanilla shaders in src/vanilla/resources/shaders/*.fs, engine/sprite.lua, engine/particles.lua, card.lua draw)` — Particles engine (engine/particles.lua) — the whole square-confetti particle system used for card dissolve/shatter/materialize debris, booster-pack-opening spar
   - vanilla: `engine/particles.lua (full Particles class, 177 lines); card.lua:1990-2205 (Card:explode_card/shatter/start_dissolve/start_materialize each spawn a Pa` · kotlin: `none — grep -rln "Particle" across ui/game/kotlin returns no files`
   - fix: Port Particles.lua's update/move loop (age/velocity/scale-ease formulas) to a small Compose particle system (Canvas + list of particle state, animated via withFrameNanos), instantiated at the same trigger points (shatter
22. **[MAJOR]** ⚠️ `Save/load & profiles` — Explicit New-Run vs Continue choice from the main menu — Play always offers a fresh run tab even when a save exists, Continue tab is only enabled/pre-selected w *(verified)*
   - vanilla: `functions/UI_definitions.lua:5297-5320 (G.UIDEF.run_setup shows 'New Run' and 'Continue' as two tabs; 'New Run' is chosen by default unless can_contin` · kotlin: `ui/MainActivity.kt:126-144 (single onPlay handler: `showRun = true`, no branch for new-vs-continue); ui/RunScreen.kt:248`
   - fix: Surface hasSave as a real UI choice on the Play flow (New Run tab that deletes the existing save file + resets RunState, vs. Continue tab that restores it), instead of auto-resuming unconditionally.
23. **[MAJOR]** ❌ `Save/load & profiles` — 3-slot profile system with per-profile switch/create/delete and independent save/meta/stats per slot
   - vanilla: `globals.lua:260-264 (self.PROFILES = {{},{},{}}); game.lua:845-925 Game:load_profile(_profile) (merges temp_profile defaults into G.PROFILES[profile])` · kotlin: `save/StatsStore.kt:17 (single hardcoded "balatro" SharedPreferences bucket, no profile index); ui/MainActivity.kt:128 (s`
   - fix: If multi-profile support is in scope, parameterize SaveIo/StatsStore file/prefs names by a profile index and add a profile-switch UI; otherwise explicitly document single-profile as an accepted simplification.
24. **[MAJOR]** ◑ `Save/load & profiles` — Per-profile career stats, high scores, and usage tallies (career_stats c_* counters, high_scores by category, joker/consumable/voucher/hand/deck usage maps, per
   - vanilla: `game.lua:857-912 (temp_profile shape: high_scores{hand,furthest_round,furthest_ante,most_money,boss_streak,collection,win_streak,current_streak,poker_` · kotlin: `save/StatsStore.kt:10-15 (Stats{games, wins, bestAnte, totalHands, bestScore} — 5 fields total, no per-category breakdow`
   - fix: Extend StatsStore.Stats with the vanilla category breakdown (high_scores map, career_stats counters, usage maps) persisted as JSON (mirroring RunSnapshot's approach) rather than flat SharedPreferences ints, and feed it f
25. **[MAJOR]** ❌ `The 150 vanilla Balatro jokers — config fidelity and scoring-trigger correctness in the Kotlin rebuild's Score.kt/JokerManifest.kt engine (cross-checked against RunScreen.kt where a joker's mechanic is legitimately non-scoring, e.g. economy/shop hooks)` — Wee Joker — +8 Chips permanently accumulated each time a scored card has rank 2, read every hand by joker_main
   - vanilla: `card.lua:3083-3092 (context.individual, `context.other_card:get_id() == 2` → self.ability.extra.chips += self.ability.extra.chip_mod(8)) read at joker` · kotlin: `Score.kt:313-314`
   - fix: Add a JOKER_MANIFEST entry using the perCard two-phase pattern (like j_cry_krustytheclown): `perCard = { s, ctx, c -> if (ctx.rankOf(c) == 2) s.copy(chips = s.chips + 8.0) else s }, jokerMain = { s, _ -> Effect.chipsOrNo
26. **[MAJOR]** ❌ `The 150 vanilla Balatro jokers — config fidelity and scoring-trigger correctness in the Kotlin rebuild's Score.kt/JokerManifest.kt engine (cross-checked against RunScreen.kt where a joker's mechanic is legitimately non-scoring, e.g. economy/shop hooks)` — Ride the Bus — +1 Mult (accumulated) each hand with no face card scored; resets to 0 the instant a face card scores
   - vanilla: `card.lua:3525-3541 (context.before) read by joker_main at card.lua:3992-3997` · kotlin: `none (0 hits for "j_ride_the_bus" anywhere in the rebuild tree)`
   - fix: Add BeforeHand reducer (reset mult=0 if any scored card is_face, else mult += 1) + jokerMain multOrNone(mult), plus a shop Offer entry.
27. **[MAJOR]** ❌ `The 150 vanilla Balatro jokers — config fidelity and scoring-trigger correctness in the Kotlin rebuild's Score.kt/JokerManifest.kt engine (cross-checked against RunScreen.kt where a joker's mechanic is legitimately non-scoring, e.g. economy/shop hooks)` — Madness — on selecting a non-boss Blind, +0.5 Xmult (accumulated, read every joker_main) AND destroys one random other non-eternal Joker
   - vanilla: `card.lua:2503-2520 (context.select_blind, not context.blind.boss) + joker_main read (card.lua:850 loc_vars, x_mult accumulator)` · kotlin: `none (0 hits anywhere in the rebuild tree)`
   - fix: Add a BlindSelected-style GameEvent (mirrors the existing BlindSkipped event already wired for j_throwback) that grows x and picks/destroys a random other joker in RunScreen; jokerMain reads x like the other X-mult accum
28. **[MAJOR]** ❌ `The 150 vanilla Balatro jokers — config fidelity and scoring-trigger correctness in the Kotlin rebuild's Score.kt/JokerManifest.kt engine (cross-checked against RunScreen.kt where a joker's mechanic is legitimately non-scoring, e.g. economy/shop hooks)` — Bloodstone — per scored Heart, 1-in-2 pseudorandom chance of X1.5 Mult
   - vanilla: `card.lua:3247-3253 (context.individual, is_suit('Hearts') && pseudorandom('bloodstone') < probabilities.normal/odds(2) → x_mult=1.5)` · kotlin: `none (0 hits anywhere in the rebuild tree)`
   - fix: Port using the same deterministic per-card seeded-Random approach already established for Lucky/m_lucky rolls in Score.score().
29. **[MAJOR]** ❌ `The 150 vanilla Balatro jokers — config fidelity and scoring-trigger correctness in the Kotlin rebuild's Score.kt/JokerManifest.kt engine (cross-checked against RunScreen.kt where a joker's mechanic is legitimately non-scoring, e.g. economy/shop hooks)` — Fortune Teller — joker_main +Mult = total Tarot cards used this run (unconditional, reads a running consumable-usage counter)
   - vanilla: `card.lua:4016-4021 (`self.ability.name == 'Fortune Teller' and G.GAME.consumeable_usage_total.tarot > 0` → mult_mod = tarot count)` · kotlin: `none (0 hits anywhere in the rebuild tree)`
   - fix: Track a run-scoped tarotUsedCount in RunScreen (already tracks similar per-consumable counters for other jokers like j_astronomer's planet-discovery gate), expose via Sctx, and add a ctx-read jokerMain spec.
30. **[MAJOR]** ❌ `The 150 vanilla Balatro jokers — config fidelity and scoring-trigger correctness in the Kotlin rebuild's Score.kt/JokerManifest.kt engine (cross-checked against RunScreen.kt where a joker's mechanic is legitimately non-scoring, e.g. economy/shop hooks)` — Satellite — end-of-round $ payout = extra × (distinct Planet types used this run), via calculate_dollar_bonus
   - vanilla: `card.lua:1667-1676 (Card:calculate_dollar_bonus, counts distinct planet sets in G.GAME.consumeable_usage)` · kotlin: `none — absent from RunScreen.kt's buildCashOut() jokerDollars list (RunScreen.kt:1693-1711, which correctly implements t`
   - fix: Add `owned.count { it.offer.key == "j_satellite" } * distinctPlanetsUsedThisRun` to the jokerDollars sum in buildCashOut(), tracking distinct planet types used (not just count) to match vanilla's per-type dedup.
31. **[MAJOR]** ❌ `The 150 vanilla Balatro jokers — config fidelity and scoring-trigger correctness in the Kotlin rebuild's Score.kt/JokerManifest.kt engine (cross-checked against RunScreen.kt where a joker's mechanic is legitimately non-scoring, e.g. economy/shop hooks)` — Invisible Joker — after 2 rounds unused, can be sold to duplicate a random other Joker
   - vanilla: `card.lua:308 (invis_rounds init), 2371 (round counter check ≥ extra(2)), 2934/4439 (sell-to-duplicate trigger)` · kotlin: `none (0 hits anywhere in the rebuild tree, including no shop Offer entry)`
   - fix: Add shop Offer + a round-counter field on FJoker/OwnedJoker + a sell-time branch that spawns a duplicate of a random other owned joker once invis_rounds >= 2.
32. **[MAJOR]** ⚠️ `UI panels (create_UIBox_* — HUD, run flow, overlays, meta/collection)` — your_collection + 12 category sub-browsers (jokers/tarots/planets/spectrals/vouchers/seals/enhancements/editions/decks/tags/blinds/boosters) *(verified)*
   - vanilla: `functions/UI_definitions.lua:3497 (your_collection) + :3535-3967 (12 dedicated create_UIBox_your_collection_* functions, each a distinct CardArea-base` · kotlin: `rebuild/.../ui/MainActivity.kt:152-213 (single Material-ish felt LazyVerticalGrid with 5 hardcoded tabs: Jokers/Tarots/P`
   - fix: Extract each your_collection_* tree via tools/uiref like the 14 already-ported panels, add the missing 7 tab categories, bind via a CollectionBind analogous to HudBind.
33. **[MAJOR]** ⚠️ `UI panels (create_UIBox_* — HUD, run flow, overlays, meta/collection)` — create_UIBox_options (in-run pause menu: Restart Run, Main Menu, Collection shortcut, Customize Deck shortcut, Settings, Stats, seed display+copy) *(verified)*
   - vanilla: `functions/UI_definitions.lua:2208-2263 create_UIBox_options — a button list of Settings/Seed(+copy)/Restart run/Main menu/Stats/Collection/Customize d` · kotlin: `rebuild/.../ui/MainActivity.kt:218-263 (hand-built Material felt dialog: Sound ON/OFF, Music ON/OFF, Lifetime Stats only`
   - fix: Extract create_UIBox_generic_options + create_UIBox_options as a real tree; wire Restart/MainMenu/Collection/CustomizeDeck/seed actions; make it reachable via the HUD options button fix above.
34. **[MAJOR]** ❌ `audio + input/controller` — Universal 'button' click SFX on every clickable UIBox node (base UIElement:click() calls play_sound('button',1,0.3) unconditionally for any config.button elemen
   - vanilla: `engine/ui.lua:989 (UIElement:click)` · kotlin: `rebuild/.../ui/UILayout.kt:326-366 (renderNode, R/C/ROOT/B branch: cfg.onClick.invoke() with no SoundManager call anywhe`
   - fix: Add SoundManager.play("button") inside renderNode's two cfg.onClick.invoke() call sites (or centrally in a shared onClick wrapper) so every UIBox-tree button gets the vanilla click blip by default, overridable per-node l
35. **[MAJOR]** ❌ `audio + input/controller` — Music state machine: desired_track selects among music1 (default), music2 (any booster pack open), music3 (meteor/sparkle pack variant), music4 (shop), music5 (
   - vanilla: `functions/misc_functions.lua:719-780 (modulate_sound: desired_track selection, PITCH_MOD) + engine/sound_manager.lua:81-105 (SET_SFX music crossfade)` · kotlin: `rebuild/.../audio/MusicManager.kt:1-55 (single hardcoded TRACK="music1.ogg", started on Activity.onResume, paused on onP`
   - fix: Bundle music2-5.ogg, add MusicManager.setTrack(trackId) driven by RunState.phase/boss, crossfade via two MediaPlayers or gapless volume ramp; feed a PITCH_MOD-equivalent playback speed (API 23+ PlaybackParams) on game-ov
36. **[MAJOR]** ⚠️ `audio + input/controller` — Three independent continuous volume sliders: master (SOUND.volume, default 50), music_volume (default 100), game_sounds_volume (default 100), each 0-100, multip *(verified)*
   - vanilla: `globals.lua:164-168 (SOUND table) + functions/UI_definitions.lua:2329-2330 (create_slider for music/game vol) + engine/sound_manager.lua:90,97 (multip` · kotlin: `rebuild/.../ui/MainActivity.kt:219-246 (two boolean BButton toggles: "Sound: ON/OFF", "Music: ON/OFF" backed by SharedPr`
   - fix: Add a volume Float (0-1) per channel (master/music/sfx) persisted to SharedPreferences, wire SoundManager.play()'s volume param and MusicManager.setVolume() to (master*channel), and add Slider composables to the Options 
37. **[MAJOR]** ◑ `audio + input/controller` — Full SFX cue roster (~45 distinct sound_code roots incl. numeric-suffix families: crumple1-5, glass1-6, voice1-11, foil1-2, multhit1-2, coin1/2/3/6/7, holo1, po
   - vanilla: `repo-wide `play_sound(` call sites (card.lua, game.lua, blind.lua, cardarea.lua, back.lua, tag.lua, functions/button_callbacks.lua, functions/misc_fun` · kotlin: `rebuild/.../audio/SoundManager.kt:15-18 (NAMES list, 17 entries) + assets/sounds/ (17 .ogg files bundled, confirmed via `
   - fix: Extract the remaining .ogg files from Balatro.love resources/sounds, add to assets/sounds and SoundManager.NAMES, then wire the corresponding call sites (see other findings) that currently substitute a wrong or no cue.
38. **[MAJOR]** ⚠️ `audio + input/controller` — Run-win sting: play_sound('win') on Game:win_game *(verified)*
   - vanilla: `functions/state_events.lua:33 (win_game(): play_sound('win'))` · kotlin: `rebuild/.../ui/RunScreen.kt:2532 (Phase.WIN -> SoundManager.play("timpani"))`
   - fix: Bundle win.ogg, change RunScreen.kt:2532 to SoundManager.play("win").
39. **[MAJOR]** ⚠️ `audio + input/controller` — Run-loss sting: play_sound('negative', 0.5, 0.7) + play_sound('whoosh2', 0.9, 0.7) on Game:update_game_over *(verified)*
   - vanilla: `game.lua:3593-3594 (Game:update_game_over)` · kotlin: `rebuild/.../ui/RunScreen.kt:2533 (Phase.OVER -> SoundManager.play("gong"))`
   - fix: Bundle negative.ogg + whoosh2.ogg, change RunScreen.kt:2533 to play both cues (SoundManager.play("negative", rate=0.5) and SoundManager.play("whoosh2", rate=0.9)).
40. **[MAJOR]** ❌ `audio + input/controller` — Card hover/peek SFX + hold-to-peek tooltip on touch: Card:hover() plays play_sound('paper1', random pitch, 0.35) and (on HID.touch, after a G.MIN_HOVER_TIME=0.1
   - vanilla: `card.lua:4306-4327 (Card:hover) + engine/controller.lua:358-366,392-419 (touch-only hover gating + delayed hover() call via Event with delay=G.MIN_HOV` · kotlin: `rebuild/.../ui/RunScreen.kt:3538-3582 (hand card Box: only `.clickable` — tap toggles selection via s.toggle(i) and play`
   - fix: Add a long-press (or press-and-hold via awaitPointerEventScope with a 0.1s timer) gesture on hand-card Boxes that plays "paper1" and opens a DetailTooltip showing rank/suit/enhancement/seal/edition text, without triggeri
41. **[MAJOR]** ❌ `board physics & card containers (CardArea/Moveable/Card — hand, jokers, consumeables, play, deck, discard)` — Hand card selection has no cap — vanilla limits G.hand.highlighted to config.highlighted_limit (default 5): once 5 cards are highlighted, CardArea:add_to_highli
   - vanilla: `cardarea.lua:18 (highlighted_limit default 5), cardarea.lua:148-159 (add_to_highlighted hand-branch cap check)` · kotlin: `ui/RunScreen.kt:1338-1354 (fun toggle) — `selected = if (i in selected) selected - i else selected + i` with zero size c`
   - fix: Add a `maxSelect` field to RunState (default 5, override where a Cryptid mechanic needs more) and gate toggle()'s add-path on `selected.size < maxSelect`.
42. **[MAJOR]** ❌ `board physics & card containers (CardArea/Moveable/Card — hand, jokers, consumeables, play, deck, discard)` — Hand auto-sort by rank/suit — CardArea:sort (config.sort defaults 'desc') runs after every draw, and vanilla exposes a rank/suit toggle button so the hand alway
   - vanilla: `cardarea.lua:22 (config.sort default 'desc'), cardarea.lua:577-590 (CardArea:sort desc/asc/suit desc/suit asc/order)` · kotlin: `game/Deck.kt:25-29 (fun draw — no sort applied); no `sort`/`get_nominal`/rank-suit toggle exists anywhere in ui/RunScree`
   - fix: Add CardArea-equivalent sort(mode) applied to `hand` after every draw/refill, plus the rank/suit toggle button near the hand.
43. **[MAJOR]** ❌ `board physics & card containers (CardArea/Moveable/Card — hand, jokers, consumeables, play, deck, discard)` — Deck pile renders as one static card-back sprite + a text counter — vanilla shows the whole deck as a fanned stack of individually-positioned card-back Moveable
   - vanilla: `cardarea.lua:412-424 (align_cards deck branch: per-card x/y parallax offset, shuffle_amt rotation/x wobble)` · kotlin: `engine/EngineHost.kt:28 (`val deck = areaMoveable(Room.deck)` — plain Moveable); ui/RunScreen.kt:3605-3610 (single stati`
   - fix: Promote host.deck to a real CardArea (kind="deck"), keep it populated with placeholder card-backs matching deckRemaining, port the deck align branch's parallax stacking, and trigger a shuffle_amt pulse on reshuffle().
44. **[MAJOR]** ❌ `board physics & card containers (CardArea/Moveable/Card — hand, jokers, consumeables, play, deck, discard)` — Clicking the deck opens a full deck viewer — CardArea:click (self==G.deck → G.FUNCS.deck_info()) and Card:click's same-effect shortcut on the top deck card.
   - vanilla: `cardarea.lua:613-617 (CardArea:click), card.lua:4610-4620 (Card:click deck_info branch)` · kotlin: `none — grep for DeckInfo/deckInfo/viewDeck/VIEWING_DECK across ui/RunScreen.kt returns nothing`
   - fix: Add a clickable modifier on the deck Box that opens a scrollable deck-contents view (list remaining PlayingCards with enhancement/seal/edition).
45. **[MAJOR]** ⚠️ `board physics & card containers (CardArea/Moveable/Card — hand, jokers, consumeables, play, deck, discard)` — G.HIGHLIGHT_H (the Y-lift applied to a selected/highlighted card) is a fixed room-unit constant, 0.2 × G.CARD_H. *(verified)*
   - vanilla: `globals.lua:276 (`self.HIGHLIGHT_H = 0.2*self.CARD_H`), globals.lua:274-275 (`CARD_W=2.4*35/41`, `CARD_H=2.4*47/41` ⇒ HIGHLIGHT_H ≈ 0.5502)` · kotlin: `engine/CardArea.kt:148 (`const val HIGHLIGHT_H = 1.0`)`
   - fix: Set `HIGHLIGHT_H = 0.2 * CARD_H` (≈0.5502) instead of the hardcoded 1.0.
46. **[MAJOR]** ⚠️ `game flow & state machine` — Fresh run always begins at G.STATES.BLIND_SELECT (start_run prep_stage default) — the player must explicitly select/confirm the Small Blind of Ante 1 before the *(verified)*
   - vanilla: `game.lua:2018-2024 (Game:start_run: self:prep_stage(G.STAGES.RUN, saveTable and saveTable.STATE or G.STATES.BLIND_SELECT)); game.lua:3258-3300 (update` · kotlin: `RunScreen.kt:1189-1220 (pickDeck: `deck.setComposition(...); startRound(); phase = Phase.ROUND` — the DECK_SELECT screen`
   - fix: pickDeck() should set phase = Phase.BLIND_SELECT (like a normal round boundary) instead of calling startRound() directly; startRound() should only run from selectBlind().
47. **[MAJOR]** ❌ `game flow & state machine` — Winning the Boss Blind of the win ante (ante 8) offers an 'Endless' button (b_endless / exit_overlay_menu) that dismisses the win overlay and lets the run conti
   - vanilla: `functions/UI_definitions.lua create_UIBox_win (~line 2785: `UIBox_button({button = 'exit_overlay_menu', label = {localize('b_endless')}, ...})`); func` · kotlin: `RunScreen.kt:1805-1807 (cashOut(): `if (wasSlot == 2 && wasAnte == 8) { phase = Phase.WIN; ... }`); RunScreen.kt:3836-39`
   - fix: Add an Endless option on WinScreen that clears the win-terminal condition and routes back to Phase.SHOP (blindIndex already advanced by cashOut()), continuing the ante/target scaling formula past ante 8.
48. **[MAJOR]** ◑ `game flow & state machine` — save_run() is called at the end of nearly every state's update handler (SELECTING_HAND completion i.e. after each hand/draw, BLIND_SELECT entry, SHOP entry, ROU
   - vanilla: `game.lua:3060-3069 (update_selecting_hand: `save_run(); G.CONTROLLER:recall_cardarea_focus('hand')` on every STATE_COMPLETE cycle, i.e. after each han` · kotlin: `RunScreen.kt:2519-2528 (`LaunchedEffect(s.phase, s.blindIndex, s.money, s.consumables.size) { when (s.phase) { Phase.SHO`
   - fix: Also autosave on hand-played/discard/draw boundaries within Phase.ROUND (e.g. after scoreBank()/discard()/refill()), matching vanilla's per-SELECTING_HAND-completion save_run() cadence.
49. **[MAJOR]** ❌ `run setup & meta screens (main menu, deck/stake select, options/settings, high scores, your collection, challenges, deck/stake definitions)` — Plasma Deck (15th deck): ante_scaling=2 config + trigger_effect 'final_scoring_step' that averages chips/mult ((chips+mult)/2 each) with the 'balanced' animatio
   - vanilla: `src/vanilla/game.lua:641 (config), src/vanilla/back.lua:121-171 (trigger_effect balancing), src/vanilla/back.lua:263-264 (ante_scaling apply)` · kotlin: `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:78-97 (DeckVariant enum — 14 entries, no PLASMA)`
   - fix: Add DeckVariant.PLASMA with an anteScalingDelta, and hook the final-scoring averaging into Score.kt's total-chips/mult computation when active.
50. **[MAJOR]** ❌ `run setup & meta screens (main menu, deck/stake select, options/settings, high scores, your collection, challenges, deck/stake definitions)` — Stake 4/7/8 shop-generation modifiers (enable_eternals_in_shop / enable_perishables_in_shop / enable_rentals_in_shop) — random eternal/perishable/rental sticker
   - vanilla: `src/vanilla/game.lua:2055,2058,2059 (stake sets modifiers); src/vanilla/functions/common_events.lua:2137-2146 (poll applies stickers on shop/pack card` · kotlin: `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:3846`
51. **[MAJOR]** ❌ `run setup & meta screens (main menu, deck/stake select, options/settings, high scores, your collection, challenges, deck/stake definitions)` — Deck unlock progression (discover_amount / win_deck / win_stake unlock_condition gates 14 of 15 decks; only Red + Challenge start unlocked)
   - vanilla: `src/vanilla/game.lua:628-644 (unlock_condition per deck); src/vanilla/back.lua:36-53 (locked-deck description rendering)` · kotlin: `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:3850-3889 (DeckSelectScreen — DeckVariant.values().forEach w`
52. **[MAJOR]** ❌ `run setup & meta screens (main menu, deck/stake select, options/settings, high scores, your collection, challenges, deck/stake definitions)` — Stake unlock progression (only White Chip starts unlocked; each higher stake requires clearing the previous one) + per-deck max-stake gating (G.UIDEF.stake_opti
   - vanilla: `src/vanilla/game.lua:253-260 (stake_red..stake_gold all 'unlocked = false'); src/vanilla/functions/UI_definitions.lua:5460-5477 (stake_option caps sta` · kotlin: `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:3853,3865-3867 (stake stepper: 'stake > 1'/'stake < 8', no g`
53. **[MAJOR]** ❌ `run setup & meta screens (main menu, deck/stake select, options/settings, high scores, your collection, challenges, deck/stake definitions)` — In-run Options overlay (create_UIBox_options when G.STAGE==RUN): 'Start New Run' restart button, 'Main Menu' button, current-seed display + copy button, mid-run
   - vanilla: `src/vanilla/functions/UI_definitions.lua:2208-2263` · kotlin: `rebuild/app/src/main/kotlin/systems/balatro/ui/MainActivity.kt:265-267 (showRun renders RunScreen as an opaque full-scre`
54. **[MAJOR]** ⚠️ `run setup & meta screens (main menu, deck/stake select, options/settings, high scores, your collection, challenges, deck/stake definitions)` — Explicit New Run vs Continue choice with saved-run summary (round/ante/money/best-hand/seed) before committing *(verified)*
   - vanilla: `src/vanilla/functions/UI_definitions.lua:5297-5337 (run_setup tab bar), 5961-6072 (Continue tab renders saved_game.GAME.round/ante/dollars/best_hand/s` · kotlin: `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:2489-2513`
55. **[MAJOR]** ◑ `run setup & meta screens (main menu, deck/stake select, options/settings, high scores, your collection, challenges, deck/stake definitions)` — Your Collection: 12 vanilla subcategories (jokers, tarots, boosters, planets, spectrals, vouchers, seals, enhancements, editions, decks, tags, blinds)
   - vanilla: `src/vanilla/functions/UI_definitions.lua:3497-4093 (create_UIBox_your_collection_* — 3535 jokers,3577 tarots,3619 boosters,3661 planets,3696 spectrals` · kotlin: `rebuild/app/src/main/kotlin/systems/balatro/ui/MainActivity.kt:161-168 (tabs = Jokers, Tarots, Planets, Spectrals, Vouch`
56. **[MAJOR]** ❌ `run setup & meta screens (main menu, deck/stake select, options/settings, high scores, your collection, challenges, deck/stake definitions)` — Card/item discovery-lock system: undiscovered items render as '?' (bypass_discovery_ui=false) until first encountered in a run; unlock_condition gating for cent
   - vanilla: `src/vanilla/functions/common_events.lua:2126-2130 (discover flag on card creation); back.lua:36-53 (locked-item description branch)` · kotlin: `none`
57. **[MAJOR]** ◑ `run setup & meta screens (main menu, deck/stake select, options/settings, high scores, your collection, challenges, deck/stake definitions)` — High Scores screen: hand / furthest_round / furthest_ante / poker_hand / most_money / win_streak rows + progress bars (collection %, deck_stake_wins %, joker_st
   - vanilla: `src/vanilla/functions/UI_definitions.lua:2591-2621 (create_UIBox_high_scores), 2623-2692 (create_progress_box)` · kotlin: `rebuild/app/src/main/kotlin/systems/balatro/save/StatsStore.kt (whole file); rebuild/app/src/main/kotlin/systems/balatro`
58. **[MAJOR]** ◑ `run setup & meta screens (main menu, deck/stake select, options/settings, high scores, your collection, challenges, deck/stake definitions)` — Settings: gamespeed, play/discard button position, rumble, screenshake slider, stake-stickers display toggle, high-contrast/colourblind toggle, reduced-motion t
   - vanilla: `src/vanilla/functions/UI_definitions.lua:2299-2331 (G.UIDEF.settings_tab 'Game' and 'Audio' branches)` · kotlin: `rebuild/app/src/main/kotlin/systems/balatro/ui/MainActivity.kt:218-263 (showSettings block)`
59. **[MAJOR]** ❌ `run setup & meta screens (main menu, deck/stake select, options/settings, high scores, your collection, challenges, deck/stake definitions)` — Challenge system: 37 challenges (challenges.lua) with fixed starting jokers/consumables/vouchers/deck and rule modifiers, challenge list/description UI, Challen
   - vanilla: `src/vanilla/challenges.lua:1-738 (37 'id = "c_..."' entries); src/vanilla/functions/UI_definitions.lua:5502-5960 (G.UIDEF.challenge_list / challenge_l` · kotlin: `none`
60. **[MAJOR]** ⚠️ `scoring pipeline (evaluate_play sequence: card order, retriggers, held-in-hand, joker context order, edition/seal timing)` — Boss blinds that disallow an entire hand type this round (The Eye: no repeats; The Mouth: only the first type played) — vanilla lets the hand play out and score *(verified)*
   - vanilla: `functions/state_events.lua:614 gates the whole scoring branch on `G.GAME.blind:debuff_hand(...)`; when true, the `else` branch (state_events.lua:997-1` · kotlin: `ui/RunScreen.kt:1373-1374: `if (boss == Boss.THE_EYE && handType0 in eyeUsedHands) return` / `if (boss == Boss.THE_MOUTH`
   - fix: Let play() proceed for a disallowed type: run Score.score() normally but force chips=0/mult=0 (a Debuff.WholeHandDisallowed variant), still consume the hand attempt and clear the selection, matching state_events.lua's el
61. **[MAJOR]** ❌ `scoring pipeline (evaluate_play sequence: card order, retriggers, held-in-hand, joker context order, edition/seal timing)` — Plasma Deck's final_scoring_step hook (averages chips and mult) fires after the joker-main pass, before card destruction.
   - vanilla: `functions/state_events.lua:946-948: `local nu_chip, nu_mult = G.GAME.selected_back:trigger_effect{context = 'final_scoring_step', chips = hand_chips, ` · kotlin: `none — grep for "Plasma" across rebuild/app/src/main/kotlin/systems/balatro returns zero hits; game/Score.kt's cascade e`
   - fix: Add a deck-back hook at the end of Score.score(), applied after the joker-main pass and before returning ScoreResult: `if (deck == Deck.PLASMA) { val tot = chips+mult; chips = floor(tot/2); mult = floor(tot/2) }`.
62. **[MAJOR]** ⚠️ `shop &amp; economy` — Joker edition odds/distribution in the shop and packs *(verified)*
   - vanilla: `functions/common_events.lua:2055-2080 (poll_edition), functions/common_events.lua:2133-2151 (create_card calls poll_edition for every Joker card, shop` · kotlin: `ui/RunScreen.kt:730-739 (drawJoker: 60% chance, first joker only, uniform Foil/Holo/Poly); ui/RunScreen.kt:2126-2135 (op`
   - fix: Call a poll_edition-equivalent (4% total, tiered Foil/Holo/Poly/Negative) independently for every joker generated, in both rollShopItems and the Buffoon pack branch of openBooster.
63. **[MAJOR]** ❌ `shop &amp; economy` — Full voucher roster (32 vouchers)
   - vanilla: `game.lua:592-624 (v_overstock_norm..v_palette, 32 entries incl. v_hone, v_glow_up, v_observatory, v_tarot_tycoon, v_planet_tycoon, v_paint_brush, v_pa` · kotlin: `ui/RunScreen.kt:641-664 (VOUCHERS list, 22 entries) and ui/RunScreen.kt:2073-2106 (redeemVoucher when-block, same 22 key`
   - fix: Add the missing VoucherOffer entries and their apply_to_run equivalents (edition_rate variable feeding the drawJoker roll fixes #1 too; hand-size += for Paint Brush/Palette; tier-2 rate stacking for the Merchant/Tycoon a
64. **[MAJOR]** ⚠️ `shop &amp; economy` — Overstock / Overstock Plus voucher — content of the newly added shop slot *(verified)*
   - vanilla: `functions/common_events.lua:1097-1118 (change_shop_size calls create_card_for_shop for the new slot — same weighted joker/tarot/planet pool as any oth` · kotlin: `ui/RunScreen.kt:2081-2086 ("v_overstock_norm", "v_overstock_plus" branch fills the new slot(s) exclusively with `CATALOG`
   - fix: Route the Overstock bonus slot(s) through the same rollShopItems weighted-type logic instead of a Joker-only CATALOG pick.
65. **[MAJOR]** ⚠️ `shop &amp; economy` — Coupon Tag scope — voucher cost *(verified)*
   - vanilla: `tag.lua:447-465 (Coupon Tag couponizes only G.shop_jokers.cards and G.shop_booster.cards); card.lua:383 (the couponed=0-cost gate is scoped to `self.a` · kotlin: `ui/RunScreen.kt:1000-1003 (price(), used uniformly by buy/buyBooster/buyTarot/buyPlanet/buySpectral AND redeemVoucher at`
   - fix: Give redeemVoucher its own cost calc that applies discountPercent but not couponThisShop (mirroring vanilla's area-scoped couponed check).
66. **[MAJOR]** ⚠️ `shop &amp; economy` — Selling a Joker down to zero owned Jokers *(verified)*
   - vanilla: `card.lua:1640-1653 (Card:can_sell_card — gated on area/eternal only, no minimum-count check)` · kotlin: `ui/RunScreen.kt:1940-1941 (`if (owned.size <= 1) return`); confirmed intentional by app/src/test/kotlin/systems/balatro/`
   - fix: Remove the `owned.size <= 1` guard; make the UI (jokers row / joker CardArea) tolerate an empty owned list instead of gating sell().
67. **[MAJOR]** ❌ `shop &amp; economy` — Eternal / Perishable / Rental Joker stickers
   - vanilla: `functions/common_events.lua:2133-2147 (create_card polls eternal/perishable/rental for shop_jokers and pack_cards jokers when the corresponding G.GAME` · kotlin: `ui/RunScreen.kt:3846 — explicit comment "(eternal/perishable/rental jokers n/a)"; no eternal/perishable/rental fields an`
   - fix: Add eternal/perishable/rental fields to Owned/FJoker, wire the stake-gated roll into rollShopItems/openBooster's Buffoon branch, add can-sell / round-countdown-destroy / per-round-rent handling.
68. **[MAJOR]** ⚠️ `tarots/planets/spectrals/vouchers/tags/seals/enhancements/editions` — Blue Seal (Trance) creates a Planet card for the last-played hand type *(verified)*
   - vanilla: `card.lua:1033-1065 Card:get_end_of_round_effect (fires only for cards remaining in G.hand.cards at end of round, per functions/state_events.lua:171-18` · kotlin: `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:1559-1563 (`val bluePlanet = ...; if (bluePlanet != null) re`
   - fix: Move Blue Seal's Planet-creation into buildCashOut()/cashOut() using pendingHeld (already used correctly for Gold-enhancement cards at line 1688), not the per-hand scoring path.
69. **[MAJOR]** ❌ `tarots/planets/spectrals/vouchers/tags/seals/enhancements/editions` — Half of vanilla's 24 skip Tags are absent from the drawable pool: tag_uncommon, tag_rare, tag_negative, tag_foil, tag_holo, tag_polychrome, tag_voucher, tag_bos
   - vanilla: `game.lua:225-246 self.P_TAGS (24 entries); tag.lua:344-466 Tag:apply_to_run handles store_joker_create (Uncommon/Rare), store_joker_modify (Foil/Holo/` · kotlin: `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:130-144 (enum class Tag — only 12 entries: INVESTMENT, JUGGL`
   - fix: Add the 12 missing Tag entries with their TagTrigger mappings (store_joker_create→new SHOP_START-ish trigger giving a free Uncommon/Rare joker, store_joker_modify→apply an edition to a shop joker, voucher_add→extra vouch
70. **[MAJOR]** ❌ `tarots/planets/spectrals/vouchers/tags/seals/enhancements/editions` — 9 of 32 vouchers absent from the shop pool: v_hone, v_glow_up, v_hieroglyph, v_petroglyph, v_paint_brush, v_palette, v_tarot_tycoon, v_planet_tycoon, v_observat
   - vanilla: `game.lua:592-624 self.P_CENTERS (32 v_* voucher entries, 16 base + 16 tier-2 unlock chains)` · kotlin: `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:642-664 (23 VoucherOffer entries)`
   - fix: Add the 9 missing VoucherOffer entries with their game.lua config.extra values and requires/unlock_condition chains (currently unmodeled entirely — the rebuild has no voucher-unlock-prerequisite system at all, a related 
71. **[MAJOR]** ❌ `tarots/planets/spectrals/vouchers/tags/seals/enhancements/editions` — 3 of 18 spectrals absent: Ankh (copy a random Joker, dissolve the rest), Cryptid (duplicate a highlighted card N times), The Soul (spawn a Legendary Joker)
   - vanilla: `game.lua:581 c_ankh, :586 c_cryptid, :587 c_soul; effects in card.lua:1201-1221 (Cryptid), :1415-1425 (Soul, shared with Judgement), :1426-1453 (Ankh)` · kotlin: `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:150-165 (enum class Spectral — 15 entries, none named ANKH/C`
   - fix: Add ANKH/CRYPTID/SOUL to the Spectral enum and applySpectral(); Soul needs the same 'spawn a Joker card' machinery as the missing Judgement tarot (see next finding) — implement both together.
72. **[MAJOR]** ❌ `tarots/planets/spectrals/vouchers/tags/seals/enhancements/editions` — 2 of 22 tarots absent: Judgement (spawn a random Joker) and The Wheel of Fortune (chance to add a random Edition to a Joker)
   - vanilla: `game.lua:543 c_wheel_of_fortune (config.extra=4, odds = probabilities.normal/4), :553 c_judgement; effects card.lua:1415-1425 (Judgement/Soul), :1467-` · kotlin: `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:760-780 (val TAROTS = listOf(...) — 20 entries)`
   - fix: Add Judgement (TarotFx variant that spawns a random Joker, gated by joker-slot room like the missing Soul) and The Wheel of Fortune (probabilistic TarotFx.PollEdition on a random eligible Joker) to TAROTS.
73. **[MAJOR]** ⚠️ `tarots/planets/spectrals/vouchers/tags/seals/enhancements/editions` — Talisman/Deja Vu/Trance/Medium (seal-granting spectrals) and Aura (edition-granting spectral) apply to a RANDOM card instead of a player-selected highlighted ca *(verified)*
   - vanilla: `card.lua:1178-1191 (Talisman/Deja Vu/Trance/Medium act on `G.hand.highlighted[1]`), :1192-1200 (Aura acts on `G.hand.highlighted[1]`); card.lua:1523-1` · kotlin: `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:950-951 `Spectral.TALISMAN -> deck.sealRandom(Seal.GOLD)`, ``
   - fix: Extend the aimTarot/tarotTarget targeting mechanism (or an equivalent) to Spectral consumables that require a 1-card target, gated the same way as TarotFx.needsTarget.
74. **[MAJOR]** ⚠️ `tarots/planets/spectrals/vouchers/tags/seals/enhancements/editions` — Hex and Ectoplasm (edition-granting spectrals affecting Jokers) don't restrict to editionless jokers and Hex destroys eternal jokers *(verified)*
   - vanilla: `card.lua:1467-1470 both use `self.eligible_editionless_jokers` as the candidate pool (i.e. only jokers with no edition can be picked); card.lua:1488-1` · kotlin: `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:952-959 `Spectral.ECTOPLASM -> { val i = owned.indices.rando`
   - fix: Filter the candidate pool to jokers with Edition.NONE before Hex/Ectoplasm's random pick; Hex's destroy loop needs an eternal-sticker check once that sticker system exists (tracked separately, but this spectral's behavio

## Per-domain detail

### game flow & state machine
> Enumerated all 19 vanilla G.STATES and their per-state Game:update_* handlers (game.lua) plus the end_round/new_round/evaluate_play/evaluate_round transition sequence (state_events.lua), and traced each against Kotlin's Phase enum and RunState transition functions in RunScreen.kt. The core hand-played -> draw -> win/loss -> shop -> blind-select -> ante-advance loop is faithfully ported (including the ante-increments-only-after-Boss arithmetic via blindIndex/3), but several structural gaps were found in the meta-flow around it: the very first blind of a run skips Blind Select entirely, there is no Endless Mode continuation after winning ante 8, autosave fires far less often than vanilla's per-hand save_run(), and the vanilla loss-save hook (jokers like Mr. Bones intercepting a game-over) has no corresponding branch in the Kotlin state machine.

| st | sev | feature | vanilla | kotlin | evidence |
|---|---|---|---|---|---|
| ❌ | major | Winning the Boss Blind of the win ante (ante 8) offers an 'Endless' button (b_endless / exit_overlay_menu) that dismisses the win overlay an | `functions/UI_definitions.lua create_UIBox_win (~line 2785: `UIBox_button({button = 'exit_o` | `RunScreen.kt:1805-1807 (cashOut(): `if (wasSlot == 2 && wasAnte == 8) { phase = Phase.WIN;` | EndScreen's button row is `BButton("New Run", ...){ onRestart() }` and `BButton("Main Menu", ...){ onMainMenu() }` — no third path back into Phase.ROUND/SHOP. Phase.WIN is a dead end in the Kotlin state machine; vanilla' |
| ◑ | major | save_run() is called at the end of nearly every state's update handler (SELECTING_HAND completion i.e. after each hand/draw, BLIND_SELECT en | `game.lua:3060-3069 (update_selecting_hand: `save_run(); G.CONTROLLER:recall_cardarea_focus` | `RunScreen.kt:2519-2528 (`LaunchedEffect(s.phase, s.blindIndex, s.money, s.consumables.size` | The autosave effect only fires (writes) when phase is SHOP or BLIND_SELECT; there is no `else` branch for Phase.ROUND, so hands played, discards used, and cards drawn mid-round are never persisted. If the app process is  |
| ⚠️ | major | Fresh run always begins at G.STATES.BLIND_SELECT (start_run prep_stage default) — the player must explicitly select/confirm the Small Blind  | `game.lua:2018-2024 (Game:start_run: self:prep_stage(G.STAGES.RUN, saveTable and saveTable.` | `RunScreen.kt:1189-1220 (pickDeck: `deck.setComposition(...); startRound(); phase = Phase.R` | pickDeck() is the sole entry point from DeckSelectScreen and it directly calls startRound() then sets `phase = Phase.ROUND`; Phase.BLIND_SELECT is never assigned anywhere in that path. Every subsequent round correctly us |
| ❌ | minor | end_round()'s per-joker game-over evaluation: when chips < blind.chips and hands_left is exhausted, every joker is still given a chance via  | `functions/state_events.lua:99-122 (end_round(): `eval = G.jokers.cards[i]:calculate_joker(` | `RunScreen.kt:1652-1663 (scoreBank(): `if (roundScore >= target) { ...; phase = Phase.ROUND` | There is no loop over `owned` jokers between the target-check and the handsLeft<=0 branch that could intercept the loss the way vanilla's eval.saved does — the loss transition is unconditional once handsLeft reaches 0 an |
| ❌ | cosmetic | G.FUNCS.draw_from_deck_to_hand forces STATE = GAME_OVER when hand.config.card_limit <= 0 and the hand is empty (an unwinnable/undrawable saf | `functions/state_events.lua:355-360 (`if not (...) and G.hand.config.card_limit <= 0 and #G` | `RunScreen.kt:1277-1284 (startRound(): `handSize = maxOf(1, handSize)`), RunScreen.kt:1326-` | handSize is always clamped to a minimum of 1, so the empty-hand condition vanilla treats as an explicit loss transition can never occur in the Kotlin state machine — refill()/startRound() will always deal at least one ca |

### scoring pipeline (evaluate_play sequence: card order, retriggers, held-in-hand, joker context order, edition/seal timing)
> Verified the full evaluate_play cascade (state_events.lua:571-1086) phase-by-phase against Score.kt's score()/RunScreen's play()+scoreBank() split. The per-card scoring loop, individual/repetition/joker_main/other_joker context ordering, edition pre/post-joker timing, and held-in-hand additive-repetition logic are faithfully ported and well-documented; the gaps are concentrated in phases the oracle's static-input tests can't see: the vanilla context.before phase (which lets several common jokers grow ON the same hand that triggers them), The Flint's exact rounding/floor formula, the whole-hand debuff_hand outcome, and one deck back-effect hook.

| st | sev | feature | vanilla | kotlin | evidence |
|---|---|---|---|---|---|
| ⚠️ | blocker | context.before joker pass mutates ability state (Spare Trousers/Square Joker/Runner/Ride the Bus/Obelisk/Green Joker's per-hand growth, Spac | `state_events.lua:628-638 (the context.before pass, run before mult/hand_chips are re-fetch` | `game/Score.kt:48 (Sctx.before declared but never set true anywhere in score()); game/Joker` | Vanilla: play a Two Pair with a fresh Spare Trousers (ability.mult=0) — context.before adds +2 to ability.mult, then the SAME hand's joker_main reads ability.mult=2 and applies +2 Mult to itself. Kotlin: the HandScored r |
| ⚠️ | blocker | The Flint boss blind: halve base chips/mult using round-half-up with a minimum Mult floor of 1 (chips floor of 0). | `blind.lua:511-513: `return math.max(math.floor(mult*0.5 + 0.5), 1), math.max(math.floor(ha` | `game/Score.kt:485: `if (debuff is Debuff.Flint) { chips = floor(chips / 2); mult = floor(m` | HandType.HIGH_CARD has baseMult=1 (game/Hands.kt:10). Vanilla: floor(1*0.5+0.5)=floor(1.0)=1, clamped to max(1,1)=1 — High Card still scores its halved chips. Kotlin: floor(1/2)=0 — mult becomes 0, so chips*mult=0 for AN |
| ❌ | major | Plasma Deck's final_scoring_step hook (averages chips and mult) fires after the joker-main pass, before card destruction. | `functions/state_events.lua:946-948: `local nu_chip, nu_mult = G.GAME.selected_back:trigger` | `none — grep for "Plasma" across rebuild/app/src/main/kotlin/systems/balatro returns zero h` | Deck is a selectable enum in the rebuild (ui/RunScreen.kt / ui/ShopArt.kt reference deck identities), so a player can pick Plasma Deck, but its entire scoring identity (averaging chips/mult into equal halves every hand)  |
| ⚠️ | major | Boss blinds that disallow an entire hand type this round (The Eye: no repeats; The Mouth: only the first type played) — vanilla lets the han | `functions/state_events.lua:614 gates the whole scoring branch on `G.GAME.blind:debuff_hand` | `ui/RunScreen.kt:1373-1374: `if (boss == Boss.THE_EYE && handType0 in eyeUsedHands) return`` | In vanilla, a player forced to play their last hand of a disallowed type at The Eye/The Mouth loses the round (the play is spent, hand_chips*mult=0). In the rebuild the same situation simply refuses the play — the player |
| ⚠️ | cosmetic | Red Seal retrigger on a HELD (non-played) card is gated on the held card having produced some effect this pass (`next(effects[1]) or #effect | `functions/state_events.lua:813-814 (held-card loop): `if next(eval) and (next(effects[1]) ` | `game/Score.kt:594: `val redSealRep = if (card.seal == Seal.RED) 1 else 0` in the held-card` | Retriggering a held card that produced zero effect (e.g. a plain non-Steel card with Red Seal and no relevant board joker) is a no-op either way — chips/mult are unaffected — but the extra retrigger is nominal (visible v |

### Poker hand evaluation + levels (evaluate_poker_hand, get_X_same/get_flush/get_straight/get_highest, hands table chips/mult/l_chips/l_mult, level-up formula)

| st | sev | feature | vanilla | kotlin | evidence |
|---|---|---|---|---|---|
| ❌ | minor | Royal Flush display rename: when the played hand is a Straight Flush and its lowest card id >= 10 (i.e. 10-J-Q-K-A), the shown hand name bec | `functions/state_events.lua:557-566 (get_poker_hand_info: disp_text override on min id >= 1` | `none (ui/CardArt.kt handName has no Royal Flush branch; a Royal-Flush check exists only in` | Vanilla: `if text=='Straight Flush' then local min=10; for j... if scoring_hand[j]:get_id()<min then min=... end; if min>=10 then disp_text='Royal Flush' end end`. Kotlin's `handName(HandType)` is a pure enum-name transf |
| ❌ | minor | Secret hand visibility gating: Flush Five, Flush House, and Five of a Kind start with `visible=false` in the hands table and their row is no | `game.lua:2002-2004 (visible=false for Flush Five/Flush House/Five of a Kind vs visible=tru` | `ui/RunScreen.kt:4378-4383 (RUN_INFO_HANDS lists all 12 unconditionally), game/Hands.kt / g` | Kotlin's Run Info poker-hands table (`currentHandsTree`, RunScreen.kt:4385-4410) iterates the fixed `RUN_INFO_HANDS` list with no visibility check, so Flush Five/Flush House/Five of a Kind rows (and their base chip/mult  |
| ✅ | cosmetic | evaluate_poker_hand overall structure: results table built in fixed priority order (Flush Five → Flush House → 5oak → Straight Flush → 4oak  | `functions/misc_functions.lua:376-520` | `game/Hands.kt:44-156 (see also 145-152 downgrade chain, 150-152)` | Vanilla: `if next(parts._5) and next(parts._flush) then results["Flush Five"]=parts._5; if not results.top then results.top=... end`, same pattern repeated in the same order through High Card, then lines 507-517 rebuild  |
| ✅ | cosmetic | get_X_same(num, hand): groups of EXACTLY `num` cards sharing an id, returned high-id-first. | `functions/misc_functions.lua:592-611` | `game/Hands.kt:159-170 (getXSame)` | Vanilla builds `vals[id]=curr` only `if #curr==num`, then walks `vals` from 14 down to 1. Kotlin's `getXSame` does the identical id-bucket + exact-size check + reverse (14→1) emission. |
| ✅ | cosmetic | get_flush(hand): one suit covering >= (5 - Four Fingers?1:0) cards; bails if hand.size>5 or <need. | `functions/misc_functions.lua:522-546` | `game/Hands.kt:172-181 (getFlush)` | Vanilla: `if #hand>5 or #hand<(5-(four_fingers and 1 or 0)) then return ret` then scans suits, returns first suit with flush_count>=need. Kotlin: identical `need = 5-(fourFingers?1:0)` guard and first-matching-suit retur |
| ✅ | cosmetic | get_straight(hand): consecutive run of ranks (Ace counted both high id=14 and as the j==1 wrap for low straights), Shortcut allows skipping  | `functions/misc_functions.lua:548-590` | `game/Hands.kt:184-199 (getStraight)` | Vanilla loops j=1..14 using `IDS[j==1 and 14 or j]`, tracks straight_length/skipped_rank/can_skip (Shortcut). Kotlin's loop `for (j in 1..14) { val key = if (j==1) 14 else j; ... }` with `len/skipped/shortcut` mirrors th |
| ✅ | cosmetic | get_highest(hand): the single card with the greatest get_nominal(), no size restriction. | `functions/misc_functions.lua:613-621` | `game/Hands.kt:205-208 (getHighest)` | Vanilla iterates all cards tracking `highest`. Kotlin: `hand.maxByOrNull { it.nominal } ?: return emptyList()`; both never apply a >5/< need bound (unlike flush/straight). |
| ✅ | cosmetic | Two Pair / Full House 5-card containment quirk: when exactly one triple and one separate pair exist (i.e. the Full House pattern), the Two P | `functions/misc_functions.lua:479-495 (esp. 484-492) and 450-462 (Full House sets top first` | `game/Hands.kt:120 (Full House `set()`), 124-141 (Two Pair `set()`)` | Vanilla: `if not fh_2b then fh_2b = parts._3[1] end` then concatenates all of fh_2a + all of fh_2b (3 cards) into the Two Pair entry. Kotlin: `val b = if (_2.size >= 2) _2[1] else _3[0]` then `set(HandType.TWO_PAIR, a +  |
| ✅ | cosmetic | Hand base values table: chips/mult/l_chips/l_mult for all 12 standard poker hands (Flush Five through High Card). | `game.lua:2002-2013 (G.GAME.hands init)` | `game/Hands.kt:8-21 (HandType enum)` | Vanilla e.g. Straight: chips=30,mult=4,l_chips=30,l_mult=3; Full House: chips=40,mult=4,l_chips=25,l_mult=2. Kotlin: `STRAIGHT(30, 4, 30, 3)`, `FULL_HOUSE(40, 4, 25, 2)` — all 12 entries checked and match exactly (verifi |
| ✅ | cosmetic | Level-up formula: at level L, mult = max(s_mult + l_mult*(L-1), 1), chips = max(s_chips + l_chips*(L-1), 0). | `functions/common_events.lua:466-468 (level_up_hand)` | `game/Score.kt:483-484` | Vanilla: `G.GAME.hands[hand].mult = math.max(s_mult + l_mult*(level-1), 1)`. Kotlin: `mult = (handType.baseMult + (level - 1) * handType.lMult).toDouble()` — same arithmetic (min-clamp is unreachable in Kotlin since leve |
| ✅ | cosmetic | Scoring-hand card selection: a played card scores if it's in the detected hand OR is a Stone card (always scores) OR Splash joker is present | `functions/state_events.lua:580-599 (pure-bonus/Stone cards + Splash added to scoring_hand)` | `game/Score.kt:478-480` | Vanilla: `if next(find_joker('Splash')) then scoring_hand[i]=G.play.cards[i] else if ability.effect=='Stone Card' then ...insert into pures... end`. Kotlin: `val scoringHand = played.filter { splash \|\| it in handCards  |
| ⚠️ | cosmetic | Hand-type display text capitalization: vanilla poker-hand names use lowercase 'of'/'a' in multi-word hands ('Three of a Kind', 'Four of a Ki | `game.lua:2010-2011 (table keys "Three of a Kind"/"Four of a Kind"/"Five of a Kind"), funct` | `ui/CardArt.kt:58-60 (handName)` | Kotlin: `h.name.lowercase().split('_').joinToString(" ") { it.replaceFirstChar { c -> c.uppercase() } }` title-cases EVERY word, so THREE_OF_A_KIND → "Three Of A Kind" and FOUR_OF_A_KIND → "Four Of A Kind" / FIVE_OF_A_KI |

### Blinds & boss effects (blind.lua hooks + P_BLINDS roster) vs Kotlin RunScreen.kt/Blinds.kt
> All 28 vanilla boss blinds (23 regular + 5 showdown) are represented in the Kotlin `Boss` enum with correct min-ante gating and reward values, and several bosses (THE_ARM, THE_WALL, THE_WATER, THE_NEEDLE, THE_PLANT, VERDANT_LEAF, VIOLET_VESSEL, AMBER_ACORN, boss-selection RNG) are faithfully ported. However, 9 bosses have concretely wrong mechanics or timing (THE_MANACLE, THE_HOOK, THE_SERPENT, THE_FLINT, THE_OX, THE_TOOTH, THE_FISH, CRIMSON_HEART, plus the shared THE_EYE/THE_MOUTH/THE_PSYCHIC debuff_hand gating pattern), and 4 more have narrow edge-case gaps (THE_MARK/Pareidolia, suit-debuff Stone/Wild/Smeared exemptions, THE_PILLAR duplicate-card matching, CERULEAN_BELL re-forcing).

| st | sev | feature | vanilla | kotlin | evidence |
|---|---|---|---|---|---|
| ⚠️ | major | THE_MANACLE boss debuff | `blind.lua:187-189 (set_blind: `if self.name == 'The Manacle' and not reset then G.hand:cha` | `game/Blinds.kt:54,62,100-104 (`THE_MANACLE -> default + 1` hands, comment "+1 Hand, -1 Jok` | Vanilla Manacle reduces the player's HAND SIZE (max cards held) by 1 via `G.hand:change_size(-1)` — it never touches hand-plays-per-round or joker slots. Kotlin instead grants an EXTRA hand play (`hands(default)=default+ |
| ⚠️ | major | THE_HOOK boss trigger and cost | `blind.lua:464-487 (press_play: fires on every PLAY, randomly picks 2 cards from the REMAIN` | `ui/RunScreen.kt:1818-1826 (`fun discard() { ... if (boss==THE_HOOK) { val hookIndices = ha` | Kotlin wires Hook into the manual `discard()` action: every time the player presses Discard under Hook, their chosen cards are silently overridden with 2 random ones AND a discard charge is consumed. Vanilla's Hook fires |
| ⚠️ | major | THE_SERPENT boss effect | `functions/state_events.lua:355-374 (`G.FUNCS.draw_from_deck_to_hand`: once `hands_played>0` | `ui/RunScreen.kt:1642 (`if (boss == Boss.THE_SERPENT) { hand = deck.draw(hand.size); faceDo` | Vanilla Serpent's signature challenge is hand-size attrition: after your first action, refills cap at 3 new cards regardless of how many you played/discarded, so your hand keeps getting smaller each hand. Kotlin instead  |
| ⚠️ | major | THE_FLINT modify_hand halving formula | `blind.lua:510-517 (`return math.max(math.floor(mult*0.5 + 0.5), 1), math.max(math.floor(ha` | `game/Score.kt:485 (`if (debuff is Debuff.Flint) { chips = floor(chips / 2); mult = floor(m` | Kotlin uses plain floor-division with no +0.5 and no minimum clamp. Worked example: High Card under Flint (chips=5, mult=1) — vanilla: floor(5*0.5+0.5)=3 chips, max(floor(1*0.5+0.5),1)=1 mult → score 3. Kotlin: floor(5/2 |
| ⚠️ | major | THE_EYE / THE_MOUTH / THE_PSYCHIC debuff_hand consequence | `blind.lua:519-548 (debuff_hand returns triggered=true for a repeat hand-type/locked-type/w` | `ui/RunScreen.kt:1372-1374 (`if (boss==THE_PSYCHIC && sel.size!=5) return`; `if (boss==THE_` | Kotlin hard-blocks the Play button before submission (`return` from `play()` with no side effects) for all three bosses. Vanilla instead lets the player SUBMIT the invalid hand — it consumes a hand attempt and shows the  |
| ⚠️ | major | THE_OX "most played hand" snapshot timing | `functions/state_events.lua:127-136 (`current_round.most_played_poker_hand` is recomputed O` | `ui/RunScreen.kt:846 (`val mostPlayedHand: Pair<HandType,Int>? get() = _handPlayed.entries.` | Kotlin recomputes `mostPlayedHand` live from `_handPlayed`, and the increment for the CURRENT hand (line 1511) happens BEFORE the Ox check reads it (line 1553) — so the hand just played can push itself into (or out of) b |
| ⚠️ | major | THE_TOOTH money loss floor | `blind.lua:497-507 (press_play: `for i=1,#G.play.cards do ease_dollars(-1) ... end`); funct` | `ui/RunScreen.kt:1549 (`if (boss == Boss.THE_TOOTH) money = maxOf(0, money - pendingSel.siz` | This is the ONLY money-mutation site in RunScreen.kt that clamps at 0 (every other `money -=`, e.g. lines 1729/1878/2013/2366/2403, is unclamped). Vanilla dollars can genuinely go negative from Tooth; the Kotlin rebuild  |
| ⚠️ | major | THE_FISH face-down cards on the opening hand | `blind.lua:176-178 (set_blind: `if self.name=='The Fish' and not reset then self.prepped=ni` | `ui/RunScreen.kt:1267 (`Boss.THE_FISH -> newIndices`, unconditional) combined with :1322 (`` | Kotlin's `applyFaceDown` when-block for THE_FISH always returns all `newIndices` with no first-hand exemption (unlike THE_HOUSE, which the caller special-cases), so `startRound()`'s initial `applyFaceDown(hand.indices.to |
| ⚠️ | major | CRIMSON_HEART joker-disable timing (first hand) | `blind.lua:94 (set_blind: `self.prepped = true` — default true for all blinds except Fish);` | `ui/RunScreen.kt:1308 (`crimsonHeartDisabled = null` at round start); :1645-1649 (only assi` | Because `self.prepped` defaults true for Crimson Heart, vanilla disables a random joker BEFORE the player even sees/plays their first hand of the round. Kotlin leaves `crimsonHeartDisabled` null through the entire first  |
| ◑ | minor | THE_PILLAR debuff matching (duplicate cards) | `blind.lua:634-637 (`if self.name=='The Pillar' and card.ability.played_this_ante then card` | `game/Blinds.kt:13-15 (`data class DebuffCards(val cards: Set<PlayingCard>)` matched by str` | Because `PlayingCard` is a structurally-equal data class, two genuinely-identical cards (e.g. two Marble-Joker-spawned Stone cards, or any duplicate suit+rank+enhancement+seal combo) are indistinguishable in the `Set<Pla |
| ⚠️ | minor | THE_MARK face-down draw + Pareidolia interaction | `blind.lua:614-616 (`if self.name=='The Mark' and card:is_face(true) then return true end`)` | `ui/RunScreen.kt:1257 (`Boss.THE_MARK -> newIndices.filter { hand.getOrNull(it)?.isFace == ` | THE_MARK's face-down check omits the `pareidolia` OR that the same file applies correctly elsewhere for other J/Q/K interactions — under Pareidolia, vanilla's Mark boss deals every card face-down; Kotlin only face-downs  |
| ⚠️ | minor | Suit-debuff bosses (THE_CLUB/THE_GOAD/THE_WINDOW/THE_HEAD) enhancement exemptions | `blind.lua:626 (`self.debuff.suit and card:is_suit(self.debuff.suit, true)`); card.lua:4076` | `game/Score.kt:539 (`if (debuff is Debuff.DebuffSuit && card.suit == debuff.suit) continue`` | Kotlin's suit match is a bare field comparison with no Stone/Wild/Smeared handling. A Stone card (e.g. from Marble Joker, `PlayingCard(Suit.S, ...)`) would be incorrectly debuffed by THE_GOAD in Kotlin though vanilla exe |
| ⚠️ | minor | CERULEAN_BELL forced-card persistence | `blind.lua:574-587 (drawn_to_hand: `if not any_forced then ... force a NEW card end` — only` | `ui/RunScreen.kt:1320 (round start) and :1651 (`if (boss == Boss.CERULEAN_BELL) bellForcedI` | Kotlin re-randomizes `bellForcedIdx` after every refill regardless of whether the previously-forced card is still present and unplayed, whereas vanilla keeps forcing the same physical card until it's actually played away **[corrected on verify: Claim is refuted. The auditor read blind.lua:574-587 (`if not any_forced then ... end`) in isolation and inferred that a previously-forced, unplayed C]** |
| ✅ | cosmetic | THE_ARM hand-level degradation | `blind.lua:550-559 (`if G.GAME.hands[handname].level > 1 then ... level_up_hand(...,-1) end` | `game/Levels.kt:15 (`fun degrade(h) { lvl[h] = maxOf(1, level(h)-1) }`); ui/RunScreen.kt:15` | Kotlin's floor-at-1 matches vanilla's implicit floor from the `level>1` gate. |
| ✅ | cosmetic | THE_WALL / THE_WATER / THE_NEEDLE structural effects | `game.lua:273 (bl_wall mult=4), blind.lua:179-182 (Water sets discards to 0), blind.lua:183` | `game/Blinds.kt:95 (`THE_WALL -> 4.0` targetMult), :106 (`discards(default) = if THE_WATER ` | All three structural multipliers/caps match vanilla's numbers exactly. |
| ✅ | cosmetic | THE_PLANT face-card debuff (incl. Pareidolia) | `blind.lua:630-633 (`self.debuff.is_face=='face' and card:is_face(true)`); card.lua:967 (id` | `game/Score.kt:540 (`if (debuff is Debuff.DebuffFace && (card.isFace \|\| pareidolia)) cont` | Unlike THE_MARK, THE_PLANT's own debuff site correctly ORs in the Pareidolia flag. |
| ✅ | cosmetic | VERDANT_LEAF (all cards debuffed) / VIOLET_VESSEL (x6 target) / AMBER_ACORN (joker shuffle) | `blind.lua:650 (Verdant Leaf debuffs every non-joker card); game.lua:288 (bl_final_vessel m` | `game/Score.kt:542 (`DebuffAllCards -> continue`); game/Blinds.kt:96 (`VIOLET_VESSEL -> 6.0` | All three showdown finishers' core numeric/structural effects are correctly ported (the visual joker-flip/3-pass-shuffle animation is cosmetic-only and not required for fidelity). |
| ✅ | cosmetic | Boss selection RNG (get_new_boss / bosses_used cycling) | `functions/common_events.lua:2338-2383 (showdown antes draw only showdown bosses; regular a` | `game/Blinds.kt:111-129 (`Boss.next`: showdownAnte gate, minAnte filter, min-use pool selec` | The eligibility and "cycle the full pool before repeating" logic is a faithful structural port (vanilla's challenge-only `banned_keys` filter is not ported, but that's an out-of-scope challenge-deck feature). |
| ✅ | cosmetic | Boss min-ante gating (all 23 regular bosses) | `game.lua:266-293 (`boss = {min = N, max = 10}` per boss)` | `game/Blinds.kt:44-69 (`minAnte = N` per boss)` | Cross-checked all 23 regular bosses' min values 1:1 against Kotlin's minAnte fields; all match exactly (e.g. THE_OX min=6, THE_PLANT min=4, THE_SERPENT min=5, THE_EYE/THE_TOOTH min=3, remaining min=1 or 2). |
| ✅ | cosmetic | THE_HOUSE first-hand-only face-down (with correct refill exemption) | `blind.lua:611-613 (`hands_played==0 and discards_used==0` — only the very first draw quali` | `ui/RunScreen.kt:1255 (`Boss.THE_HOUSE -> newIndices`) gated by the caller: :1334 (`if (bos` | Unlike the THE_FISH bug above, THE_HOUSE correctly restricts its face-down effect to the opening deal only. |

### The 150 vanilla Balatro jokers — config fidelity and scoring-trigger correctness in the Kotlin rebuild's Score.kt/JokerManifest.kt engine (cross-checked against RunScreen.kt where a joker's mechanic is legitimately non-scoring, e.g. economy/shop hooks)
> Enumerated all 150 vanilla P_CENTERS joker keys from game.lua and grepped every one against JokerManifest.kt, Score.kt, and RunScreen.kt (not just the two nominal files, to rule out legitimate non-scoring implementation sites); cross-referenced calculate_joker/calculate_dollar_bonus in card.lua and the evaluate_play pass ordering in state_events.lua for the sampled every-10th, scaling, economy, and copy-joker jokers. Coverage of the sample is deep (source-line evidence on both sides for every reported item) but the roster-wide sweep is breadth-only (existence check, not per-config verification) for the ~113 jokers not in the explicit sample.

| st | sev | feature | vanilla | kotlin | evidence |
|---|---|---|---|---|---|
| ❌ | blocker | Roster completeness — every vanilla joker key must exist somewhere in the engine (scoring, shop offer, or economy hook) | `game.lua P_CENTERS (150 j_* entries, lines 368-526)` | `none (grepped the entire rebuild/app/src/main/kotlin tree)` | A full-tree grep of every one of the 150 vanilla joker keys found ZERO occurrences (no JokerManifest entry, no Score.kt branch, no RunScreen Offer, no other mechanic file) for 36 keys: j_8_ball, j_astronomer, j_bloodston |
| ⚠️ | blocker | context.before accumulator timing — Green Joker (+1 Mult/hand), Spare Trousers (+2 Mult/Two-Pair-or-Full-House), Runner (+15 Chips/Straight) | `functions/state_events.lua:574 (hands[text].played++ happens before the context.before loo` | `JokerManifest.kt:194-234,860-870 (green_joker/j_spare_trousers/j_runner/j_square/j_obelisk` | Score.kt's `for (j in jokers) JOKER_MANIFEST[j.key]?.reduce?.let { j.restore(it(j.snapshot(), GameEvent.BeforeHand(ctx))) }` (line 521) runs inside score() before any card is scored — this is the correct hook point and I |
| ⚠️ | blocker | Constellation — X(1 + 0.1×planets_used) Mult, grown each time a Planet consumable is used | `card.lua:2727-2732 (context.consumeable, ability.set=='Planet' → self.ability.x_mult += se` | `RunScreen.kt:931-934 (the x accumulator IS grown: `for (o in owned) if (o.fj.key == "j_con` | The joker's state is faithfully accumulated on every planet use, but calcJoker() (Score.kt:144) never dispatches for "j_constellation" — JOKER_MANIFEST[j.key] lookup returns null and no legacy when-block matches the key  |
| ❌ | major | Wee Joker — +8 Chips permanently accumulated each time a scored card has rank 2, read every hand by joker_main | `card.lua:3083-3092 (context.individual, `context.other_card:get_id() == 2` → self.ability.` | `Score.kt:313-314` | Score.kt's own comment marks it explicitly: `"j_wee" -> if (j.chips != 0.0) return Fx().apply { chipMod = j.chips } // accumulated +Chips (j_wee: unimplemented placeholder)`. j.chips is never written anywhere (no individ |
| ❌ | major | Ride the Bus — +1 Mult (accumulated) each hand with no face card scored; resets to 0 the instant a face card scores | `card.lua:3525-3541 (context.before) read by joker_main at card.lua:3992-3997` | `none (0 hits for "j_ride_the_bus" anywhere in the rebuild tree)` | Confirmed absent from JokerManifest.kt, Score.kt, and RunScreen.kt (no shop Offer entry either) — the joker cannot even be acquired in the rebuild. |
| ❌ | major | Madness — on selecting a non-boss Blind, +0.5 Xmult (accumulated, read every joker_main) AND destroys one random other non-eternal Joker | `card.lua:2503-2520 (context.select_blind, not context.blind.boss) + joker_main read (card.` | `none (0 hits anywhere in the rebuild tree)` | No blind-selection hook, no accumulator, no joker_main entry, no shop Offer — entirely absent. This is a mechanically unique joker (the only vanilla joker that destroys a board joker as its OWN growth cost) so it can't b |
| ❌ | major | Bloodstone — per scored Heart, 1-in-2 pseudorandom chance of X1.5 Mult | `card.lua:3247-3253 (context.individual, is_suit('Hearts') && pseudorandom('bloodstone') < ` | `none (0 hits anywhere in the rebuild tree)` | Same pseudorandom-scoring shape as j_lucky_cat/m_lucky (already correctly ported with a seeded Random(luckySeed+...) pattern in Score.kt:552-559), but Bloodstone has no individual hook at all — full-tree grep for "j_bloo |
| ❌ | major | Fortune Teller — joker_main +Mult = total Tarot cards used this run (unconditional, reads a running consumable-usage counter) | `card.lua:4016-4021 (`self.ability.name == 'Fortune Teller' and G.GAME.consumeable_usage_to` | `none (0 hits anywhere in the rebuild tree)` | Full-tree grep for "j_fortune_teller" is empty — no shop Offer, no jokerMain, no counter of tarots-used tracked for this purpose. |
| ❌ | major | Satellite — end-of-round $ payout = extra × (distinct Planet types used this run), via calculate_dollar_bonus | `card.lua:1667-1676 (Card:calculate_dollar_bonus, counts distinct planet sets in G.GAME.con` | `none — absent from RunScreen.kt's buildCashOut() jokerDollars list (RunScreen.kt:1693-1711` | buildCashOut()'s jokerDollars accumulator explicitly ports Golden Joker/Cloud 9/To the Moon/Delayed Gratification/Rocket from card.lua's calculate_dollar_bonus (same Lua function Satellite lives in, card.lua:1655-1676) b |
| ❌ | major | Invisible Joker — after 2 rounds unused, can be sold to duplicate a random other Joker | `card.lua:308 (invis_rounds init), 2371 (round counter check ≥ extra(2)), 2934/4439 (sell-t` | `none (0 hits anywhere in the rebuild tree, including no shop Offer entry)` | Full-tree grep for "j_invisible" and "invis_rounds" both return nothing — the joker is not purchasable at all. |
| ◑ | cosmetic | Spare Trousers internal key renamed from vanilla's `j_trousers` to `j_spare_trousers` throughout the rebuild (JokerManifest, Score.kt commen | `game.lua:471 (`j_trousers= {..., name = "Spare Trousers", ...}`)` | `JokerManifest.kt:222, RunScreen.kt:551, JokerArt.kt:141, Oracle.kt:477` | The literal vanilla P_CENTERS key `j_trousers` never appears anywhere in the rebuild (confirmed by grep); the rebuild instead uses `j_spare_trousers` consistently across manifest, shop offer, sprite atlas lookup, and its |

### tarots/planets/spectrals/vouchers/tags/seals/enhancements/editions
> Planets (12/12), seals (4/4 + none), enhancements (8/8 vanilla values) and editions (5/5) are fully and faithfully ported with matching config values (mod_conv keys, max_highlighted targeting counts, dollar/mult constants all cross-checked against game.lua). Tarots (20/22), spectrals (15/18), vouchers (23/32) and tags (12/24) are meaningfully incomplete, and several implemented spectral/seal mechanics diverge from vanilla's actual trigger semantics (random targeting instead of player selection, wrong trigger timing for Blue Seal, missing eligibility filters for Hex/Ectoplasm).

| st | sev | feature | vanilla | kotlin | evidence |
|---|---|---|---|---|---|
| ❌ | major | Half of vanilla's 24 skip Tags are absent from the drawable pool: tag_uncommon, tag_rare, tag_negative, tag_foil, tag_holo, tag_polychrome,  | `game.lua:225-246 self.P_TAGS (24 entries); tag.lua:344-466 Tag:apply_to_run handles store_` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:130-144 (enum class Tag — only` | grep across ui/RunScreen.kt for tag_uncommon/tag_rare/tag_negative/tag_foil/tag_holo/tag_polychrome/tag_voucher/tag_boss/tag_garbage/tag_double/tag_top_up/tag_skip returns zero hits (only a comment referencing j_diet_col |
| ❌ | major | 9 of 32 vouchers absent from the shop pool: v_hone, v_glow_up, v_hieroglyph, v_petroglyph, v_paint_brush, v_palette, v_tarot_tycoon, v_plane | `game.lua:592-624 self.P_CENTERS (32 v_* voucher entries, 16 base + 16 tier-2 unlock chains` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:642-664 (23 VoucherOffer entri` | Counting the VoucherOffer(...) list at RunScreen.kt:642-664 yields 23 unique vouchers; grep for v_hone/v_glow_up/v_hieroglyph/v_petroglyph/v_paint_brush/v_palette/v_tarot_tycoon/v_planet_tycoon/v_observatory across the w |
| ❌ | major | 3 of 18 spectrals absent: Ankh (copy a random Joker, dissolve the rest), Cryptid (duplicate a highlighted card N times), The Soul (spawn a L | `game.lua:581 c_ankh, :586 c_cryptid, :587 c_soul; effects in card.lua:1201-1221 (Cryptid),` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:150-165 (enum class Spectral —` | grep -rn "Ankh\\|\"The Soul\"\\|Soul(" over the whole rebuild returns nothing; the Spectral enum tops out at AURA. The Soul is one of vanilla's two paths (with Judgement) to spawn a Legendary Joker outside specific unloc |
| ❌ | major | 2 of 22 tarots absent: Judgement (spawn a random Joker) and The Wheel of Fortune (chance to add a random Edition to a Joker) | `game.lua:543 c_wheel_of_fortune (config.extra=4, odds = probabilities.normal/4), :553 c_ju` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:760-780 (val TAROTS = listOf(.` | Counting TarotOffer(...) entries at lines 761-780 yields 20; grep for "Judgement" and "Wheel of Fortune" (excluding the unrelated j_cry_wheelhope joker's flavor text at line 406, which references a trigger event that can |
| ⚠️ | major | Blue Seal (Trance) creates a Planet card for the last-played hand type | `card.lua:1033-1065 Card:get_end_of_round_effect (fires only for cards remaining in G.hand.` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:1559-1563 (`val bluePlanet = .` | Vanilla explicitly separates 'end of round' (held-card) effects from 'context.cardarea==G.play' (scored-card) effects; Blue Seal is wired to the former (state_events.lua iterates G.hand.cards, which by round-end excludes |
| ⚠️ | major | Talisman/Deja Vu/Trance/Medium (seal-granting spectrals) and Aura (edition-granting spectral) apply to a RANDOM card instead of a player-sel | `card.lua:1178-1191 (Talisman/Deja Vu/Trance/Medium act on `G.hand.highlighted[1]`), :1192-` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:950-951 `Spectral.TALISMAN -> ` | Tarots with targeting (Enhance/ConvertSuit/RankUp/Destroy/ConvertCopy) correctly go through aimTarot()/useTarot() and a hand-card-index selection UI (lines 2187-2228), but Spectral consumables have no equivalent targetin |
| ⚠️ | major | Hex and Ectoplasm (edition-granting spectrals affecting Jokers) don't restrict to editionless jokers and Hex destroys eternal jokers | `card.lua:1467-1470 both use `self.eligible_editionless_jokers` as the candidate pool (i.e.` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:952-959 `Spectral.ECTOPLASM ->` | Both branches pick from `owned.indices.random()` (any joker, editioned or not) with no editionless filter, and Hex's `owned.clear(); owned.add(keep)` unconditionally destroys every other joker — the rebuild has no Eterna |
| ⚠️ | minor | Wraith (create a random Joker, set money to $0) has no Joker-slot-room gate | `card.lua:1531-1533 Card:can_use_consumeable — Wraith is grouped with Judgement/The Soul an` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:960 `Spectral.WRAITH -> { val ` | useConsumable()/applySpectral() has no `owned.size < maxJokers` (or equivalent) check before adding the Wraith joker, and no UI gate disables the 'Use' button for a held Wraith when Joker slots are full (the DetailToolti |
| ⚠️ | minor | Familiar/Grim/Incantation's random-enhancement card pool omits Lucky | `card.lua:1330-1335 — cen_pool excludes only `m_stone`; all other Enhanced centers (includi` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:991-996 `createEnhancedCards`:` | Vanilla's pool has 7 eligible enhancements (Bonus/Mult/Wild/Glass/Steel/Gold/Lucky); Kotlin's `enh` list has 6, omitting LUCKY, which skews the RNG distribution and removes Lucky as a possible outcome of Familiar/Grim/In |

### shop &amp; economy

| st | sev | feature | vanilla | kotlin | evidence |
|---|---|---|---|---|---|
| ❌ | major | Full voucher roster (32 vouchers) | `game.lua:592-624 (v_overstock_norm..v_palette, 32 entries incl. v_hone, v_glow_up, v_obser` | `ui/RunScreen.kt:641-664 (VOUCHERS list, 22 entries) and ui/RunScreen.kt:2073-2106 (redeemV` | Kotlin's VOUCHERS list and redeemVoucher() handle only 22 of vanilla's 32 vouchers. Missing: Hone/Glow Up (edition_rate x2/x4 in shop), Observatory (tier-2 Telescope), Tarot Tycoon/Planet Tycoon (tier-2 rate boosts), Pai |
| ❌ | major | Eternal / Perishable / Rental Joker stickers | `functions/common_events.lua:2133-2147 (create_card polls eternal/perishable/rental for sho` | `ui/RunScreen.kt:3846 — explicit comment "(eternal/perishable/rental jokers n/a)"; no etern` | Vanilla's higher-stake runs (and some run modifiers) roll Eternal (unsellable, indestructible), Perishable (self-destructs after N rounds, debuffed near expiry), and Rental (costs $ per round, cheap) stickers on shop/pac |
| ⚠️ | major | Joker edition odds/distribution in the shop and packs | `functions/common_events.lua:2055-2080 (poll_edition), functions/common_events.lua:2133-215` | `ui/RunScreen.kt:730-739 (drawJoker: 60% chance, first joker only, uniform Foil/Holo/Poly);` | Vanilla: poll_edition gives a joker an edition on roughly 4% of ALL joker-card creations (edition_rate=1, top 0.04*edition_rate of the roll), weighted Foil(~2%)>Holo(~1.4%)>Polychrome(~0.3%)>Negative(~0.3%), applied unif |
| ⚠️ | major | Overstock / Overstock Plus voucher — content of the newly added shop slot | `functions/common_events.lua:1097-1118 (change_shop_size calls create_card_for_shop for the` | `ui/RunScreen.kt:2081-2086 ("v_overstock_norm", "v_overstock_plus" branch fills the new slo` | Vanilla's change_shop_size(1) fills the newly opened slot via create_card_for_shop, the SAME weighted poll (joker_rate/tarot_rate/planet_rate) used for every other shop slot — so an Overstock slot can be a Tarot or Plane |
| ⚠️ | major | Coupon Tag scope — voucher cost | `tag.lua:447-465 (Coupon Tag couponizes only G.shop_jokers.cards and G.shop_booster.cards);` | `ui/RunScreen.kt:1000-1003 (price(), used uniformly by buy/buyBooster/buyTarot/buyPlanet/bu` | Vanilla explicitly never sets `ability.couponed` on the shop voucher card — vouchers still cost money during a Coupon Tag shop. Kotlin's single shared `price()` helper applies `couponThisShop -> 100% off` to every purcha |
| ⚠️ | major | Selling a Joker down to zero owned Jokers | `card.lua:1640-1653 (Card:can_sell_card — gated on area/eternal only, no minimum-count chec` | `ui/RunScreen.kt:1940-1941 (`if (owned.size <= 1) return`); confirmed intentional by app/sr` | Vanilla lets a player sell every Joker they own, ending with zero on the board (a legitimate strategy for cash, e.g. before a Boss that punishes Jokers). Kotlin's sell() silently refuses to sell the last remaining Joker; |
| ❌ | minor | Chaos the Clown / numeric free-reroll pool | `functions/state_events.lua:311-313 (`G.GAME.current_round.free_rerolls = #chaos`, set from` | `ui/RunScreen.kt:2354-2390 (rerollCost/reroll() — only `freeRerollThisShop: Boolean`, no nu` | Vanilla's free-reroll mechanic is a numeric per-round counter (one stack per owned Chaos the Clown Joker) that both zeroes cost AND suppresses the cost escalation for each consumed free reroll. Kotlin only has the D6-Tag |
| ⚠️ | minor | Discounted shop price rounding | `card.lua:375 — `self.cost = math.max(1, math.floor((self.base_cost + self.extra_cost + 0.5` | `ui/RunScreen.kt:1000-1003 (`fun price(base: Int) = maxOf(1, base * (100 - d) / 100)`)` | Vanilla adds 0.5 to the base cost before applying the discount fraction and floors the result; Kotlin multiplies and integer-divides the raw base cost with no +0.5 bias. For odd base costs under a discount the results di |
| ⚠️ | minor | Antimatter voucher — timing of the +1 Joker slot | `card.lua:1950-1956 (`G.jokers.config.card_limit = G.jokers.config.card_limit + 1`, queued ` | `ui/RunScreen.kt:2095 (`"v_antimatter" -> deckJokerBonus += v.extra`) and ui/RunScreen.kt:1` | Vanilla applies Antimatter's +1 Joker slot immediately, so a player can redeem Antimatter and buy an extra Joker in the same shop visit. Kotlin only recomputes `maxJokers` in `startRound()`, so `deckJokerBonus` from a sa |

### board physics & card containers (CardArea/Moveable/Card — hand, jokers, consumeables, play, deck, discard)

| st | sev | feature | vanilla | kotlin | evidence |
|---|---|---|---|---|---|
| ❌ | blocker | Discard pile has no visual container at all — vanilla flies discarded cards into G.discard, which scatters them via a dedicated align branch | `cardarea.lua:425-435 (align_cards discard branch)` | `engine/EngineHost.kt:29 (`val discard = areaMoveable(Room.discard)` — a plain Moveable, no` | `grep -n "host.discard" ui/RunScreen.kt` returns zero hits — the discard CardArea exists in EngineHost but is never fed cards, never has align_cards run, and is never drawn. Discarded cards (RunScreen.kt ~1819-1869 `fun  |
| ❌ | major | Hand card selection has no cap — vanilla limits G.hand.highlighted to config.highlighted_limit (default 5): once 5 cards are highlighted, Ca | `cardarea.lua:18 (highlighted_limit default 5), cardarea.lua:148-159 (add_to_highlighted ha` | `ui/RunScreen.kt:1338-1354 (fun toggle) — `selected = if (i in selected) selected - i else ` | Vanilla: `if #self.highlighted >= self.config.highlighted_limit then card:highlight(false) else ... end`. Kotlin's toggle() only gates on `phase != Phase.ROUND`, Cerulean-Bell forced-card, and face-down reveal — nothing  |
| ❌ | major | Hand auto-sort by rank/suit — CardArea:sort (config.sort defaults 'desc') runs after every draw, and vanilla exposes a rank/suit toggle butt | `cardarea.lua:22 (config.sort default 'desc'), cardarea.lua:577-590 (CardArea:sort desc/asc` | `game/Deck.kt:25-29 (fun draw — no sort applied); no `sort`/`get_nominal`/rank-suit toggle ` | Deck.draw() just pops from a pre-shuffled ArrayDeque with no post-draw ordering step, and grep across ui/RunScreen.kt and game/*.kt finds no sort-by-rank or sort-by-suit call/button. Every vanilla hand renders sorted (ra |
| ❌ | major | Deck pile renders as one static card-back sprite + a text counter — vanilla shows the whole deck as a fanned stack of individually-positione | `cardarea.lua:412-424 (align_cards deck branch: per-card x/y parallax offset, shuffle_amt r` | `engine/EngineHost.kt:28 (`val deck = areaMoveable(Room.deck)` — plain Moveable); ui/RunScr` | Like discard, `deck` in EngineHost is a plain Moveable, never a CardArea, so align_cards' deck branch (which fans 52 individually-offset card-backs and adds a shuffle wobble on reshuffle) is entirely unported. The render |
| ❌ | major | Clicking the deck opens a full deck viewer — CardArea:click (self==G.deck → G.FUNCS.deck_info()) and Card:click's same-effect shortcut on th | `cardarea.lua:613-617 (CardArea:click), card.lua:4610-4620 (Card:click deck_info branch)` | `none — grep for DeckInfo/deckInfo/viewDeck/VIEWING_DECK across ui/RunScreen.kt returns not` | No click handler is wired on the deck sprite at RunScreen.kt:3605-3610, and no deck-viewer screen/composable exists in the codebase at all (confirmed via grep for the relevant vanilla symbol names). |
| ⚠️ | major | G.HIGHLIGHT_H (the Y-lift applied to a selected/highlighted card) is a fixed room-unit constant, 0.2 × G.CARD_H. | `globals.lua:276 (`self.HIGHLIGHT_H = 0.2*self.CARD_H`), globals.lua:274-275 (`CARD_W=2.4*3` | `engine/CardArea.kt:148 (`const val HIGHLIGHT_H = 1.0`)` | Kotlin's CARD_H itself is ported correctly and matches vanilla exactly (CardArea.kt:147 `CARD_H = 2.4*47.0/41.0` ≈ 2.7512), so the correct HIGHLIGHT_H should be 0.2*2.7512 ≈ 0.5502 — Kotlin hardcodes 1.0, roughly 1.8× to |
| ❌ | minor | align_cards' title / voucher(single) / voucher(multi) / hand-in-pack-open (TAROT_PACK/SPECTRAL_PACK/PLANET_PACK, a differently-tilted 0.4-fa | `cardarea.lua:436-449 (hand-in-pack branch), 466-479 (title/single-voucher), 480-494 (multi` | `engine/CardArea.kt:81-130 (`when (kind) { "hand" -> ...; "play" -> ...; else -> ... }` — o` | CardArea.kt's `kind` is only ever "joker", "hand", "consumeable"/isConsumeables, or "play" (EngineHost.kt:24-27); vouchers/pack-reveal titles are rendered through a separate static extracted-UI-tree mechanism per RunScre |
| ⚠️ | minor | The hand area's own Y target is itself low-pass filtered every frame (CardArea:move, a SECOND damping layer distinct from each card's per-ca | `cardarea.lua:229-241 (CardArea:move — `G.hand.T.y = 15*G.real_dt*desired_y + (1-15*G.real_` | `ui/RunScreen.kt:3208 (`host.hand.T.y = (if (s.scoring) Room.handScoringY else Room.handSel` | Vanilla eases the CardArea's own T.y toward the desired value with a 15/dt-rate filter EVERY frame (in addition to each card's individual moveXY spring toward that area's T), so the hand-slide (SELECTING_HAND ↔ scoring/t |
| ❌ | cosmetic | Joker reorder sort key includes a 'pinned' override (`- 100*(a.pinned and a.sort_id or 0)`) so pinned jokers hold a fixed slot regardless of | `cardarea.lua:528 (joker branch's table.sort comparator)` | `engine/CardArea.kt:142-143 (`fun xOrder() = cards.indices.sortedBy { cards[it].T.x + cards` | No `pinned`/`sort_id` concept exists anywhere in the Kotlin joker model (confirmed via grep across ui/RunScreen.kt, game/*.kt, engine/*.kt) — since the rebuild has no 'pin joker order' UI at all, this is effectively N/A  |
| ⚠️ | cosmetic | Joker/consumable click-to-select audio direction — vanilla plays 'cardSlide1' on ADD-to-highlighted and 'cardSlide2' on REMOVE-from-highligh | `cardarea.lua:140/147/154 (add_to_highlighted → play_sound('cardSlide1')), card.lua:4610-46` | `ui/RunScreen.kt:3555 (`SoundManager.play("highlight1")` unconditionally before `s.toggle(i` | Kotlin plays the same 'highlight1' sample every click; vanilla differentiates the sample by direction (cardSlide1 select / cardSlide2 deselect). **[corrected on verify: Vanilla side is accurately described: cardarea.lua add_to_highlighted (lines 140/147/154, including the joker/consumeable branch) plays play_sound('ca]** |

### UI panels (create_UIBox_* — HUD, run flow, overlays, meta/collection)
> Of vanilla's ~55 create_UIBox_* panel functions, 14 have been extracted to JSON and ported through the byte-verified UILayout.kt engine (HUD, HUD_blind x3, shop, round_evaluation, game_over, win, 5 booster-pack families, main_menu_buttons) — this is the faithful core of the run loop. The remaining ~40 functions (collection browsers x12, options/settings/generic_options, high_scores family, customize_deck, blind_popup, card_alert/notify_alert/card_unlock/deck_unlock, hand_tip, profile_button, dev-only tools) are either absent or replaced by hand-built Material/Compose approximations that don't share vanilla's tree structure, colours, or exact text; one verified regression makes the in-run Options/Run-Info HUD buttons render but be non-functional.

| st | sev | feature | vanilla | kotlin | evidence |
|---|---|---|---|---|---|
| ⚠️ | blocker | HUD sidebar button_area — 'run_info' and 'options' buttons (pause/settings + poker-hand reference) | `functions/UI_definitions.lua:1385 (run_info_button, button='run_info') and :1573/6218-area` | `rebuild/.../ui/HudSpec.kt:139-145 (buttonOnClick only handles toggle_shop/reroll_shop/buy_` | hud_tree.json (extracted, verified with a Python dump) DOES contain both button nodes with button:'run_info' and button:'options'. HudBind.buttonOnClick's when-expression only maps 5 shop button ids; 'run_info' and 'opti |
| ⚠️ | major | your_collection + 12 category sub-browsers (jokers/tarots/planets/spectrals/vouchers/seals/enhancements/editions/decks/tags/blinds/boosters) | `functions/UI_definitions.lua:3497 (your_collection) + :3535-3967 (12 dedicated create_UIBo` | `rebuild/.../ui/MainActivity.kt:152-213 (single Material-ish felt LazyVerticalGrid with 5 h` | No occurrences of your_collection_jokers/tarots/planets/spectrals/vouchers/seals/enhancements/editions/decks/tags/blinds/boosters found anywhere in the Kotlin source (grep). MainActivity's grid only covers 5 of 12 catego |
| ⚠️ | major | create_UIBox_options (in-run pause menu: Restart Run, Main Menu, Collection shortcut, Customize Deck shortcut, Settings, Stats, seed display | `functions/UI_definitions.lua:2208-2263 create_UIBox_options — a button list of Settings/Se` | `rebuild/.../ui/MainActivity.kt:218-263 (hand-built Material felt dialog: Sound ON/OFF, Mus` | Kotlin panel omits Restart Run, Main Menu, Collection, Customize Deck buttons and the seed display/copy row entirely — it's a strict subset (audio toggles + stats) explicitly commented as covering "the applicable subset" |
| ❌ | minor | create_UIBox_settings (graphics/audio/gameplay settings incl. VSync, colourblind mode, screenshake, etc.) | `functions/UI_definitions.lua:2265 create_UIBox_settings` | `none` | No 'settings' panel distinct from the audio-only options dialog; grep for create_UIBox_settings-equivalent content (VSync/colourblind/screenshake) turns up nothing in ui/*.kt. |
| ❌ | minor | create_UIBox_high_scores / online_high_scores / high_scores_filling / high_scores_row | `functions/UI_definitions.lua:196 (online_high_scores), :206 (high_scores_filling), :2591 (` | `none (MainActivity's 'Lifetime Stats' rows are a different, non-tree, hand-summarized 5-li` | grep for high_scores across ui/*.kt returns zero hits outside the audit doc. Rebuild has no per-run leaderboard/best-scores browser matching the vanilla structure (per-hand-type best scores, achievements panel), only a f |
| ❌ | minor | create_UIBox_hand_tip (example-card CardArea tooltip shown when hovering a poker-hand row) | `functions/UI_definitions.lua:3020-3040 create_UIBox_hand_tip` | `none` | grep for hand_tip / example-card preview returns nothing. RunInfoScreen's currentHandsTree (RunScreen.kt:4388-4418) shows level/chips/mult/played but never the example-card visual on hover/tap. |
| ❌ | minor | create_UIBox_card_alert (in-play floating '!' / bump-text alert, e.g. debuff notices, Sixth Sense trigger, etc.) | `functions/UI_definitions.lua:1859-1866 create_UIBox_card_alert(args) — a DynaText bump/rot` | `none` | grep for card_alert across the whole ui/ and engine/ trees returns nothing. |
| ❌ | minor | create_UIBox_notify_alert (achievement/joker/voucher/deck unlock toast) | `functions/UI_definitions.lua:145-194 create_UIBox_notify_alert(_achievement, _type)` | `none` | grep for notify_alert returns nothing anywhere in the rebuild. |
| ❌ | minor | create_UIBox_card_unlock / deck_unlock (the multi-stage juice_up→dissolve→materialize reveal animation when discovering a new joker/voucher/ | `functions/UI_definitions.lua:4143-4224 create_UIBox_card_unlock / create_UIBox_deck_unlock` | `none` | grep for card_unlock/deck_unlock returns nothing; the rebuild has no discovery/unlock system exposed in UI at all (consumables/jokers all appear immediately available rather than gated behind a discovery reveal). |
| ◑ | minor | create_UIBox_your_collection_decks / stake+deck run-setup picker | `functions/UI_definitions.lua:3869-3910 create_UIBox_your_collection_decks — a real flip-an` | `rebuild/.../ui/RunScreen.kt:3849-3889 DeckSelectScreen — hand-built Compose Column+Row lis` | Kotlin version is functionally complete (pick deck + stake, correct stake effect text) but structurally nothing like vanilla: no animated 52-card deck visual, no generate_UI() description box, plain scrollable list inste |
| ◑ | minor | create_UIBox_current_hands (the full 12-hand-type table, incl. secret hands, shown standalone — not just inside run-info) | `functions/UI_definitions.lua:3080-3497 create_UIBox_current_hands(simple) / :3042 current_` | `rebuild/.../ui/RunScreen.kt:4388-4418 currentHandsTree — hand-authored UI-DSL tree (not JS` | Structure matches closely (row bg colour darken(JOKER_GREY,0.1), level pip, chips/mult panels) but is hand-transcribed into Kotlin's UI DSL rather than extracted from the real tree, so any vanilla-side tweak (colours, pa |
| ⚠️ | minor | create_UIBox_blind_tag (the 'OR' skip-for-tag choice row attached to each blind_choice card, incl. run_info variant) | `functions/UI_definitions.lua:1461-1483 create_UIBox_blind_tag — an 'or' label + tag descri` | `rebuild/.../ui/RunScreen.kt:4304-4312 — hand-built Compose Row (BButton "Skip Blind → {tag` | Vanilla nests the skip-tag choice INSIDE each Small/Big blind_choice card (part of the same UIBox tree); the rebuild renders the extracted blind card tree via BlindSpec.forSlot(...) and then bolts a separate hand-built C **[corrected on verify: Vanilla ref is accurate: create_UIBox_blind_tag (UI_definitions.lua:1461-1483) builds an 'or'+tag+skip-button block that create_UIBox_blind_choice (li]** |
| ✅ | cosmetic | HUD core layout (dollars/chips, hands/discards, ante/round, hand-name/chip/mult readout) | `functions/UI_definitions.lua:1274-1417 create_UIBox_HUD` | `rebuild/.../ui/HudSpec.kt (HudBind) + assets/ui/hud_tree.json, rendered via UILayout.kt Re` | Extracted-JSON tree, engine-exact layout algorithm (UILayout.kt calc/setWh/setAlignments mirrors ui.lua's three passes), live RunState bindings via HudBind.read(). This is the one panel with actual pixel-verification cla |
| ✅ | cosmetic | HUD_blind (blind token + score target sub-box, per blind type Small/Big/Boss) | `functions/UI_definitions.lua:1210-1274 create_UIBox_HUD_blind` | `rebuild/.../ui/RunScreen.kt:2841 hudBlind(...) + assets/ui/blind_small_tree.json / blind_b` | hudBlind is called (not dead code, unlike hudRound/hudDollarsChips/hudButtons/hudHand) and its output is composited via RenderUIBoxAt into row_blind — matches vanilla's separate-attach architecture (HUD_blind attaches to |
| ✅ | cosmetic | Shop panel (create_UIBox_ shop tree: jokers/vouchers/boosters slots, Next Round, Reroll) | `UI_definitions.lua (shop tree function; not in the 55-list dump directly but extracted per` | `rebuild/.../ui/RunScreen.kt:3955 ShopPhase + assets/ui/shop_tree.json + shop_card_ui.json,` | All 5 shop-relevant button ids ARE handled in HudBind.buttonOnClick (contrast with the run_info/options gap) — Next Round, Reroll, Buy, Redeem, Open all wired to live RunState actions. |
| ✅ | cosmetic | round_evaluation / round_scores_row (cash-out panel) | `functions/UI_definitions.lua:1612-1629 create_UIBox_round_evaluation, :2933 round_scores_r` | `rebuild/.../ui/RunScreen.kt:3689 RoundEvalScreen + assets/ui/round_eval_tree.json via buil` | Extracted JSON tree + a dedicated EvalRowView binding live EvalRow data; matches the earlier fixed-crutch note (#178 cash-out panel). |
| ✅ | cosmetic | game_over / win screens | `functions/UI_definitions.lua:2862 create_UIBox_game_over, :2749 create_UIBox_win` | `rebuild/.../ui/RunScreen.kt:3830-3841 GameOverScreen/WinScreen + assets/ui/game_over_tree.` | Both trees extracted and button actions genuinely wired (contrast case proving the HUD run_info/options gap is a specific omission, not a systemic 'buttons never wired' pattern). |
| ✅ | cosmetic | Booster packs (arcana/celestial/standard/buffoon/spectral) | `functions/UI_definitions.lua:1629 (arcana), 1675 (spectral), 1721 (standard), 1767 (buffoo` | `rebuild/.../ui/RunScreen.kt:4146 PackOpenScreen + assets/ui/pack_arcana_tree.json, pack_bu` | Contradicts the stale UI_AUDIT.md claim of 'one generic pack-open screen' — five distinct extracted trees exist as separate assets and PackOpenScreen dispatches per pack family (packItemView / buildPack per node). |
| ✅ | cosmetic | main_menu_buttons (Play / Options / Collection on the felt) | `functions/UI_definitions.lua:6194 create_UIBox_main_menu_buttons` | `rebuild/.../ui/MainActivity.kt:132-148 + assets/ui/main_menu_tree.json via buildMenu/MenuB` | Extracted JSON tree rendered via RenderUIBoxNatural with real onClick wiring (onPlay/onOptions/onCollection) — contradicts the stale audit doc's 'replaced by Material3 dev launcher, fully non-faithful' claim, which was a |
| ❌ | cosmetic | create_UIBox_customize_deck (face-card collab art picker + colourblind toggle) — distinct from the rebuild's deck-select screen | `functions/UI_definitions.lua:2513-2543 create_UIBox_customize_deck (tabs by suit, calls G.` | `rebuild/.../ui/RunScreen.kt:3849-3889 DeckSelectScreen — this is actually the vanilla RUN-` | No reference to 'customize_deck', suit tabs, or collab face-card art anywhere in the Kotlin UI code. Cosmetic-only vanilla feature (face-card art variants); DeckSelectScreen in Kotlin is a different vanilla screen entire |
| ❌ | cosmetic | create_UIBox_blind_popup (per-blind info popup shown when tapping a blind icon, e.g. in run-info/collection) | `functions/UI_definitions.lua:4093-4143 create_UIBox_blind_popup(blind, discovered, vars)` | `none — RunInfoBlinds (RunScreen.kt:4450-4473) shows a static list of the CURRENT ante's 3 ` | grep for blind_popup returns nothing; the Kotlin equivalent inlines the info instead of a discrete popup panel. |
| ❌ | cosmetic | create_UIBox_debug_tools / test_framework / usage / character_button / main_menu_competittion_name / profile_button / exit_CTA / small_cta / | `functions/UI_definitions.lua:5 (debug_tools), :2344 (test_framework), :2436 (usage), :621 ` | `none` | None of these referenced anywhere in the Kotlin source. debug_tools/test_framework/usage are LÖVE-dev-only tooling (not player-facing); character_button is used by customize_deck (already tracked separately) and stake pi |

### Rendering & shaders — card/joker edition shimmer, dissolve/materialize, particles, felt/CRT post-processing, hover-tilt (vanilla shaders in src/vanilla/resources/shaders/*.fs, engine/sprite.lua, engine/particles.lua, card.lua draw)

| st | sev | feature | vanilla | kotlin | evidence |
|---|---|---|---|---|---|
| ❌ | major | Full-screen CRT post-process filter (scanlines, vignette distortion, bloom, noise) applied over the entire play canvas every frame | `src/vanilla/resources/shaders/CRT.fs (153 lines); game.lua:2940-2952 (G.SHADERS['CRT']:sen` | `none (only mention: ui/MainActivity.kt:216 comment 'CRT/shake ... Android-inapplicable' fo` | Vanilla always renders the run through CRT.fs (distortion_fac/scale_fac/bloom_fac/noise_fac/scanlines/crt_intensity uniforms) at intensity 70/100 by default -- this is the persistent 'retro CRT' look of the whole game. K |
| ❌ | major | debuff.fs (red diagonal-slash desaturation overlay on boss-suit/face-debuffed playing cards) and played.fs (darkened/desaturated overlay on  | `src/vanilla/resources/shaders/debuff.fs, played.fs; card.lua:4526-4537 (self.debuff -> 'de` | `none — grep for isCardDebuffed/DebuffOverlay/isDisabled/greyOut/desaturate in ui/RunScreen` | Boss blinds THE_CLUB/THE_GOAD/THE_WINDOW/THE_HEAD/THE_PLANT/THE_PILLAR/VERDANT_LEAF debuff specific cards for scoring (mechanically implemented in game/Score.kt via Debuff.DebuffSuit/DebuffCards), and CRIMSON_HEART disab |
| ❌ | major | Particles engine (engine/particles.lua) — the whole square-confetti particle system used for card dissolve/shatter/materialize debris, boost | `engine/particles.lua (full Particles class, 177 lines); card.lua:1990-2205 (Card:explode_c` | `none — grep -rln "Particle" across ui/game/kotlin returns no files` | Every card destroy/shatter/materialize animation, and every single booster-pack opening, spawns a physically-simulated particle burst (velocity/lifespan/scale-ease/colour per particle) in vanilla. Kotlin's RunScreen.kt w |
| ❌ | minor | Card hover-tilt / mouse-proximity vertex bulge (the pseudo-3D 'skew' look) present in every card shader's `#ifdef VERTEX position()` functio | `src/vanilla/resources/shaders/skew.fs (dedicated shader) plus the identical position() ver` | `ui/Editions.kt:12-13 (comment explicitly: 'the dissolve_mask + hover-bulge vertex pass are` | Vanilla's vertex shader displaces card corner vertices toward/away from the mouse cursor when hovering, giving the subtle screen-space 'lean toward the cursor' 3D look every joker/card has on desktop. Editions.kt documen |
| ❌ | minor | voucher.fs (glossy shimmer overlay for eternal/perishable/rental/gold-seal card stickers and shop voucher cards) | `src/vanilla/resources/shaders/voucher.fs; card.lua:4473-4501 (Gold seal, eternal, perishab` | `ui/RunScreen.kt:3567-3570 — enhancement/seal shown as a flat BTxt(...) text badge with sol` | Vanilla renders seals/stickers as small animated glossy icon sprites (voucher.fs shimmer over a dissolve-masked-in sticker sprite); Kotlin substitutes a plain coloured text-badge (e.g. "G" on gold background) — informati |
| ❌ | minor | gold_seal.fs — animated gold-shimmer specifically for the Gold seal sticker | `src/vanilla/resources/shaders/gold_seal.fs (21 lines, sin-wave gold shimmer keyed by gold_` | `ui/RunScreen.kt:3569 (Seal.GOLD renders as the same flat text badge as any other seal, no ` | Gold seal is meant to visually shimmer/gleam distinctly from Red/Blue/Purple seals; Kotlin gives it the identical static badge treatment as the other three seal colours. |
| ❌ | minor | booster.fs — glow/shimmer overlay applied to booster-pack cover sprites | `src/vanilla/resources/shaders/booster.fs (97 lines, same dissolve_mask + burn-colour struc` | `none — grep for booster/BOOSTER_AGSL/boosterRenderEffect across kotlin/ returns nothing` | Booster pack sprites (Arcana/Celestial/Spectral/Standard/Buffoon covers) get an animated shimmer in vanilla shop/pack-open UI; Kotlin's ShopArt.kt/RunScreen pack rendering draws the flat pack sprite with no shader pass. |
| ⚠️ | minor | Card drop-shadow directional parallax (shadow shifts horizontally based on the card's position relative to room centre, not just straight do | `engine/moveable.lua:461 (self.shadow_parrallax.x = (self.T.x + self.T.w/2 - G.ROOM.T.w/2)/` | `ui/RunScreen.kt:3061-3070 (CardFace shadow only applies `translationY`, no x-offset at all` | Vanilla: `self.VT.x = self.VT.x - _draw_major.shadow_parrallax.x*_shadow_height` gives cards on the left/right of the play area a shadow that leans away from room-centre; Kotlin's shadow is always a straight-down offset  |
| ❌ | cosmetic | hologram.fs — the double-image ghost/hologram effect for jokers with the 'Hologram' ability (floating soul-icon duplication) | `src/vanilla/resources/shaders/hologram.fs (128 lines); card.lua:4512-4519 (ability.name=='` | `none — grep for hologram/HOLOGRAM_AGSL/hologramRenderEffect across kotlin/ returns nothing` | This is a rare per-joker special-case visual (only the Hologram joker's floating soul icon uses this specific shader instead of the shared dissolve pass); no Kotlin equivalent exists. |
| ❌ | cosmetic | vortex.fs — swirl/distortion applied to cards during the deck-shuffle/draw 'vortex' animation and the boss blind card-back warp | `src/vanilla/resources/shaders/vortex.fs (20 lines); game.lua:1462,1467 (card.vortex=true, ` | `none — grep for vortex/VORTEX_AGSL/vortexRenderEffect across kotlin/ returns nothing` | A brief warp/distortion plays on cards as they're drawn from the deck each round; entirely absent, so drawn cards simply pop into place with no shuffle-swirl transition. |
| ❌ | cosmetic | splash.fs + flash.fs — the boot/intro splash-screen swirling-smoke + white-flash animation shown on game launch | `src/vanilla/resources/shaders/splash.fs (55 lines), flash.fs (22 lines); game.lua:1394-141` | `none` | No intro/boot splash sequence exists in the Kotlin app at all (consistent with the known gap that there's no real main menu, per rebuild/docs/UI_AUDIT.md); the splash/flash shaders that would drive it are correspondingly |
| ❌ | cosmetic | flame.fs — animated flame sprites rendered behind the HUD chip/mult counters that scale in intensity with chip_flames/mult_flames score magn | `src/vanilla/resources/shaders/flame.fs (68 lines); functions/button_callbacks.lua:1963-200` | `ui/HudSpec.kt:14,205,213 — flame_handler anchor nodes exist (w=0/h=0 placeholders) but exp` | This is a self-documented, already-tracked gap (Kotlin's own code comments say 'flame visual pending'), not a silent omission — the HUD layout preserves the zero-size anchor nodes so the effect can be dropped in later. |

### Save/load & profiles
> The rebuild covers the mechanical shell of run persistence — a single JSON snapshot written/read on Dispatchers.IO, with atomic write and lenient schema decoding — but only for the two phases where vanilla's UI conventionally offers a "Continue" (shop and blind-select); it omits the periodic 30s in-round autosave, the entire unlock/discovery/meta-progression system, the 3-slot profile model, and the rich per-profile career-stat/high-score/usage surface, collapsing all of that to two SharedPreferences booleans and five aggregate counters. Roughly the outer 20% of vanilla's save subsystem (run-snapshot I/O) is faithfully ported; the inner 80% (mid-round durability, unlocks, profiles, stats) is either missing or drastically reduced.

| st | sev | feature | vanilla | kotlin | evidence |
|---|---|---|---|---|---|
| ❌ | blocker | Mid-round autosave (SELECTING_HAND state) — run is durable during actual card play, not just at shop/blind-select | `functions/misc_functions.lua:1452-1485 (save_run culls cardAreas{hand,deck,play,discard,jo` | `ui/RunScreen.kt:2514-2528 (LaunchedEffect(s.phase, s.blindIndex, s.money, s.consumables.si` | Vanilla: save_run() fires every time the hand-draw completes in SELECTING_HAND and the throttled writer flushes it to disk within F_SAVE_TIMER (30s) seconds regardless of state, so a mid-round kill loses at most ~30s of  |
| ❌ | blocker | Unlock/discovery/alert persistence (meta.jkr) — cross-run progression: which jokers/backs/vouchers are unlocked, which items have been disco | `engine/save_manager.lua:18-49 (save_progress request merges UDA 'u'/'d'/'a' flags into met` | `none (grep -rln "unlocked\|discovered" over rebuild/app/src/main/kotlin/systems/balatro → ` | Vanilla persists a meta.jkr per profile that gates which jokers/backs/vouchers are purchasable at all (unlock conditions) and tracks first-time discovery for the Collection screen + 'NEW' notification badges (unlock_noti |
| ❌ | major | 3-slot profile system with per-profile switch/create/delete and independent save/meta/stats per slot | `globals.lua:260-264 (self.PROFILES = {{},{},{}}); game.lua:845-925 Game:load_profile(_prof` | `save/StatsStore.kt:17 (single hardcoded "balatro" SharedPreferences bucket, no profile ind` | Vanilla stores three independent numbered profile directories the player can switch between/delete from a tab UI, each with its own progression, save, and stats. The Kotlin rebuild has exactly one implicit profile: one s |
| ◑ | major | Per-profile career stats, high scores, and usage tallies (career_stats c_* counters, high_scores by category, joker/consumable/voucher/hand/ | `game.lua:857-912 (temp_profile shape: high_scores{hand,furthest_round,furthest_ante,most_m` | `save/StatsStore.kt:10-15 (Stats{games, wins, bestAnte, totalHands, bestScore} — 5 fields t` | Vanilla's profile.jkr tracks dozens of distinct stat dimensions (best poker hand type played, most money held, boss-defeat streak, per-deck win counts gating deck-stake unlocks, per-joker/voucher/consumable usage counts  |
| ⚠️ | major | Explicit New-Run vs Continue choice from the main menu — Play always offers a fresh run tab even when a save exists, Continue tab is only en | `functions/UI_definitions.lua:5297-5320 (G.UIDEF.run_setup shows 'New Run' and 'Continue' a` | `ui/MainActivity.kt:126-144 (single onPlay handler: `showRun = true`, no branch for new-vs-` | Vanilla gives the player an explicit choice every time they open the Play screen: start a brand-new run (deck/stake picker) or continue the exact saved run, with Continue disabled/greyed when no valid save exists. In the |
| ◑ | minor | settings.jkr — full G.SETTINGS persistence (colourblind option, graphics, language, tutorial progress, window state) alongside the save subs | `game.lua:11-40 Game:start_up (loads settings.jkr, merges every G.SETTINGS.* key); engine/s` | `ui/MainActivity.kt:221,236-247 (only two booleans — "sfx" and "music" — persisted via getS` | Vanilla's save subsystem round-trips the entire settings tree as part of the same save_progress/save_settings save-thread requests that also persist unlocks and profile — colourblind mode, graphics scaling, tutorial_prog |
| ◑ | minor | Save version gating — vanilla wipes all profile save/meta files on a version jump below 1.0.0, and gates 'Continue' eligibility on saved VER | `game.lua:14-38 (version-gated wipe of profile.jkr/save.jkr/meta.jkr/unlock_notify.jkr acro` | `save/RunSnapshot.kt:103,107,111 (schemaVersion field defined with SCHEMA_VERSION=1, writte` | Vanilla explicitly compares the saved VERSION string to gate whether an old save is safe to resume, and force-wipes stale saves on a major version bump. Kotlin's schemaVersion is written but write-only — dead field, no e |
| ✅ | cosmetic | Run snapshot storage mechanics for the phases it does cover (shop stock, deck composition with per-card enhancement/seal/edition, joker FJok | `cardarea.lua:619-654 (CardArea:save/load round-trips every card incl. enhancement/seal/edi` | `save/RunSnapshot.kt:15-113 (CardSnap/JokerSnap/ShopItemSnap/etc. capture enh/seal/edition/` | For the two phases it actually saves at (shop/blind-select), the rebuild's snapshot is a reasonably faithful and in some respects more robust port: it captures per-card enhancement/seal/edition/permaBonus and exact shop  |

### audio + input/controller
> Covers the full vanilla sound_manager.lua state machine (PLAY_SOUND/SET_SFX/MODULATE/RESTART_MUSIC/AMBIENT), all ~45 distinct play_sound cue roots found across every vanilla call site, the music-track state machine (music1-5 + pitch_mod), the three-slider volume model, and the touch-vs-mouse branch of controller.lua (click/drag/hover semantics, hold-to-peek). Matched each against the two Kotlin audio singletons and every SoundManager/MusicManager call site plus the Compose gesture/click wiring in RunScreen.kt, HudSpec.kt and UILayout.kt. Gamepad/HID/focus-navigation portions of controller.lua were read but treated as out-of-scope for a touch-only rebuild rather than audited item-by-item.

| st | sev | feature | vanilla | kotlin | evidence |
|---|---|---|---|---|---|
| ❌ | major | Universal 'button' click SFX on every clickable UIBox node (base UIElement:click() calls play_sound('button',1,0.3) unconditionally for any  | `engine/ui.lua:989 (UIElement:click)` | `rebuild/.../ui/UILayout.kt:326-366 (renderNode, R/C/ROOT/B branch: cfg.onClick.invoke() wi` | Vanilla: every UIBox button press (Reroll, Next Round, blind Select/Skip, pack Choose/Skip, run-info tabs, options controls, main menu) routes through the base click handler and gets 'button' SFX for free. Kotlin: `grep  |
| ❌ | major | Music state machine: desired_track selects among music1 (default), music2 (any booster pack open), music3 (meteor/sparkle pack variant), mus | `functions/misc_functions.lua:719-780 (modulate_sound: desired_track selection, PITCH_MOD) ` | `rebuild/.../audio/MusicManager.kt:1-55 (single hardcoded TRACK="music1.ogg", started on Ac` | Vanilla explicitly branches: `(G.shop and 'music4') or (G.GAME.blind and G.GAME.blind.boss and 'music5') or (booster_pack and 'music2') or ('music1')`, crossfading over ~0.33s. Kotlin's MusicManager has exactly one const |
| ❌ | major | Card hover/peek SFX + hold-to-peek tooltip on touch: Card:hover() plays play_sound('paper1', random pitch, 0.35) and (on HID.touch, after a  | `card.lua:4306-4327 (Card:hover) + engine/controller.lua:358-366,392-419 (touch-only hover ` | `rebuild/.../ui/RunScreen.kt:3538-3582 (hand card Box: only `.clickable` — tap toggles sele` | On mobile (no mouse), vanilla's entire card-reading affordance is hold-to-peek: press and hold a card ~0.1s to hear the paper riffle and see its full ability text. Kotlin's hand cards only support tap-to-select (SoundMan |
| ◑ | major | Full SFX cue roster (~45 distinct sound_code roots incl. numeric-suffix families: crumple1-5, glass1-6, voice1-11, foil1-2, multhit1-2, coin | `repo-wide `play_sound(` call sites (card.lua, game.lua, blind.lua, cardarea.lua, back.lua,` | `rebuild/.../audio/SoundManager.kt:15-18 (NAMES list, 17 entries) + assets/sounds/ (17 .ogg` | Only button, cancel, highlight1, card1, cardSlide1, chips1, chips2, multhit1, coin1, coin3, tarot1, whoosh1, gong, timpani, generic1, paper1, glass1 are bundled/loadable. Roughly two-thirds of vanilla's distinct cues — i |
| ⚠️ | major | Three independent continuous volume sliders: master (SOUND.volume, default 50), music_volume (default 100), game_sounds_volume (default 100) | `globals.lua:164-168 (SOUND table) + functions/UI_definitions.lua:2329-2330 (create_slider ` | `rebuild/.../ui/MainActivity.kt:219-246 (two boolean BButton toggles: "Sound: ON/OFF", "Mus` | Vanilla: `s.sound:setVolume(s.original_volume*(sound_settings.volume/100.0)*(sound_settings.game_sounds_volume/100.0))` — three independently adjustable 0-100 sliders. Kotlin only exposes on/off; there is no Slider/volum |
| ⚠️ | major | Run-win sting: play_sound('win') on Game:win_game | `functions/state_events.lua:33 (win_game(): play_sound('win'))` | `rebuild/.../ui/RunScreen.kt:2532 (Phase.WIN -> SoundManager.play("timpani"))` | Vanilla's dedicated 'win' cue plays once on ante-8 clear. Kotlin substitutes 'timpani', which in vanilla is actually the card-unlock/discovery fanfare (`play_sound('timpani', 0.8, 1.8)` in functions/UI_definitions.lua:41 |
| ⚠️ | major | Run-loss sting: play_sound('negative', 0.5, 0.7) + play_sound('whoosh2', 0.9, 0.7) on Game:update_game_over | `game.lua:3593-3594 (Game:update_game_over)` | `rebuild/.../ui/RunScreen.kt:2533 (Phase.OVER -> SoundManager.play("gong"))` | Vanilla plays a two-layer 'negative'+'whoosh2' sting on run loss. Kotlin substitutes a single 'gong', which in vanilla only ever occurs in Anaglyph/Plasma Deck-specific effect contexts (back.lua:134-136), never on generi |
| ❌ | minor | Shop reroll SFX: play_sound('coin2') + play_sound('other1') on reroll | `functions/button_callbacks.lua:2879-2880 (shop reroll handler)` | `rebuild/.../ui/HudSpec.kt:141 ("reroll_shop" -> ({ s.reroll() })) + RunScreen.kt:2364-2390` | reroll() itself has no SoundManager call, and it's invoked exclusively through the generic UIBox click path (HudSpec.buttonOnClick -> UILayout.renderNode) which per the first finding plays no sound at all, so shop reroll |
| ❌ | minor | Drag-to-reorder hand cards: dragging any card (mouse or touch) live-reorders CardArea.cards via align_cards' centre-x sort, with 'cardSlide1 | `engine/moveable.lua:217-248 (Moveable:drag) + cardarea.lua:410-464 (align_cards' x-positio` | `rebuild/.../ui/RunScreen.kt:3550-3559 (hand card Box: `.clickable` only, no `.pointerInput` | Jokers (RunScreen.kt:3348-3362) and consumables (3455-3469) both got the engine drag-physics port (`m.startDrag`/`m.dragBy`/`m.stopDrag` + "card1" drag-start SFX) per the 2026-07-02 CardArea work, but the hand row block  |
| ❌ | minor | Ambient dynamic sound layer: ambientFire1/2/3 (volume tied to chip/mult flame VFX intensity) and ambientOrgan1 (volume tied to log(earned_sc | `functions/misc_functions.lua:736-773 (AMBIENT()/ambient_sounds volfuncs, driven every fram` | `none — no ambient*.ogg assets, no AMBIENT-equivalent code in audio/ or RunScreen.kt` | These are continuous atmospheric layers reacting to in-round scoring intensity (fire crackle rises with joker "flame" VFX, organ swells as score approaches the blind requirement); no equivalent mechanism, asset, or hook  |
| ⚠️ | minor | Cash-out SFX: play_sound("coin7") on G.FUNCS.cash_out | `functions/button_callbacks.lua:2945 (cash_out)` | `rebuild/.../ui/RunScreen.kt:3703,3740 (BButton("Cash Out...", sound="coin1") { s.cashOut()` | Vanilla's cash-out uses the distinct 'coin7' cue (not the generic buy blip). Kotlin's two Cash Out buttons pass `sound="coin1"` — the same generic single-purchase blip used for buying jokers/vouchers/tarots — rather than |
| ❌ | cosmetic | Gamepad/controller input (axis cursor, dpad focus navigation, snap-to-focus, coyote-time card selection, button_registry, HID auto-detection | `engine/controller.lua (whole file: set_gamepad, update_axis, navigate_focus, capture_focus` | `none in rebuild/.../ui — touch-only Compose gesture handling throughout RunScreen.kt` | The rebuild is a touch-native Android app with no gamepad/focus-navigation layer; this is a large fraction of controller.lua (dpad hand-navigation, slider hold-repeat, tab/cycle focus_args, vibration feedback) with zero  |

### run setup & meta screens (main menu, deck/stake select, options/settings, high scores, your collection, challenges, deck/stake definitions)
> The core run-launch loop is covered — the 8 vanilla stake modifiers' ANTE/discard/no-blind-reward math is faithfully ported and 14 of 15 decks apply correct starting-run deltas — but nearly every surrounding meta system is either a stripped-down placeholder or entirely absent: no deck/stake unlock progression, no discovery/collection-lock system, no in-run Options overlay (restart/seed/mid-run collection access), no challenges (37 of them), no seeded runs, no customize-deck/colourblind screen, and Your Collection covers only 5 of ~12 vanilla categories. Plasma Deck (a full, mechanically distinct starting deck) is missing outright. Roughly a quarter of the enumerated vanilla surface is faithfully ported; the rest is missing or a minimal stand-in.

| st | sev | feature | vanilla | kotlin | evidence |
|---|---|---|---|---|---|
| ❌ | major | Plasma Deck (15th deck): ante_scaling=2 config + trigger_effect 'final_scoring_step' that averages chips/mult ((chips+mult)/2 each) with the | `src/vanilla/game.lua:641 (config), src/vanilla/back.lua:121-171 (trigger_effect balancing)` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:78-97 (DeckVariant enum — 14 e` | Vanilla: 'b_plasma= {... config = {ante_scaling = 2}, unlock_condition = {type="win_stake", stake=5}}' plus a scoring hook that sets 'args.chips = math.floor(tot/2); args.mult = math.floor(tot/2)'. Kotlin's DeckVariant e |
| ❌ | major | Stake 4/7/8 shop-generation modifiers (enable_eternals_in_shop / enable_perishables_in_shop / enable_rentals_in_shop) — random eternal/peris | `src/vanilla/game.lua:2055,2058,2059 (stake sets modifiers); src/vanilla/functions/common_e` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:3846` | Vanilla rolls a pseudorandom 'eternal_perishable_poll' per shop/pack Joker and applies set_eternal/set_perishable/set_rental when the corresponding stake modifier is on. Kotlin's own stake-select caption text literally r |
| ❌ | major | Deck unlock progression (discover_amount / win_deck / win_stake unlock_condition gates 14 of 15 decks; only Red + Challenge start unlocked) | `src/vanilla/game.lua:628-644 (unlock_condition per deck); src/vanilla/back.lua:36-53 (lock` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:3850-3889 (DeckSelectScreen — ` | Vanilla: only b_red/b_challenge are 'unlocked=true'; all others require discovering N items, winning a specific deck, or clearing a stake. Kotlin's DeckSelectScreen renders every DeckVariant as an immediately-clickable r |
| ❌ | major | Stake unlock progression (only White Chip starts unlocked; each higher stake requires clearing the previous one) + per-deck max-stake gating | `src/vanilla/game.lua:253-260 (stake_red..stake_gold all 'unlocked = false'); src/vanilla/f` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:3853,3865-3867 (stake stepper:` | Kotlin's stake stepper lets the player freely pick stake 1-8 for ANY deck from the first launch — vanilla requires winning stake N on that specific deck to unlock stake N+1 for it, and Gold Chip is the hardest-earned sta |
| ❌ | major | In-run Options overlay (create_UIBox_options when G.STAGE==RUN): 'Start New Run' restart button, 'Main Menu' button, current-seed display +  | `src/vanilla/functions/UI_definitions.lua:2208-2263` | `rebuild/app/src/main/kotlin/systems/balatro/ui/MainActivity.kt:265-267 (showRun renders Ru` | In vanilla, pausing mid-run opens Options with 'Start New Run', 'Main Menu', a copyable seed readout, and buttons to Your Collection/Stats/Customize Deck — all reachable without abandoning the run's save. In the rebuild, |
| ❌ | major | Card/item discovery-lock system: undiscovered items render as '?' (bypass_discovery_ui=false) until first encountered in a run; unlock_condi | `src/vanilla/functions/common_events.lua:2126-2130 (discover flag on card creation); back.l` | `none` | grep for 'discovered'/'Discover' across rebuild/app/src/main/kotlin/systems/balatro/ returns zero matches — the Collection grid (MainActivity.kt:163-169) always shows every Joker/Tarot/Planet/Spectral/Voucher's real name |
| ❌ | major | Challenge system: 37 challenges (challenges.lua) with fixed starting jokers/consumables/vouchers/deck and rule modifiers, challenge list/des | `src/vanilla/challenges.lua:1-738 (37 'id = "c_..."' entries); src/vanilla/functions/UI_def` | `none` | grep for 'challenge'/'Challenge' across rebuild/app/src/main/kotlin/systems/balatro/ returns zero matches. The entire challenge game mode — its own tab in run_setup, 37 predefined challenge configs, and the Challenge Dec |
| ◑ | major | Your Collection: 12 vanilla subcategories (jokers, tarots, boosters, planets, spectrals, vouchers, seals, enhancements, editions, decks, tag | `src/vanilla/functions/UI_definitions.lua:3497-4093 (create_UIBox_your_collection_* — 3535 ` | `rebuild/app/src/main/kotlin/systems/balatro/ui/MainActivity.kt:161-168 (tabs = Jokers, Tar` | 5 of 12 vanilla collection categories are ported (Jokers/Tarots/Planets/Spectrals/Vouchers). Boosters, Seals, Enhancements, Editions, Decks, Tags, and Blinds have no corresponding tab or grid anywhere in MainActivity.kt' |
| ◑ | major | High Scores screen: hand / furthest_round / furthest_ante / poker_hand / most_money / win_streak rows + progress bars (collection %, deck_st | `src/vanilla/functions/UI_definitions.lua:2591-2621 (create_UIBox_high_scores), 2623-2692 (` | `rebuild/app/src/main/kotlin/systems/balatro/save/StatsStore.kt (whole file); rebuild/app/s` | StatsStore.Stats only tracks games/wins/bestAnte/totalHands/bestScore — vanilla's six score categories (best hand, furthest round, furthest ante, best poker hand score, most money held, win streak) collapse to 'Runs/Wins |
| ◑ | major | Settings: gamespeed, play/discard button position, rumble, screenshake slider, stake-stickers display toggle, high-contrast/colourblind togg | `src/vanilla/functions/UI_definitions.lua:2299-2331 (G.UIDEF.settings_tab 'Game' and 'Audio` | `rebuild/app/src/main/kotlin/systems/balatro/ui/MainActivity.kt:218-263 (showSettings block` | Kotlin's Options dialog offers exactly two binary toggles ('Sound: ON/OFF', 'Music: ON/OFF'). Vanilla's Game tab has 8 distinct settings (gamespeed cycle, play/discard position, rumble, screenshake slider, run_stake_stic |
| ⚠️ | major | Explicit New Run vs Continue choice with saved-run summary (round/ante/money/best-hand/seed) before committing | `src/vanilla/functions/UI_definitions.lua:5297-5337 (run_setup tab bar), 5961-6072 (Continu` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:2489-2513` | Vanilla always shows a tabbed choice ('New Run' / 'Continue') so the player can knowingly start fresh even with a save present. Kotlin's onPlay handler auto-resumes silently whenever a save file exists ('null -> ... s.re |
| ❌ | minor | Seeded-run entry: 'Seeded Run' toggle + text-entry seed field so players can reproduce/share a run | `src/vanilla/functions/UI_definitions.lua:6111-6114 (create_toggle 'k_seeded_run' + G.run_s` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:857 (runSeed is a random UUID ` | 'var runSeed: String = java.util.UUID.randomUUID().toString().take(8).uppercase(); private set' — no UI anywhere exposes seed entry; DeckSelectScreen has no seed field. |
| ❌ | minor | create_UIBox_customize_deck: per-suit face-card 'collab' art cycle + high-contrast/colourblind toggle for card faces | `src/vanilla/functions/UI_definitions.lua:2513-2589` | `none` | No 'customize' or 'collab' or 'colourblind' identifiers anywhere in rebuild/app/src/main/kotlin/systems/balatro/ — the whole screen (and its Options-menu entry point 'b_customize_deck') is absent. |
| ✅ | cosmetic | Stake scaling tiers: stake>=3 doubles (scaling=2) and stake>=6 triples (scaling=3) the Ante blind-score tables | `src/vanilla/game.lua:2054,2057` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:1039-1046` | Kotlin's three-tier anteTables array selected by 'val tier = if (stakeLevel >= 6) 2 else if (stakeLevel >= 3) 1 else 0' mirrors vanilla's scaling=2/scaling=3 stake gates, with matching per-ante base values (e.g. ante-2 s |
| ✅ | cosmetic | Stake>=2: Small Blind gives no cash reward | `src/vanilla/game.lua:2050-2053 (modifiers.no_blind_reward.Small = true)` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:1134-1136` | Kotlin: 'slotIdx == 0 && stakeLevel >= 2 -> 0' zeroes the Small Blind reward exactly as vanilla's Red-Chip-and-above modifier does. |
| ✅ | cosmetic | Stake>=5: -1 discard per round | `src/vanilla/game.lua:2056` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:1194` | 'if (stakeLevel >= 5) baseDiscards -= 1' matches vanilla's Blue-Chip-and-above 'starting_params.discards - 1'. |
| ✅ | cosmetic | Red/Blue/Yellow/Black/Painted/Green/Anaglyph/Magic/Ghost/Zodiac/Nebula/Abandoned/Checkered deck starting-run deltas (hands, discards, money, | `src/vanilla/game.lua:628-641 (per-deck config), src/vanilla/back.lua:174-278 (apply_to_run` | `rebuild/app/src/main/kotlin/systems/balatro/ui/RunScreen.kt:83-96 (DeckVariant enum), 1189` | Each remaining deck's numeric deltas match its vanilla config field-for-field (e.g. BLACK: jokerSlotDelta=1/handsDelta=-1 vs config={hands=-1, joker_slot=1}; PAINTED: handSizeDelta=2/jokerSlotDelta=-1 vs config={hand_siz |
| ❌ | cosmetic | Multi-profile system: 3 save-slot profiles with name entry, load/create/delete, per-profile progress, 'unlock all' debug action | `src/vanilla/functions/UI_definitions.lua:5339-5440 (G.UIDEF.profile_select / profile_optio` | `none` | grep for 'profile'/'Profile' in rebuild/app/src/main/kotlin/systems/balatro/ only matches Telemetry.kt and RunSnapshot.kt (unrelated usages) — the rebuild has a single implicit save (SaveIo.FILE_NAME), no profile concept |
| ◑ | cosmetic | Main menu chrome: language selection, Discord/Twitter icon links, Quit button | `src/vanilla/functions/UI_definitions.lua:6194-6243 (create_UIBox_main_menu_buttons)` | `rebuild/app/src/main/kotlin/systems/balatro/ui/MainActivity.kt:143-147 (buildMenu(mroot, M` | MenuBind only wires Play/Options/Collection callbacks; there is no language picker, no Discord/Twitter buttons, and no Quit button (reasonable on Android where the OS back-gesture substitutes for quit, but it is a diverg |
